fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pydantic-settings>=2.0
torch>=2.1
torchvision>=0.16
ultralytics>=8.2
Pillow>=10.0
numpy>=1.24
scipy>=1.10

# Grounding DINO (open-vocabulary detection)
groundingdino-py>=0.4
transformers>=4.38,<5

# V4.2 Phase 3: DINOv2 Foundation-Encoder + Linear-Heads (ueber transformers)
# Keine zusaetzliche Dependency noetig — DINOv2 wird via AutoModel geladen.

# SAM (pixel-precise segmentation)
segment-anything>=1.0

# NVIDIA GPU-Beschleunigung
tensorrt>=10.0          # TensorRT fuer YOLO: .pt -> .engine Export (2-4x schneller)
pynvvideocodec>=2.0     # NVDEC Hardware-Video-Dekodierung (dedizierter HW-Decoder)
av>=12.0                # PyAV: Software-Fallback fuer NVDEC (immer benoetigt)

# Video Super Resolution (optional, opt-in via SEWER_SIDECAR_VSR_ENABLED=1)
# realesrgan>=0.3       # Real-ESRGAN Upscaling (+ pip install basicsr)
# Gewichte: models/RealESRGAN_x4plus.pth (https://github.com/xinntao/Real-ESRGAN/releases)
