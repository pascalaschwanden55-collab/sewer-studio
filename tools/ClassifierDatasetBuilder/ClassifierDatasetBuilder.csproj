<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reines CLI-Tool: nur Application (EvalSetManifestHasher + ClassifierDatasetPlan).
         Keine UI/Infrastructure noetig -> entkoppelt von der laufenden App. -->
    <ProjectReference Include="..\..\src\AuswertungPro.Next.Application\AuswertungPro.Next.Application.csproj" />
  </ItemGroup>
</Project>
