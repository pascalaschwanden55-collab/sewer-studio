<UserControl x:Class="AuswertungPro.Next.UI.Controls.DistributionTreePreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:AuswertungPro.Next.UI"
             xmlns:local="clr-namespace:AuswertungPro.Next.UI.Controls"
             x:Name="Root">
    <UserControl.Resources>
        <local:DistributionTreeDepthToMarginConverter x:Key="DepthToMargin"/>
    </UserControl.Resources>
    <Border Background="{DynamicResource SurfaceSubtleBrush}"
            BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1"
            CornerRadius="6" Padding="10,8">
        <ItemsControl ItemsSource="{Binding Nodes, ElementName=Root}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"
                                Margin="{Binding Depth, Converter={StaticResource DepthToMargin}}">
                        <ui:FluentIcon x:Name="Icon" Glyph="&#xE8B7;" FontSize="13"
                                       Foreground="{DynamicResource AccentBrush}" Margin="0,0,7,0"/>
                        <TextBlock Text="{Binding Label}" FontSize="12" FontFamily="Consolas"
                                   Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Kind}" Value="Pdf">
                            <Setter TargetName="Icon" Property="Glyph" Value="&#xE8A5;"/>
                            <Setter TargetName="Icon" Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Kind}" Value="Video">
                            <Setter TargetName="Icon" Property="Glyph" Value="&#xE714;"/>
                            <Setter TargetName="Icon" Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
