<Window x:Class="AuswertungPro.Next.UI.Views.ProtocolHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Protokoll-Historie"
        Width="980"
        Height="620"
        MinWidth="800"
        MinHeight="520"
        ResizeMode="CanResizeWithGrip"
        WindowState="Maximized"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="HistoryGrid"
                  Grid.Column="0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding CreatedAt}" Width="160"/>
                <DataGridTextColumn Header="Kommentar" Binding="{Binding Comment}" Width="*"/>
                <DataGridTextColumn Header="Entries" Binding="{Binding EntryCount}" Width="80"/>
                <DataGridTextColumn Header="User" Binding="{Binding CreatedBy}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBox x:Name="PreviewBox"
                 Grid.Column="1"
                 Margin="10,0,0,0"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 AcceptsReturn="True"/>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="RestoreButton" Content="Auswahl wiederherstellen" Margin="0,0,8,0" Width="180"/>
            <Button x:Name="CloseButton" Content="Schliessen" Width="110" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
