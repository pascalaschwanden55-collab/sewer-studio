<Window x:Class="AuswertungPro.Next.UI.Views.Windows.DossierPrintDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SewerStudio — Haltungsdossier drucken"
        Width="480" Height="620" MinWidth="480" MinHeight="620"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgBrush}">

    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Bd" Width="18" Height="18"
                                    Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="3" VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBlock x:Name="CheckMark" Text="&#x2713;"
                                           FontSize="13" FontWeight="Bold"
                                           Foreground="{DynamicResource AccentBrush}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSubtleBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Dossier-Inhalte auswählen"
                       FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"
                       Margin="0,0,0,4"/>
            <TextBlock Text="Die Reihenfolge entspricht der Ausgabe im PDF."
                       FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
            <StackPanel Margin="8,0,0,0">
                <TextBlock Text="1) Haltung"
                           FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,2"/>
                <CheckBox x:Name="ChkDeckblatt" Content="Deckblatt (Projekt- und Haltungsübersicht)" IsChecked="True"/>
                <CheckBox x:Name="ChkHaltungsprotokoll" Content="Haltungsprotokoll (Beobachtungen / Codes)" IsChecked="True"/>
                <CheckBox x:Name="ChkFotos" Content="Fotos aus Haltungsprotokoll" IsChecked="True"/>

                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,8,0,8"/>

                <TextBlock Text="2) Schächte"
                           FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,2"/>
                <CheckBox x:Name="ChkSchachtVon" Content="Schacht Von (Stammdaten + Befunde)" IsChecked="True"/>
                <CheckBox x:Name="ChkSchachtBis" Content="Schacht Bis (Stammdaten + Befunde)" IsChecked="True"/>

                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,8,0,8"/>

                <TextBlock Text="3) Auswertung"
                           FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,2"/>
                <CheckBox x:Name="ChkHydraulik" Content="Hydraulik-Berechnung" IsChecked="True"/>
                <CheckBox x:Name="ChkKostenschaetzung" Content="Kostenschätzung / Maßnahmen" IsChecked="True"/>

                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,8,0,8"/>

                <TextBlock Text="4) Anhänge"
                           FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,2"/>
                <CheckBox x:Name="ChkOriginalProtokolle" Content="Original-Protokolle als PDF-Anhang" IsChecked="True"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Info-Labels -->
        <StackPanel Grid.Row="2" Margin="8,12,0,0">
            <TextBlock Text="Verfügbarkeit in den aktuellen Daten:"
                       FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
            <TextBlock x:Name="InfoSchachtVon" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2"/>
            <TextBlock x:Name="InfoSchachtBis" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2"/>
            <TextBlock x:Name="InfoHydraulik" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2"/>
            <TextBlock x:Name="InfoKosten" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2"/>
            <TextBlock x:Name="InfoOriginalPdf" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Abbrechen" Width="90" Height="30" Margin="0,0,8,0"
                    Click="Cancel_Click"
                    Style="{StaticResource SecondaryButton}"/>
            <Button Content="Dossier erstellen" Width="130" Height="30"
                    Click="Ok_Click"
                    Style="{StaticResource SuccessButton}"/>
        </StackPanel>
    </Grid>
</Window>
