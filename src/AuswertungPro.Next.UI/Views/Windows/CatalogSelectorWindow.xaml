<Window x:Class="AuswertungPro.Next.UI.Views.Windows.CatalogSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinCan Katalog-Auswahl"
        Width="960"
        Height="620"
        MinWidth="700"
        MinHeight="450"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Directory selection -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Verzeichnis:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
            <TextBox Grid.Column="1" Text="{Binding CatalogDirectory, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Padding="4,3"/>
            <Button Grid.Column="2" Content="Durchsuchen..." Command="{Binding BrowseDirectoryCommand}" Margin="6,0,0,0" Padding="12,4"/>
            <Button Grid.Column="3" Content="Scannen" Command="{Binding ScanCommand}" Margin="6,0,0,0" Padding="12,4"/>
        </Grid>

        <!-- Row 1: Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Land:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox ItemsSource="{Binding CountryOptions}" SelectedItem="{Binding FilterCountry}" Width="80" Margin="0,0,16,0"/>
            <TextBlock Text="Standard:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox ItemsSource="{Binding StandardOptions}" SelectedItem="{Binding FilterStandard}" Width="120" Margin="0,0,16,0"/>
            <TextBlock Text="Typ:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox ItemsSource="{Binding ObjectTypeOptions}" SelectedItem="{Binding FilterObjectType}" Width="80"/>
        </StackPanel>

        <!-- Row 2: Catalog list -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredCatalogs}"
                  SelectedItem="{Binding SelectedCatalog}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F8F8F8"
                  RowHeight="26"
                  BorderThickness="1"
                  BorderBrush="#CCCCCC">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Standard" Binding="{Binding Standard}" Width="100"/>
                <DataGridTextColumn Header="Typ" Binding="{Binding CustomType}" Width="120"/>
                <DataGridTextColumn Header="Land" Binding="{Binding Country}" Width="60"/>
                <DataGridTextColumn Header="Sprache" Binding="{Binding Language}" Width="70"/>
                <DataGridTextColumn Header="Objekt" Binding="{Binding ObjectType}" Width="60"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="90"/>
                <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Datei" Binding="{Binding FileName}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Row 3: Status and current catalog info -->
        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusText}" Foreground="#666666" FontSize="12"/>
            <TextBlock Text="{Binding CurrentCatalogInfo}" Foreground="#0066CC" FontSize="12" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Row 4: Action buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Uebernehmen" Command="{Binding ApplyCommand}" IsDefault="True" Padding="20,6" MinWidth="120" FontWeight="SemiBold"/>
            <Button Content="Abbrechen" Command="{Binding CancelCommand}" IsCancel="True" Padding="20,6" MinWidth="100" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
