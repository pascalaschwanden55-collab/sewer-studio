<Window x:Class="AuswertungPro.Next.UI.Views.Windows.DossierPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:AuswertungPro.Next.UI"
        ui:WindowFx.Entrance="True"
        Title="Dossier-Vorschau"
        Width="1400" Height="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgBrush}">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,10" TextWrapping="Wrap"
                   Text="Links die Seiten, in der Mitte das Blatt, rechts die Felder dieser Seite. Was rechts eingegeben wird, erscheint sofort im Blatt — die bearbeitete Stelle blinkt rot auf."/>

        <DockPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
            <TextBlock DockPanel.Dock="Top" Text="Seiten" Margin="0,0,0,6"/>
            <ListBox x:Name="PageList" DisplayMemberPath="Title"
                     SelectionChanged="OnPageSelected"/>
        </DockPanel>

        <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource SurfaceBrush}"
                CornerRadius="4" Padding="0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                            HorizontalAlignment="Center" Margin="0,6,0,8">
                    <TextBlock Text="Zoom" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Slider x:Name="ZoomSlider" Width="180" Minimum="0.4" Maximum="1.6"
                            Value="0.85" TickFrequency="0.05" IsSnapToTickEnabled="True"
                            VerticalAlignment="Center"
                            ValueChanged="OnZoomChanged"/>
                    <TextBlock x:Name="ZoomText" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <ScrollViewer x:Name="SheetScroll" VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto" Padding="20">
                    <!-- Das Blatt bringt seine eigenen Masse mit: A4 in
                         Bildpunkten, so wie Word bei 100 % zeigt. -->
                    <Border x:Name="Sheet" Background="White" Margin="10"
                            HorizontalAlignment="Center" VerticalAlignment="Top">
                        <Border.LayoutTransform>
                            <ScaleTransform x:Name="SheetZoom" ScaleX="0.85" ScaleY="0.85"/>
                        </Border.LayoutTransform>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.35"/>
                        </Border.Effect>
                    </Border>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <DockPanel Grid.Row="1" Grid.Column="2" Margin="12,0,0,0">
            <TextBlock DockPanel.Dock="Top" x:Name="FieldsHeader" Text="Felder" Margin="0,0,0,6"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FieldPanel" Margin="0,0,10,0"/>
            </ScrollViewer>
        </DockPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,14,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,14,0"/>
            <Button Content="Verwerfen"
                        ToolTip="Schliesst die Vorschau und wirft alle Eingaben dieser Sitzung weg. Das Dossier bleibt so, wie es beim Öffnen war." Width="120" Height="32" Margin="0,0,8,0"
                    IsCancel="True" Click="OnCancel"/>
            <Button Content="Übernehmen"
                        ToolTip="Speichert alle Eingaben der Vorschau in das Dossier und die Gebietsangaben." Width="120" Height="32"
                    IsDefault="True" Click="OnAccept"/>
        </StackPanel>
    </Grid>
</Window>
