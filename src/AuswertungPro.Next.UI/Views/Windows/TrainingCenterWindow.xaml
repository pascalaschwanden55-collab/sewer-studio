<Window x:Class="AuswertungPro.Next.UI.Views.Windows.TrainingCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AuswertungPro.Next.UI.Views.Windows"
        Title="Training Center" Height="900" Width="1400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:NotNullToBoolConverter x:Key="NotNullToBool"/>
        <local:FileToImageConverter x:Key="FileToImage"/>
    </Window.Resources>
    <Grid Margin="12" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Padding="10" CornerRadius="6"
                Background="{DynamicResource CardBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Ordner:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" Text="{Binding RootFolder}" IsReadOnly="True"
                         ToolTip="{Binding RootFolder}" Margin="0,0,8,0"/>
                <Button Grid.Column="2" Content="Ordner wählen..."
                        Command="{Binding BrowseRootFolderCommand}"
                        ToolTip="Mehrere Ordner auswählen (werden kumuliert)" Margin="0,0,4,0"/>
                <Button Grid.Column="3" Content="X"
                        Command="{Binding ClearRootFoldersCommand}"
                        ToolTip="Ordnerauswahl zurücksetzen"
                        Padding="6,2" Margin="0,0,8,0"/>
                <Button Grid.Column="4" Content="Scannen" Command="{Binding ScanCommand}" Margin="0,0,4,0"
                        ToolTip="Ausgewaehlte Ordner nach Videos und Protokollen durchsuchen und Faelle auflisten"/>
                <Button Grid.Column="5" Content="Speichern" Command="{Binding SaveCommand}" Margin="0,0,8,0"
                        ToolTip="Aktuellen Trainingsstand und alle Falldaten persistent auf Festplatte speichern"/>
                <Button Grid.Column="6" Content="&#x25B6; Batch-Import + KB" Command="{Binding BatchImportAndIndexCommand}"
                        ToolTip="Alle Ordner scannen, Samples generieren, auto-approve, Knowledge Base indizieren"
                        FontWeight="SemiBold" Margin="0,0,8,0"/>
                <Button Grid.Column="7" Content="KB prüfen" Command="{Binding CheckKnowledgeBaseCommand}"
                        ToolTip="Liest Samples, Embeddings, Versionen und Top-Codes aus der Knowledge Base"
                        Margin="0,0,8,0"/>
                <Button Grid.Column="8" Content="KB nachindexieren" Command="{Binding ReindexPendingSamplesCommand}"
                        ToolTip="Indexiert alle Approved-Samples die noch nicht in der KB sind (z.B. wenn Ollama temporaer offline war)"
                        Margin="0,0,8,0"/>
                <Button Grid.Column="9" x:Name="BtnExtractProfiles" Content="Profile extrahieren"
                        Click="ExtractProfiles_Click"
                        ToolTip="WinCan DB3 einlesen: Inspektions-Profile, Muster und Trainings-Frames extrahieren"
                        Margin="0,0,8,0"/>
                <Button Grid.Column="10" x:Name="BtnGenerateEvalSet" Content="Eval-Set"
                        Click="GenerateEvalSet_Click"
                        ToolTip="120 Pruefungs-Frames aus Operateur-Codierungen generieren (fuer KI-Qualitaetsmessung)"
                        Margin="0,0,8,0"/>
                <Button Grid.Column="11" Content="Abbrechen" Command="{Binding CancelBatchCommand}"
                        ToolTip="Laufenden Batch-Vorgang sofort abbrechen und zum letzten Speicherpunkt zurueckkehren"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Border>

        <!-- Row 1: Trainingsstand -->
        <Border Grid.Row="1" Background="{StaticResource HeaderBrush}" CornerRadius="6" Padding="12,8" Margin="0,0,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Ampel-Kreis + Label -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,16,0">
                    <Border Width="14" Height="14" CornerRadius="7"
                            VerticalAlignment="Center" Margin="0,0,8,0"
                            Background="{Binding KbReadinessBrush}"/>
                    <TextBlock Text="{Binding KbReadinessLabel}" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Zaehler -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Foreground="{StaticResource MutedBrush}" Text="Samples: "/>
                    <TextBlock Foreground="{StaticResource TextBrush}" Text="{Binding KbSampleCount}" FontWeight="Bold"/>
                    <TextBlock Foreground="#FBBF24" FontSize="{StaticResource TypeCaption}"
                               Text="{Binding KbErrorCount, StringFormat=' ({0}⚠'}"/>
                    <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeCaption}"
                               Text="{Binding KbNewCount, StringFormat=' {0} neu)'}"/>
                    <TextBlock Foreground="{StaticResource MutedBrush}" Text="  |  Codes: "/>
                    <TextBlock Foreground="{StaticResource TextBrush}" Text="{Binding KbCodesCovered}" FontWeight="Bold"/>
                </StackPanel>

                <!-- Top-Codes Tooltip -->
                <TextBlock Grid.Column="2" Text="Code-Verteilung" Foreground="{StaticResource MutedBrush}" Cursor="Help">
                    <TextBlock.ToolTip>
                        <TextBlock Text="{Binding KbTopCodesText}" FontFamily="Consolas"/>
                    </TextBlock.ToolTip>
                </TextBlock>

                <!-- Letztes Update -->
                <TextBlock Grid.Column="4" Foreground="{StaticResource MutedBrush}">
                    <Run Text="Letzte KB-Aktualisierung: "/>
                    <Run Text="{Binding KbLastUpdate, Mode=OneWay}" Foreground="{StaticResource MutedBrush}"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Tab Content -->
        <TabControl Grid.Row="2">

            <!-- Tab 1: Fälle -->
            <TabItem Header="Fälle">
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0"
                              ItemsSource="{Binding Cases}"
                              SelectedItem="{Binding SelectedCase, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="CaseId" Binding="{Binding CaseId}" Width="1.2*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="0.6*"/>
                            <DataGridTextColumn Header="Video" Binding="{Binding VideoPath}" Width="2*"/>
                            <DataGridTextColumn Header="Protokoll" Binding="{Binding ProtocolPath}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Column="1" Margin="12,0,0,0" Padding="10" CornerRadius="6"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Ausgewählter Fall" FontWeight="SemiBold" FontSize="{StaticResource TypeBodyLarge}"/>
                            <TextBlock Grid.Row="1" Text="{Binding SelectedCase.CaseId}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                            <TextBlock Grid.Row="2" Text="{Binding SelectedCase.FolderPath}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            <TextBlock Grid.Row="3" Text="{Binding SelectedCase.VideoPath}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            <TextBlock Grid.Row="4" Text="{Binding SelectedCase.ProtocolPath}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            <StackPanel Grid.Row="5" Orientation="Vertical" Margin="0,12,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,6">
                                    <Button Content="Approve" Command="{Binding ApproveCommand}" Margin="0,0,8,0"
                                            ToolTip="Fall als korrekt markieren — wird fuer KI-Training und Knowledge Base freigegeben"/>
                                    <Button Content="Reject" Command="{Binding RejectCommand}" Margin="0,0,8,0"
                                            ToolTip="Fall ablehnen — wird nicht fuer Training verwendet und aus der Warteschlange entfernt"/>
                                    <Button Content="Set New" Command="{Binding SetNewCommand}"
                                            ToolTip="Fall auf Status Neu zuruecksetzen fuer erneute Pruefung und Bearbeitung"/>
                                </StackPanel>
                                <Button Content="Samples generieren..."
                                        Command="{Binding GenerateSamplesCommand}"
                                        HorizontalAlignment="Stretch"
                                        Padding="8,6"
                                        ToolTip="Aus dem ausgewaehlten Fall automatisch Trainings-Samples mit Frames und Codes erstellen"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Tab 2: Samples -->
            <TabItem Header="Samples">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Filter-Leiste fuer Bulk-Review -->
                    <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" CornerRadius="6"
                            Padding="10,6" Margin="0,0,0,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <ComboBox Width="110" SelectedValue="{Binding SampleStatusFilter, Mode=TwoWay}"
                                      SelectedValuePath="Content" Margin="0,0,16,0"
                                      ToolTip="Filtert nach Sample-Status">
                                <ComboBoxItem Content="Pending" IsSelected="True"/>
                                <ComboBoxItem Content="Approved"/>
                                <ComboBoxItem Content="Rejected"/>
                                <ComboBoxItem Content="Alle"/>
                            </ComboBox>

                            <TextBlock Text="Code-Praefix:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBox Width="120" Text="{Binding SampleCodeFilter, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="z.B. 'BA' filtert alle BA-Codes, 'AEF' nur AEF" Margin="0,0,16,0"/>

                            <TextBlock VerticalAlignment="Center" Margin="0,0,16,0">
                                <Run Text="Sichtbar: " Foreground="{DynamicResource MutedBrush}"/>
                                <Run Text="{Binding SampleVisibleCount, Mode=OneWay}" FontWeight="Bold"/>
                            </TextBlock>

                            <Button Content="Alle Sichtbaren approven"
                                    Command="{Binding ApproveAllVisibleCommand}"
                                    Background="#16A34A" Foreground="White"
                                    Padding="14,4" FontWeight="SemiBold" Margin="0,0,8,0"
                                    ToolTip="Setzt alle aktuell durch den Filter sichtbaren Pending-Samples auf Approved (mit Konfirmation)"/>
                            <Button Content="Alle Sichtbaren rejecten"
                                    Command="{Binding RejectAllVisibleCommand}"
                                    Background="#DC2626" Foreground="White"
                                    Padding="14,4" FontWeight="SemiBold" Margin="0,0,16,0"
                                    ToolTip="Setzt alle aktuell durch den Filter sichtbaren Pending-Samples auf Rejected (mit Konfirmation)"/>

                            <Border Width="1" Background="{DynamicResource BorderBrush}" Margin="0,2,16,2"/>

                            <Button Content="Markierte approven"
                                    Command="{Binding ApproveSelectedCommand}"
                                    CommandParameter="{Binding SelectedItems, ElementName=SamplesGrid}"
                                    Background="#16A34A" Foreground="White"
                                    Padding="14,4" FontWeight="SemiBold" Margin="0,0,8,0"
                                    ToolTip="Setzt nur die per Strg+Klick / Shift+Klick markierten Zeilen auf Approved"/>
                            <Button Content="Markierte loeschen"
                                    Command="{Binding DeleteSelectedCommand}"
                                    CommandParameter="{Binding SelectedItems, ElementName=SamplesGrid}"
                                    Background="#7C2D12" Foreground="White"
                                    Padding="14,4" FontWeight="SemiBold"
                                    ToolTip="Loescht die markierten Zeilen ENDGUELTIG aus dem Training-Store (Konfirmation)"/>
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0"
                                  x:Name="SamplesGrid"
                                  ItemsSource="{Binding SamplesView}"
                                  SelectedItem="{Binding SelectedSample, Mode=TwoWay}"
                                  SelectionMode="Extended"
                                  SelectionUnit="FullRow"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="0.6*"/>
                                <DataGridTextColumn Header="m Start" Binding="{Binding MeterStart, StringFormat=F2}" Width="0.5*"/>
                                <DataGridTextColumn Header="m End" Binding="{Binding MeterEnd, StringFormat=F2}" Width="0.5*"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="0.6*"/>
                                <DataGridCheckBoxColumn Header="OSD ?" Binding="{Binding HasOsdMismatch}" Width="0.4*"/>
                                <DataGridTextColumn Header="Exportiert" Binding="{Binding ExportedUtc, StringFormat=g}" Width="0.8*"/>
                                <DataGridTextColumn Header="Fall" Binding="{Binding CaseId}" Width="1*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Sample-Vorschau -->
                        <Border Grid.Column="1" Margin="12,0,0,0" Padding="10" CornerRadius="6"
                                Background="{DynamicResource CardBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Sample-Details" FontWeight="SemiBold" FontSize="{StaticResource TypeBodyLarge}"/>
                                <StackPanel Grid.Row="1" Margin="0,8,0,0">
                                    <TextBlock>
                                        <Run Text="Code: "/>
                                        <Run Text="{Binding SelectedSampleCodeLabel, Mode=OneWay}" FontWeight="SemiBold"/>
                                    </TextBlock>
                                    <TextBlock Margin="0,4,0,0">
                                        <Run Text="Beschreibung: "/>
                                        <Run Text="{Binding SelectedSample.Beschreibung}"/>
                                    </TextBlock>
                                    <TextBlock Margin="0,4,0,0">
                                        <Run Text="Meter: "/>
                                        <Run Text="{Binding SelectedSample.MeterStart, StringFormat=F2}"/>
                                        <Run Text=" – "/>
                                        <Run Text="{Binding SelectedSample.MeterEnd, StringFormat=F2}"/>
                                        <Run Text=" m"/>
                                    </TextBlock>
                                    <TextBlock Margin="0,4,0,0" TextWrapping="Wrap">
                                        <Run Text="Frame: "/>
                                        <Run Text="{Binding SelectedSample.FramePath}"/>
                                    </TextBlock>
                                </StackPanel>
                                <Image Grid.Row="2" MaxHeight="200" Margin="0,8,0,0"
                                       Stretch="Uniform" StretchDirection="DownOnly"
                                       Source="{Binding SelectedSample.FramePath, Converter={StaticResource FileToImage}}"/>
                                <StackPanel Grid.Row="5" Orientation="Horizontal"
                                            HorizontalAlignment="Right" Margin="0,12,0,0">
                                    <Button Content="Approve" Command="{Binding ApproveSampleCommand}" Margin="0,0,8,0"
                                            ToolTip="Dieses Sample als korrekt bestaetigen und fuer YOLO-Export sowie KB freigeben"/>
                                    <Button Content="Reject" Command="{Binding RejectSampleCommand}"
                                            ToolTip="Dieses Sample als fehlerhaft markieren — wird vom Training ausgeschlossen"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Neu laden" Command="{Binding LoadSamplesCommand}" Margin="0,0,8,0"
                                ToolTip="Sample-Liste vom Speicher neu einlesen und aktualisierten Status anzeigen"/>
                        <Button Content="YOLO Export..."
                                Command="{Binding ExportYoloCommand}"
                                ToolTip="Exportiert Approved-Samples als YOLO-Trainingsdaten (images + labels + data.yaml). Nutzt Sidecar wenn verfügbar, sonst lokaler Export."
                                FontWeight="SemiBold" Margin="0,0,8,0"/>
                        <Button Content="Protokoll-Training"
                                Command="{Binding ExportApprovedCommand}"
                                ToolTip="Speichert Approved-Samples als Few-Shot-Beispiele für die Qwen-Protokollgenerierung (protocol_training.json)"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Tab 3: Selbsttraining – Echtzeit-Visualisierungen -->
            <TabItem Header="Selbsttraining" x:Name="SelfTrainingTab">
                <Grid Margin="0,4,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>  <!-- Steuerung + Werkzeuge -->
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="Auto"/>  <!-- Pipeline-Leiste -->
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="Auto"/>  <!-- Service-Indikatoren -->
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="*"/>     <!-- Hauptbereich: 3 Spalten -->
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="Auto"/>  <!-- Log (klappbar) -->
                    </Grid.RowDefinitions>

                    <!-- ═══ Row 0: Steuerung (oben) + Werkzeuge (unten) ═══ -->
                    <StackPanel Grid.Row="0">
                        <!-- Zeile 1: Start/Pause/Stop -->
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,4">
                            <Button Content="▶ Selbsttraining starten"
                                    Command="{Binding RunSelfTrainingCommand}"
                                    Background="#059669" Foreground="White" FontWeight="SemiBold"
                                    Padding="16,6" Margin="0,0,8,0" Cursor="Hand"
                                    ToolTip="Selbsttraining starten — KI analysiert alle Haltungen und vergleicht mit Protokollen"/>
                            <CheckBox Content="Alle erneut durchlaufen"
                                     IsChecked="{Binding ForceRerunAll}"
                                     Foreground="White" FontSize="{StaticResource TypeCaption}"
                                     VerticalAlignment="Center" Margin="0,0,12,0"
                                     ToolTip="Bereits verarbeitete Haltungen nochmals analysieren (KI lernt mit mehr Wissen)"/>
                            <Button Content="⏸ Pause / Fortsetzen"
                                    Command="{Binding PauseSelfTrainingCommand}"
                                    IsEnabled="{Binding IsSelfTrainingRunning}"
                                    Background="#D97706" Foreground="White" FontWeight="SemiBold"
                                    Padding="16,6" Margin="0,0,8,0" Cursor="Hand"
                                    ToolTip="Laufendes Selbsttraining pausieren oder nach Pause fortsetzen ohne Datenverlust"/>
                            <Button Content="⏹ Abbrechen"
                                    Command="{Binding StopSelfTrainingCommand}"
                                    IsEnabled="{Binding IsSelfTrainingRunning}"
                                    Background="#DC2626" Foreground="White" FontWeight="SemiBold"
                                    Padding="16,6" Cursor="Hand"
                                    ToolTip="Selbsttraining sofort abbrechen — bisherige Ergebnisse bleiben erhalten"/>
                        </WrapPanel>

                        <!-- Zeile 2: Werkzeuge -->
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,2">
                            <Button Content="🖼 Bild-Bibliothek"
                                    Click="OpenImageAnnotation_Click"
                                    Background="#059669" Foreground="White" FontWeight="SemiBold"
                                    Padding="12,5" Margin="0,0,6,0" Cursor="Hand"
                                    ToolTip="Bild-Bibliothek: Kanalbilder manuell annotieren — Rechteck zeichnen, Code zuweisen, KI lernt"/>
                            <Button Content="🎬 Video-Blindtest"
                                    Click="OpenVideoTrainingReview_Click"
                                    Background="#2563EB" Foreground="White" FontWeight="SemiBold"
                                    Padding="12,5" Margin="0,0,6,0" Cursor="Hand"
                                    ToolTip="Video-Blindtest: KI analysiert ein Video blind und vergleicht Ergebnisse mit dem Protokoll (F1-Score)"/>
                            <Button Content="📊 Benchmark"
                                    Click="OpenBenchmark_Click"
                                    Background="#7C3AED" Foreground="White" FontWeight="SemiBold"
                                    Padding="12,5" Margin="0,0,6,0" Cursor="Hand"
                                    ToolTip="Benchmark-Suite: Mehrere Haltungen automatisch testen und Metriken vergleichen"/>
                            <Button x:Name="BtnEvalRun" Content="📏 Eval-Set laufen"
                                    Click="RunEvalSet_Click"
                                    Background="#0369A1" Foreground="White" FontWeight="SemiBold"
                                    Padding="12,5" Margin="0,0,6,0" Cursor="Hand"
                                    ToolTip="Frozen Eval-Set durch Qwen laufen lassen und F1 pro VSA-Code messen (Regressions-Check nach Commits)"/>
                            <Button x:Name="BtnBatchNight" Content="🌙 Batch-Nachtbetrieb"
                                    Click="StartBatchNightRun_Click"
                                    Background="#0D9488" Foreground="White" FontWeight="SemiBold"
                                    Padding="12,5" Cursor="Hand"
                                    ToolTip="Batch-Nachtbetrieb: Alle Haltungen in einem Ordner automatisch ueber Nacht verarbeiten"/>
                            <Button x:Name="BtnBatchCancel" Content="⛔ Abbrechen"
                                    Click="CancelBatchNightRun_Click"
                                    Background="#DC2626" Foreground="White" FontWeight="SemiBold"
                                    Padding="12,5" Cursor="Hand" Visibility="Collapsed"
                                    ToolTip="Batch-Nachtbetrieb abbrechen (aktuelle Haltung wird noch fertig verarbeitet)"/>
                        </WrapPanel>
                    </StackPanel>

                    <!-- ═══ Row 2: Pipeline-Stage-Leiste ═══ -->
                    <Border Grid.Row="2" Background="{StaticResource HeaderBrush}" CornerRadius="6" Padding="12,8">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <!-- Stage 0: Timeline -->
                            <StackPanel Orientation="Horizontal" x:Name="Stage0">
                                <Ellipse Width="12" Height="12" x:Name="Dot0" Fill="{StaticResource MutedBrush}"/>
                                <TextBlock Text="OSD-Scan" Foreground="{StaticResource MutedBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            </StackPanel>
                            <TextBlock Text=" → " Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            <!-- Stage 1: Frame -->
                            <StackPanel Orientation="Horizontal" x:Name="Stage1">
                                <Ellipse Width="12" Height="12" x:Name="Dot1" Fill="{StaticResource MutedBrush}"/>
                                <TextBlock Text="Frame" Foreground="{StaticResource MutedBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            </StackPanel>
                            <TextBlock Text=" → " Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            <!-- Stage 2: KI -->
                            <StackPanel Orientation="Horizontal" x:Name="Stage2">
                                <Ellipse Width="12" Height="12" x:Name="Dot2" Fill="{StaticResource MutedBrush}"/>
                                <TextBlock Text="KI-Analyse" Foreground="{StaticResource MutedBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            </StackPanel>
                            <TextBlock Text=" → " Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            <!-- Stage 3: Vergleich -->
                            <StackPanel Orientation="Horizontal" x:Name="Stage3">
                                <Ellipse Width="12" Height="12" x:Name="Dot3" Fill="{StaticResource MutedBrush}"/>
                                <TextBlock Text="Vergleich" Foreground="{StaticResource MutedBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            </StackPanel>
                            <TextBlock Text=" → " Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            <!-- Stage 4: Technik -->
                            <StackPanel Orientation="Horizontal" x:Name="Stage4">
                                <Ellipse Width="12" Height="12" x:Name="Dot4" Fill="{StaticResource MutedBrush}"/>
                                <TextBlock Text="Technik" Foreground="{StaticResource MutedBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            </StackPanel>
                            <TextBlock Text=" → " Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            <!-- Stage 5: Fertig -->
                            <StackPanel Orientation="Horizontal" x:Name="Stage5">
                                <Ellipse Width="12" Height="12" x:Name="Dot5" Fill="{StaticResource MutedBrush}"/>
                                <TextBlock Text="Fertig" Foreground="{StaticResource MutedBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="{StaticResource TypeCaption}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- ═══ Row 4: Service-Indikatoren + aktueller Eintrag ═══ -->
                    <Border Grid.Row="4" Background="{StaticResource HeaderBrush}" CornerRadius="6" Padding="10,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 5 Service-Dots -->
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <Border Width="10" Height="10" CornerRadius="5" Background="#475569" x:Name="SvcOsd"
                                        ToolTip="OSD-Meter-Erkennung" Margin="0,0,6,0"/>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#475569" x:Name="SvcFrame"
                                        ToolTip="Frame-Extraktion" Margin="0,0,6,0"/>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#475569" x:Name="SvcQwen"
                                        ToolTip="Qwen VL KI-Analyse" Margin="0,0,6,0"/>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#475569" x:Name="SvcCompare"
                                        ToolTip="Deterministischer Vergleich" Margin="0,0,6,0"/>
                                <Border Width="10" Height="10" CornerRadius="5" Background="#475569" x:Name="SvcTech"
                                        ToolTip="Technik-Bewertung" Margin="0,0,6,0"/>
                                <TextBlock Text="Services" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                            </StackPanel>

                            <!-- Aktives Modell -->
                            <Border Grid.Column="1" CornerRadius="4" Padding="8,2" Margin="12,0,0,0"
                                    IsEnabled="{Binding IsModelActive, Mode=OneWay}"
                                    x:Name="ActiveModelBorder" Background="{StaticResource CardBrush}">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8" x:Name="ModelPulse" Fill="{StaticResource MutedBrush}" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ActiveModelName, Mode=OneWay}" x:Name="ModelNameText"
                                               Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeCaption}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Aktueller Eintrag -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeSmall}">
                                    <Run Text="Code: "/><Run Text="{Binding CurrentEntryCode, Mode=OneWay}" Foreground="{StaticResource TextBrush}" FontWeight="Bold"/>
                                    <Run Text="  |  Meter: "/><Run Text="{Binding CurrentEntryMeter, Mode=OneWay, StringFormat=F1}" Foreground="{StaticResource TextBrush}"/>
                                    <Run Text=" m"/>
                                </TextBlock>
                            </StackPanel>

                            <!-- Technik-Note -->
                            <StackPanel Grid.Column="3" Orientation="Horizontal">
                                <TextBlock Text="Technik: " Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeCaption}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding CurrentTechniqueGrade, Mode=OneWay}" Foreground="#FBBF24"
                                           FontWeight="Bold" FontSize="{StaticResource TypeBody}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- ═══ Row 6: Hauptbereich – 3 Spalten ═══ -->
                    <Grid Grid.Row="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>  <!-- Frame-Vorschau -->
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>    <!-- Vergleich + Match-Rate -->
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="280"/>  <!-- Ergebnis-Verlauf + Code-Verteilung -->
                        </Grid.ColumnDefinitions>

                        <!-- Spalte 1: Live-Frame -->
                        <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="6">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Live-Frame" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <Border Grid.Row="1" Background="{StaticResource HeaderBrush}" CornerRadius="4">
                                    <Image Stretch="Uniform" StretchDirection="DownOnly"
                                           Source="{Binding LiveFramePath, Mode=OneWay, Converter={StaticResource FileToImage}}" Margin="2"/>
                                </Border>
                                <TextBlock Grid.Row="2" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" Margin="0,4,0,0"
                                           Text="{Binding CurrentComparisonText, Mode=OneWay}"/>
                            </Grid>
                        </Border>

                        <!-- Spalte 2: Vergleich + Match-Rate-Balken -->
                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>  <!-- Match-Rate Balken -->
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="*"/>     <!-- Log / Vergleich Details -->
                            </Grid.RowDefinitions>

                            <!-- Match-Rate-Balken (gestapelt) -->
                            <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="6"/>
                                        <RowDefinition Height="24"/>
                                        <RowDefinition Height="4"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Match-Rate" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" FontWeight="SemiBold"/>
                                    <!-- Gestapelter Balken -->
                                    <Grid Grid.Row="2" x:Name="MatchRateBar">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="0" x:Name="ExactCol"/>
                                            <ColumnDefinition Width="0" x:Name="PartialCol"/>
                                            <ColumnDefinition Width="0" x:Name="MismatchCol"/>
                                            <ColumnDefinition Width="0" x:Name="NoFindingsCol"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#4ADE80" CornerRadius="3,0,0,3"
                                                ToolTip="ExactMatch"/>
                                        <Border Grid.Column="1" Background="#FBBF24"
                                                ToolTip="PartialMatch"/>
                                        <Border Grid.Column="2" Background="#F87171"
                                                ToolTip="Mismatch"/>
                                        <Border Grid.Column="3" Background="#64748B" CornerRadius="0,3,3,0"
                                                ToolTip="NoFindings"/>
                                        <Border Grid.Column="4" Background="{StaticResource HeaderBrush}" CornerRadius="0,3,3,0"/>
                                    </Grid>
                                    <!-- Legende -->
                                    <StackPanel Grid.Row="4" Orientation="Horizontal">
                                        <Border Width="8" Height="8" Background="#4ADE80" CornerRadius="2" Margin="0,0,3,0"/>
                                        <TextBlock Text="{Binding ExactPercent, Mode=OneWay, StringFormat=P0}" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" Margin="0,0,10,0"/>
                                        <Border Width="8" Height="8" Background="#FBBF24" CornerRadius="2" Margin="0,0,3,0"/>
                                        <TextBlock Text="{Binding PartialPercent, Mode=OneWay, StringFormat=P0}" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" Margin="0,0,10,0"/>
                                        <Border Width="8" Height="8" Background="#F87171" CornerRadius="2" Margin="0,0,3,0"/>
                                        <TextBlock Text="{Binding MismatchPercent, Mode=OneWay, StringFormat=P0}" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" Margin="0,0,10,0"/>
                                        <Border Width="8" Height="8" Background="#64748B" CornerRadius="2" Margin="0,0,3,0"/>
                                        <TextBlock Text="{Binding NoFindingsPercent, Mode=OneWay, StringFormat=P0}" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Log / Batch-Import Bereich -->
                            <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Live-Log" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <TextBox Grid.Row="1"
                                             Text="{Binding LogText, Mode=OneWay}"
                                             IsReadOnly="True"
                                             VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Auto"
                                             TextWrapping="NoWrap" AcceptsReturn="True"
                                             BorderThickness="0"
                                             x:Name="LogTextBox">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Background" Value="#0F172A"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="CaretBrush" Value="White"/>
                                                <Setter Property="FontFamily" Value="Consolas"/>
                                                <Setter Property="FontSize" Value="11"/>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Spalte 3: Ergebnis-Verlauf + Code-Verteilung -->
                        <Grid Grid.Column="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>    <!-- Ergebnis-Verlauf -->
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="Auto"/> <!-- Code-Verteilung -->
                            </Grid.RowDefinitions>

                            <!-- Ergebnis-Verlauf (scrollbare Liste) -->
                            <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="6">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Ergebnis-Verlauf" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <ListBox Grid.Row="1" x:Name="ResultsListBox"
                                             ItemsSource="{Binding SelfTrainingResults}"
                                             Background="Transparent" BorderThickness="0"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Border Padding="4,2" Margin="0,1" CornerRadius="3"
                                                        Background="{StaticResource HeaderBrush}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="24"/>
                                                            <ColumnDefinition Width="60"/>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Index}" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" VerticalAlignment="Center"/>
                                                        <TextBlock Grid.Column="1" Text="{Binding VsaCode}" Foreground="{StaticResource TextBrush}"
                                                                   FontWeight="SemiBold" FontSize="{StaticResource TypeCaption}" VerticalAlignment="Center"/>
                                                        <TextBlock Grid.Column="2" Text="{Binding Meter, StringFormat=F1}" Foreground="{StaticResource MutedBrush}"
                                                                   FontSize="{StaticResource TypeMicro}" VerticalAlignment="Center"/>
                                                        <!-- Farbiger Level-Indikator -->
                                                        <Border Grid.Column="3" HorizontalAlignment="Right" Padding="4,1" CornerRadius="3"
                                                                x:Name="LevelBorder">
                                                            <TextBlock Text="{Binding Level}" FontSize="9" Foreground="White" FontWeight="Bold"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                                <DataTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="ExactMatch">
                                                        <Setter TargetName="LevelBorder" Property="Background" Value="#4ADE80"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="PartialMatch">
                                                        <Setter TargetName="LevelBorder" Property="Background" Value="#FBBF24"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Mismatch">
                                                        <Setter TargetName="LevelBorder" Property="Background" Value="#F87171"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="NoFindings">
                                                        <Setter TargetName="LevelBorder" Property="Background" Value="#64748B"/>
                                                    </DataTrigger>
                                                </DataTemplate.Triggers>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                            </Border>

                            <!-- Code-Verteilung (Balken pro Code) -->
                            <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="6" Padding="6"
                                    MaxHeight="200">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Code-Verteilung" Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <ItemsControl Grid.Row="1" ItemsSource="{Binding CodeDistribution}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="55"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="30"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Code}" Foreground="{StaticResource TextBrush}" FontSize="{StaticResource TypeMicro}"
                                                               FontFamily="Consolas" VerticalAlignment="Center"/>
                                                    <!-- Gestapelter Mini-Balken -->
                                                    <Grid Grid.Column="1" Height="10" Margin="4,0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="{Binding Exact, FallbackValue=0}"/>
                                                            <ColumnDefinition Width="{Binding Partial, FallbackValue=0}"/>
                                                            <ColumnDefinition Width="{Binding Mismatch, FallbackValue=0}"/>
                                                            <ColumnDefinition Width="{Binding NoFindings, FallbackValue=0}"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Border Grid.Column="0" Background="#4ADE80" CornerRadius="2,0,0,2"/>
                                                        <Border Grid.Column="1" Background="#FBBF24"/>
                                                        <Border Grid.Column="2" Background="#F87171"/>
                                                        <Border Grid.Column="3" Background="#64748B" CornerRadius="0,2,2,0"/>
                                                    </Grid>
                                                    <TextBlock Grid.Column="2" Text="{Binding Total}" Foreground="{StaticResource MutedBrush}"
                                                               FontSize="{StaticResource TypeMicro}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>

                    <!-- ═══ Row 8: Klappbares Echtzeit-Log ═══ -->
                    <Expander Grid.Row="8" Header="Echtzeit-Log" IsExpanded="False"
                              FontSize="{StaticResource TypeSmall}">
                        <Border Background="{DynamicResource CardBrush}" CornerRadius="4"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <TextBox x:Name="SelfTrainingLogList"
                                     Text="{Binding EchtzeitLogText, Mode=OneWay}"
                                     IsReadOnly="True"
                                     MinHeight="120" MaxHeight="350"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     TextWrapping="NoWrap" AcceptsReturn="True"
                                     BorderThickness="0"
                                     Background="{DynamicResource CardBrush}"
                                     Foreground="{DynamicResource TextBrush}"
                                     CaretBrush="{DynamicResource TextBrush}"
                                     FontFamily="Consolas"
                                     FontSize="{StaticResource TypeSmall}"
                                     Padding="8,6">
                            </TextBox>
                        </Border>
                    </Expander>
                </Grid>
            </TabItem>

            <!-- Tab 4: Review Queue (Self-Improving Loop) -->
            <TabItem Header="Review Queue">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Zur Prüfung:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ReviewQueueCount}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <TextBlock Text="{Binding ReviewStatusText}" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" MinWidth="400"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="2*" MinWidth="280"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0"
                                  ItemsSource="{Binding ReviewQueue}"
                                  SelectedItem="{Binding SelectedReviewItem}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  CanUserSortColumns="True"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Priorität" Binding="{Binding PriorityLabel}" Width="70"/>
                                <DataGridTextColumn Header="Befund (Code — Klartext)" Binding="{Binding Label}" Width="2*" MinWidth="260"/>
                                <DataGridTextColumn Header="Vorgeschlagen" Binding="{Binding SuggestedCode}" Width="*" MinWidth="140"/>
                                <DataGridTextColumn Header="Konfidenz" Binding="{Binding Confidence, StringFormat=P0}" Width="75"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- V4.2 Phase 1.5: Frame-Viewer + Metadaten rechts -->
                        <Border Grid.Column="2"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                Padding="8" Background="{DynamicResource SurfaceBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Image Grid.Row="0"
                                       Source="{Binding SelectedReviewFramePath, TargetNullValue={x:Null}}"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       Margin="0,0,0,6"/>

                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding SelectedReviewItem.Label, TargetNullValue='Kein Item ausgewaehlt'}"
                                               FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource MutedBrush}">
                                        <Run Text="Vorschlag: "/>
                                        <Run Text="{Binding SelectedReviewItem.SuggestedCode, TargetNullValue='—'}" FontWeight="SemiBold"/>
                                        <Run Text="  ·  Prio: "/>
                                        <Run Text="{Binding SelectedReviewItem.PriorityLabel, TargetNullValue='—'}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Akzeptieren" x:Name="ReviewApproveBtn"
                                Click="ReviewApprove_Click"
                                Padding="12,4" Margin="0,0,8,0"
                                ToolTip="KI-Vorschlag bestaetigen und als korrektes Trainingsbeispiel uebernehmen"/>
                        <Button Content="Ablehnen" x:Name="ReviewRejectBtn"
                                Click="ReviewReject_Click"
                                Padding="12,4" Margin="0,0,8,0"
                                ToolTip="KI-Vorschlag verwerfen und aus der Review-Queue entfernen"/>
                        <Button Content="Korrigieren + Speichern" x:Name="ReviewCorrectBtn"
                                Click="ReviewCorrect_Click"
                                Padding="12,4"
                                ToolTip="Frame im Bildtraining-Fenster oeffnen und korrekten VSA-Code markieren/speichern"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Schwachstellen">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <Button Content="Aktualisieren"
                                    Command="{Binding RefreshWeakSpotsCommand}"
                                    Padding="10,4" Margin="0,0,8,0"
                                    ToolTip="Berechnet Schwachstellen aus Benchmark-Metriken, Rejected-Samples und offener Review neu"/>
                            <Button Content="Samples anzeigen"
                                    Command="{Binding ShowWeakSpotSamplesCommand}"
                                    Padding="10,4"
                                    ToolTip="Filtert den Samples-Tab auf den Code der ausgewaehlten Schwachstelle"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Priorisiert:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding WeakSpotCount}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                            <TextBlock Text="{Binding WeakSpotSummary}" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DockPanel>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" MinWidth="520"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="2*" MinWidth="320"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0"
                                  ItemsSource="{Binding WeakSpots}"
                                  SelectedItem="{Binding SelectedWeakSpot}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  CanUserSortColumns="True"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Prio" Binding="{Binding Priority, StringFormat=F1}" Width="60"/>
                                <DataGridTextColumn Header="Quelle" Binding="{Binding Source}" Width="100"/>
                                <DataGridTextColumn Header="Code" Binding="{Binding ConfusionLabel}" Width="130"/>
                                <DataGridTextColumn Header="F1" Binding="{Binding F1, StringFormat=F2, TargetNullValue=--}" Width="60"/>
                                <DataGridTextColumn Header="P" Binding="{Binding Precision, StringFormat=F2, TargetNullValue=--}" Width="55"/>
                                <DataGridTextColumn Header="R" Binding="{Binding Recall, StringFormat=F2, TargetNullValue=--}" Width="55"/>
                                <DataGridTextColumn Header="FP" Binding="{Binding FalsePositives}" Width="50"/>
                                <DataGridTextColumn Header="FN" Binding="{Binding FalseNegatives}" Width="50"/>
                                <DataGridTextColumn Header="Rejected" Binding="{Binding RejectedCount}" Width="75"/>
                                <DataGridTextColumn Header="Grund" Binding="{Binding Reason}" Width="*" MinWidth="220"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Column="2"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                Padding="10" Background="{DynamicResource SurfaceBrush}">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedWeakSpot.ConfusionLabel, TargetNullValue='Keine Schwachstelle ausgewaehlt'}"
                                           FontWeight="SemiBold" FontSize="{StaticResource TypeBodyLarge}" TextWrapping="Wrap"/>
                                <TextBlock Margin="0,8,0,0" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap">
                                    <Run Text="Quelle: "/>
                                    <Run Text="{Binding SelectedWeakSpot.Source, TargetNullValue=--}"/>
                                    <Run Text="  |  Prio: "/>
                                    <Run Text="{Binding SelectedWeakSpot.Priority, StringFormat=F1, TargetNullValue=--}"/>
                                </TextBlock>
                                <TextBlock Margin="0,10,0,0" Text="{Binding SelectedWeakSpot.Reason, TargetNullValue=--}"
                                           TextWrapping="Wrap"/>
                                <TextBlock Margin="0,12,0,0" Foreground="{DynamicResource MutedBrush}"
                                           Text="Empfohlene Aktion" FontWeight="SemiBold"/>
                                <TextBlock Margin="0,4,0,0" Text="{Binding SelectedWeakSpot.SuggestedAction, TargetNullValue=--}"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TextBlock Grid.Row="4" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap"
                               Text="Die Liste veraendert keine Trainingsdaten. Sie zeigt Codes und Verwechslungen, die fuer Review, Teacher-Annotation oder gezielte neue Beispiele priorisiert werden sollten."/>
                </Grid>
            </TabItem>

            <!-- KB Qualitaet Tab -->
            <TabItem Header="KB Qualit&#228;t">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">

                        <!-- Coverage-Analyse -->
                        <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="Coverage-Analyse" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" FontSize="{StaticResource TypeBody}"/>
                                <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeCaption}" Margin="0,4,0,0">
                                    <Run Text="Codes mit weniger als 3 KB-Samples: "/>
                                    <Run Text="{Binding KbCoverageGapsCount, Mode=OneWay}" Foreground="#FBBF24" FontWeight="Bold"/>
                                </TextBlock>
                                <TextBlock Text="{Binding KbCoverageGapsText, Mode=OneWay}"
                                           FontFamily="Consolas" FontSize="{StaticResource TypeCaption}" Foreground="{StaticResource TextBrush}"
                                           TextWrapping="Wrap" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Genauigkeit pro Code -->
                        <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="Genauigkeit pro Code (Top 8)" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" FontSize="{StaticResource TypeBody}"/>
                                <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="{StaticResource TypeMicro}" Margin="0,2,0,4"
                                           Text="F1 = Gesamtgenauigkeit, P = Precision, R = Recall"/>
                                <TextBlock Text="{Binding KbAccuracyText, Mode=OneWay}"
                                           FontFamily="Consolas" FontSize="{StaticResource TypeCaption}" Foreground="{StaticResource TextBrush}"
                                           TextWrapping="NoWrap" Margin="0,0,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Problematische KB-Samples -->
                        <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Problematische KB-Samples: " Foreground="{StaticResource TextBrush}" FontSize="{StaticResource TypeBody}"/>
                                <TextBlock Text="{Binding KbStaleSampleCount, Mode=OneWay}"
                                           Foreground="#F87171" FontWeight="Bold" FontSize="{StaticResource TypeBody}"/>
                            </StackPanel>
                        </Border>

                        <!-- Match-Rate Trend -->
                        <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="10">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Match-Rate Trend (letzte Laeufe)" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" FontSize="{StaticResource TypeBody}"/>
                                    <TextBlock Text="{Binding KbTrendDirection, Mode=OneWay}"
                                               Foreground="#4ADE80" FontSize="{StaticResource TypeH4}" FontWeight="Bold" Margin="8,0,0,0"/>
                                </StackPanel>
                                <TextBlock Text="{Binding KbTrendText, Mode=OneWay}"
                                           FontFamily="Consolas" FontSize="{StaticResource TypeCaption}" Foreground="{StaticResource TextBrush}"
                                           TextWrapping="NoWrap" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ═══════ Tab 6: Lehrer-Annotationen ═══════ -->
            <TabItem Header="Lehrer" x:Name="TeacherTab">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>

                    <!-- Filter-Leiste -->
                    <Border Grid.Row="0" Padding="8" CornerRadius="4" Margin="0,0,0,8"
                            Background="{DynamicResource HeaderBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <DockPanel>
                            <TextBlock Text="VSA-Filter:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox x:Name="TeacherFilterCombo" Width="120"
                                      SelectionChanged="TeacherFilter_Changed" Margin="0,0,16,0">
                                <ComboBoxItem Content="Alle" IsSelected="True"/>
                            </ComboBox>
                            <TextBlock x:Name="TeacherCountText" VerticalAlignment="Center"
                                       FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                                <Button x:Name="BtnTeacherRefresh" Content="🔄 Aktualisieren"
                                        Click="TeacherRefresh_Click" Margin="0,0,8,0"
                                        Padding="8,4" FontSize="{StaticResource TypeSmall}"
                                        ToolTip="Lehrer-Annotationen aus dem Speicher neu laden und Galerie aktualisieren"/>
                                <Button x:Name="BtnTeacherAddFewShot" Content="📚 → FewShot"
                                        Click="TeacherAddToFewShot_Click" Margin="0,0,8,0"
                                        Padding="8,4" FontSize="{StaticResource TypeSmall}" IsEnabled="False"
                                        ToolTip="Ausgewaehlte Annotation als Few-Shot-Beispiel fuer Qwen-Protokollgenerierung speichern"/>
                                <Button x:Name="BtnTeacherDelete" Content="🗑 Loeschen"
                                        Click="TeacherDelete_Click"
                                        Padding="8,4" FontSize="{StaticResource TypeSmall}" IsEnabled="False"
                                        Foreground="#EF4444"
                                        ToolTip="Ausgewaehlte Lehrer-Annotation dauerhaft loeschen — kann nicht rueckgaengig gemacht werden"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <!-- Thumbnail-Galerie -->
                    <Border Grid.Row="1" CornerRadius="4"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="TeacherGallery">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border x:Name="ThumbBorder" Margin="4" Padding="4" CornerRadius="4"
                                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                                Background="{DynamicResource HeaderBrush}"
                                                Cursor="Hand"
                                                MouseLeftButtonDown="TeacherThumb_Click"
                                                Width="140">
                                            <StackPanel>
                                                <Image Source="{Binding CroppedRegionPath, Converter={StaticResource FileToImage}}"
                                                       Width="120" Height="90" Stretch="Uniform"
                                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                                <TextBlock Text="{Binding VsaCode}" FontWeight="Bold"
                                                           FontSize="{StaticResource TypeBody}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                                <TextBlock Text="{Binding Beschreibung}" FontSize="{StaticResource TypeMicro}"
                                                           Foreground="{StaticResource MutedBrush}"
                                                           HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"
                                                           MaxWidth="130" ToolTip="{Binding Beschreibung}"/>
                                                <TextBlock FontSize="{StaticResource TypeMicro}" Foreground="{StaticResource MutedBrush}"
                                                           HorizontalAlignment="Center">
                                                    <Run Text="{Binding MeterPosition, StringFormat={}{0:F1}m}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <!-- Splitter -->
                    <GridSplitter Grid.Row="2" Height="4" HorizontalAlignment="Stretch"
                                  Background="Transparent" Margin="0,2"/>

                    <!-- Detail-Ansicht -->
                    <Border Grid.Row="3" CornerRadius="4" Padding="8"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <Grid x:Name="TeacherDetailPanel" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="280"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Volles Frame -->
                            <Image x:Name="TeacherDetailImage" Grid.Column="0"
                                   Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"
                                   Margin="0,0,8,0"/>

                            <!-- Metadaten -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock x:Name="TeacherDetailCode" FontSize="{StaticResource TypeH3}" FontWeight="Bold"/>
                                <TextBlock x:Name="TeacherDetailBeschreibung" FontSize="{StaticResource TypeSmall}"
                                           Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                <TextBlock x:Name="TeacherDetailMeter" FontSize="{StaticResource TypeBody}"/>
                                <TextBlock x:Name="TeacherDetailClock" FontSize="{StaticResource TypeBody}"/>
                                <TextBlock x:Name="TeacherDetailTool" FontSize="{StaticResource TypeBody}"/>
                                <TextBlock x:Name="TeacherDetailDate" FontSize="{StaticResource TypeSmall}"
                                           Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
                                <TextBlock x:Name="TeacherDetailId" FontSize="{StaticResource TypeCaption}"
                                           Foreground="{StaticResource MutedBrush}" FontFamily="Consolas" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Progress Bar (sichtbar wenn Busy) -->
        <Grid Grid.Row="3" Margin="0,8,0,0"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <ProgressBar Height="20" Minimum="0"
                         Maximum="{Binding ProgressMax}"
                         Value="{Binding ProgressValue}"/>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontSize="{StaticResource TypeCaption}">
                <Run Text="{Binding ProgressValue, Mode=OneWay}"/>
                <Run Text="/"/>
                <Run Text="{Binding ProgressMax, Mode=OneWay}"/>
            </TextBlock>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="4" Padding="8" CornerRadius="6" Margin="0,4,0,0"
                Background="{DynamicResource HeaderBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1">
            <DockPanel>
                <TextBlock Text="{Binding StatusText}" DockPanel.Dock="Left" TextWrapping="Wrap"/>
                <TextBlock Text="Busy..." DockPanel.Dock="Right"
                           Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
