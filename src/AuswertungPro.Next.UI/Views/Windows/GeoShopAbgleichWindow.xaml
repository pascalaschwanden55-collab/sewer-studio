<Window x:Class="AuswertungPro.Next.UI.Views.Windows.GeoShopAbgleichWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:AuswertungPro.Next.UI"
        ui:WindowFx.Entrance="True"
        Title="SewerStudio — GeoShop-Abgleich" Width="980" Height="720" MinWidth="600" MinHeight="420"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BgLightBrush}">
    <DockPanel Margin="20">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,14">
            <TextBlock Text="GeoShop-Abgleich" FontSize="{DynamicResource TextTitle}" FontWeight="SemiBold"/>
            <TextBlock x:Name="Status" Text="Die XTF wird gelesen. Deine Projektdaten werden noch nicht geändert."
                       TextWrapping="Wrap" Margin="0,8,0,0"/>
            <ProgressBar x:Name="Fortschritt" Height="4" IsIndeterminate="True" Margin="0,12,0,0"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="Uebernehmen" Content="Gezeigte Änderungen übernehmen" IsEnabled="False"
                    Style="{DynamicResource PrimaryButton}"
                    Click="Uebernehmen_Click" Padding="16,8" Margin="0,0,10,0"/>
            <Button Content="Abbrechen" IsCancel="True" Padding="16,8"/>
        </StackPanel>
        <TextBox x:Name="Bericht" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" Padding="12" FontSize="{DynamicResource TextM}"
                 AutomationProperties.Name="Vorschau der GeoShop-Änderungen"/>
    </DockPanel>
</Window>
