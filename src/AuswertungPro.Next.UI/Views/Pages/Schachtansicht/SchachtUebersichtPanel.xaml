<UserControl x:Class="AuswertungPro.Next.UI.Views.Pages.Schachtansicht.SchachtUebersichtPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AuswertungPro.Next.UI.Views.Pages.Schachtansicht"
             xmlns:haltung="clr-namespace:AuswertungPro.Next.UI.Views.Pages.Haltungsansicht"
             xmlns:dp="clr-namespace:AuswertungPro.Next.UI.DataPage" x:Name="Root">
    <UserControl.Resources>
        <local:SchachtZustandsklasseBrushConverter x:Key="ZkBrushConv"/>
        <dp:ZustandsklasseInkConverter x:Key="ZkInkConv"/>
        <!-- Task 6: Leere Eckdaten zeigen denselben Gedankenstrich wie im Haltungspanel,
             statt eine leere Zeile unter der Beschriftung. -->
        <haltung:FaktWertConverter x:Key="FaktWertConv"/>
        <haltung:SchadenMeterConverter x:Key="SchadenMeterConv"/>
        <haltung:SchadenKlartextConverter x:Key="SchadenKlartextConv"/>
        <haltung:SchadenStufeQuelleConverter x:Key="SchadenStufeQuelleConv"/>
        <Style x:Key="FaktLabel" TargetType="TextBlock"><Setter Property="FontSize" Value="{DynamicResource TextXS}"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/></Style>
        <Style x:Key="FaktWert" TargetType="TextBlock"><Setter Property="FontSize" Value="{DynamicResource TextS}"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="FontFamily" Value="{DynamicResource FontMono}"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style>
    </UserControl.Resources>
    <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusL}" Padding="12">
        <Grid>
            <!-- Task 6: Ohne gewaehlten Schacht bleibt der ganze Inhalt weg; sonst stehen Grafik,
                 leere Beschriftungen und Knoepfe ohne Bezug da.
                 Task 5: Alles in einem Bildlauf (Muster HaltungUebersichtPanel) — die mindestens
                 320 px hohe Schachtgrafik braucht mehr Platz als der fruehere 120-px-Grundriss;
                 ohne ScrollViewer wuerden Eckdaten und Schadenliste sonst abgeschnitten. Nur die
                 Schadenliste bekommt die Reststrecke (*). -->
            <ScrollViewer x:Name="Inhalt" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          DataContext="{Binding Record, ElementName=Root}">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Record, ElementName=Root}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,8">
                        <Border DockPanel.Dock="Right" MinWidth="34" Height="22" Padding="6,0" CornerRadius="{DynamicResource RadiusS}" Background="{Binding Fields[Zustandsklasse], Converter={StaticResource ZkBrushConv}}" ToolTip="Zustandsklasse (von Hand gesetzt)">
                            <TextBlock Text="{Binding Fields[Zustandsklasse], StringFormat=Z{0}}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="{DynamicResource TextS}" FontWeight="SemiBold" Foreground="{Binding Fields[Zustandsklasse], Converter={StaticResource ZkInkConv}}"/>
                        </Border>
                        <TextBlock Text="Schachtansicht" FontSize="{DynamicResource TextL}" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Fields[Schachtnummer]}" Margin="10,0,0,0" VerticalAlignment="Center" FontFamily="{DynamicResource FontMono}" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </DockPanel>

                    <!-- Schachtgrafik des Protokolls: senkrechter Schnitt (WinCan-Art) statt des
                         frueheren Grundriss-Kreises. Der Masstext steht in der Grafik unter der
                         Sohle; er wird hier nicht mehr separat gefuehrt. -->
                    <local:SchachtgrafikControl x:Name="Grafik" Grid.Row="1" Height="340" Margin="0,0,0,8"
                                                 HorizontalAlignment="Stretch"
                                                 Record="{Binding Record, ElementName=Root}"
                                                 Haltungen="{Binding Haltungen, ElementName=Root}"
                                                 Catalog="{Binding Catalog, ElementName=Root}"/>

                    <UniformGrid Grid.Row="2" Columns="2" Margin="0,0,0,8">
                        <StackPanel Margin="0,0,8,6"><TextBlock Text="Funktion" Style="{StaticResource FaktLabel}"/><TextBlock Text="{Binding Fields[Funktion], Converter={StaticResource FaktWertConv}}" Style="{StaticResource FaktWert}"/></StackPanel>
                        <StackPanel Margin="0,0,0,6"><TextBlock Text="Material" Style="{StaticResource FaktLabel}"/><TextBlock Text="{Binding Fields[Material], Converter={StaticResource FaktWertConv}}" Style="{StaticResource FaktWert}"/></StackPanel>
                        <StackPanel Margin="0,0,8,6"><TextBlock Text="Tiefe" Style="{StaticResource FaktLabel}"/><TextBlock Text="{Binding Fields[Schachttiefe], Converter={StaticResource FaktWertConv}}" Style="{StaticResource FaktWert}"/></StackPanel>
                        <StackPanel Margin="0,0,0,6"><TextBlock Text="Baujahr" Style="{StaticResource FaktLabel}"/><TextBlock Text="{Binding Fields[Baujahr], Converter={StaticResource FaktWertConv}}" Style="{StaticResource FaktWert}"/></StackPanel>
                        <StackPanel Margin="0,0,8,6"><TextBlock Text="Belastungsklasse" Style="{StaticResource FaktLabel}"/><TextBlock Text="{Binding Fields[Belastungsklasse], Converter={StaticResource FaktWertConv}}" Style="{StaticResource FaktWert}"/></StackPanel>
                        <StackPanel Margin="0,0,0,6"><TextBlock Text="Inspektion" Style="{StaticResource FaktLabel}"/><TextBlock Text="{Binding Fields[Inspektionsdatum], Converter={StaticResource FaktWertConv}}" Style="{StaticResource FaktWert}"/></StackPanel>
                    </UniformGrid>

                    <TextBlock Grid.Row="3" Text="Schäden" FontSize="{DynamicResource TextM}" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,4,0,6"/>
                    <ListBox Grid.Row="4" ItemsSource="{Binding Entries, ElementName=Root}" BorderThickness="0" Background="Transparent" AutomationProperties.Name="Schäden"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <Border Background="{DynamicResource AccentSubtleBrush}" CornerRadius="{DynamicResource RadiusS}" Padding="6,2" Margin="0,0,8,0" VerticalAlignment="Top"><TextBlock Text="{Binding Code}" FontFamily="{DynamicResource FontMono}" FontWeight="SemiBold" FontSize="{DynamicResource TextS}" Foreground="{DynamicResource TextBrush}"/></Border>
                                    <StackPanel Grid.Column="1"><TextBlock Text="{Binding Converter={StaticResource SchadenKlartextConv}}" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextBrush}"/><TextBlock Text="{Binding Converter={StaticResource SchadenStufeQuelleConv}}" FontSize="{DynamicResource TextXS}" Foreground="{DynamicResource MutedBrush}"/></StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding Converter={StaticResource SchadenMeterConv}}" FontFamily="{DynamicResource FontMono}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="8,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Grid.Row="5" Content="Protokoll (PDF)" Click="Pdf_Click" Style="{StaticResource ToolbarButton}" HorizontalAlignment="Left" Margin="0,8,0,0" ToolTip="Schachtprotokoll öffnen"/>
                    <TextBlock Grid.Row="6" Text="Am Schacht wird die Zustandsklasse nie berechnet. Die Fachperson setzt sie von Hand." TextWrapping="Wrap" FontSize="{DynamicResource TextXS}" Foreground="{DynamicResource MutedBrush}" Margin="0,8,0,0"/>
                </Grid>
            </ScrollViewer>

            <TextBlock x:Name="Leerzustand" Text="Kein Schacht gewählt. Links eine Zeile wählen."
                       Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"
                       VerticalAlignment="Top" HorizontalAlignment="Left">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Record, ElementName=Root}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
