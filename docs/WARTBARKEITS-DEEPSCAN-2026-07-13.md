# Wartbarkeits- und Belastbarkeits-Deepscan SewerStudio 4.5.0

## Umsetzungsstand vom 13.07.2026

Die wichtigsten Sofortmaßnahmen aus diesem Bericht sind umgesetzt und geprüft:

- **Release-Protokollierung repariert:** Im Produktionscode gibt es keine `Debug.WriteLine`-Stelle mehr. Wichtige Warnungen und Fehler aus KI, Training, Import, Backup und Dateizugriff landen über `BestEffort` im Tageslog. Reine Statusinformationen verwenden `Trace`.
- **Echter Log-Dateitest ergänzt:** Ein Integrationstest prüft, dass eine `BestEffort`-Warnung tatsächlich als Warnung in die Tageslogdatei geschrieben wird.
- **Schacht-Seite verkleinert:** Die Sanierungsmaßnahmen- und Speicherlogik wurde in `SchachtMassnahmenDialogController` ausgelagert. `SchaechtePage.xaml.cs` liegt nun mit 930 Zeilen wieder unter der Grenze von 1.000 Zeilen.
- **Protokoll-Editor entkoppelt (A1-04 erledigt):** Request-Aufbau, KI-Aufruf und Fehlerbehandlung liegen im testbaren `ProtocolEntryEditorKiViewModel`; die VSA- und Gesamtprüfung liegt im `ProtocolEntryEditorValidationViewModel`. Der Dialog zeigt nur noch Ergebnisse und Feldmarkierungen an. Laufende KI-Aufrufe werden beim Schließen abgebrochen; technische Fehlerdetails landen im Tageslog statt im Nutzerdialog. `ProtocolEntryEditorDialog.xaml.cs` sank von 943 auf 789 Zeilen.
- **Medien-Suche entkoppelt (A1-07, Teilpaket):** `BatchMediaSearchService` wird zentral im `ServiceProvider` bereitgestellt und dem Fenster zusammen mit nur den benötigten Dialog- und Einstellungsdiensten übergeben. Das Fenster erzeugt den Dienst nicht mehr bei jedem Suchlauf und hält den ganzen Container nicht mehr als Feld. Technische Suchfehler landen im Tageslog.
- **Fenster-Dienste zentralisiert (A1-07 erledigt):** Medien-Suche sowie alle Training-Center-Dienste werden zentral bereitgestellt. Review-Warteschlange, SAM und Few-Shot bleiben bedarfsgesteuert; die Review-Warteschlange wird über alle Fenster geteilt. Das Fenster erzeugt diese Datei- und Netzwerkdienste nicht mehr selbst.
- **Diagnose-ViewModel entkoppelt (A1-05, Teilpaket):** `DiagnosticsPageViewModel` erhält nur noch `ILogTailReader` statt des ganzen `ServiceProvider` und greift nicht mehr direkt auf Dateien zu. `DailyLogTailReader` liegt in Infrastructure, liefert höchstens die letzten 200 Zeilen und protokolliert technische Fehler, während die Oberfläche eine verständliche Meldung zeigt.
- **Schattenauswertung entkoppelt (A1-05, Teilpaket):** Das ViewModel erhält Projektzugriff, `ISchattenAuswertungStore`, Service-Fabrik und Projektpfad gezielt statt `ShellViewModel` und `ServiceProvider`. Laden, Berechnen, Speichern und Fehlerpfad sind ohne echte KI oder Dateien testbar; rohe technische Fehler erscheinen nicht mehr in der Oberfläche.
- **VSA-Seite entkoppelt (A1-05, Teilpaket):** `VsaPageViewModel` speichert weder `ShellViewModel` noch `ServiceProvider`. Projektzugriff, Statusaktionen sowie XTF-, PDF-, VSA- und Maßnahmendienst werden gezielt übergeben. Zwei Tests sichern die Abhängigkeiten und den vollständigen Lauf mit Wiederherstellungspunkt, relativen Quelldateien und Projektänderung.
- **Schacht-Matrix entkoppelt (A1-05, Teilpaket):** Das ViewModel erhält Projekt, Projektpfad, Dialoge und Aktualisierungsmeldung gezielt. Der gesamte Container und die Shell werden nicht mehr gespeichert; fünf Tests schützen Laden, Kostenberechnung, Speichern und die Abhängigkeitsgrenze.
- **Medienkonflikte entkoppelt (A1-05, Teilpaket):** Projektzugriff, Quellordner, Dialoge und Video-Start werden gezielt übergeben. Das ViewModel erzeugt weder den Konfliktdienst noch ein Player-Fenster selbst. Drei Tests schützen die Abhängigkeitsgrenze, den fehlenden Projektordner und den Video-Start.
- **Einstellungen entkoppelt (A1-05, Teilpaket):** Einstellungen, Diagnose, Dialoge, Sicherungsdienst, Sicherungsstatus, Meldungen und Programmbereinigung werden gezielt übergeben. Der gemeinsam genutzte Sicherungsstatus bleibt erhalten; die Programmbereinigung wird zentral erzeugt. 156 Einstellungs- und Schutztests sind grün.
- **Export entkoppelt (A1-05, Teilpaket):** Das Export-ViewModel erhält Einstellungen, Dialoge, Excel-Export, Meldungen und Kostenabgleich gezielt. Es speichert den zentralen Container nicht mehr. 14 Export- und Verteilungstests schützen die betroffenen Abläufe.
- **Karte entkoppelt (A1-05, Teilpaket):** Das Karten-ViewModel erhält Einstellungen, Kartendaten und den Video-Start gezielt. Es speichert den zentralen Container nicht mehr; der Video-Player wird außerhalb des ViewModels erzeugt. 34 Karten-Tests schützen Navigation, Pfade und Darstellung.
- **Übersicht entkoppelt (A1-05, Teilpaket):** Das Übersichts-ViewModel erhält Einstellungen, Aktualisierungsmeldung, Dialoge und Projektablage gezielt. Es speichert den zentralen Container nicht mehr. 27 Übersichts-Tests schützen Projektauswahl, Vorschau, Navigation und den Schutz ungespeicherter Änderungen.
- **Schacht-Maßnahmen entkoppelt (A1-05, Vorbereitung):** Der Maßnahmen-Controller erhält nur noch Einstellungen, Dialoge und den Maßnahmenkatalog. Er kennt den zentralen Container nicht mehr. Die bestehenden 24 Schacht- und Maßnahmen-Tests schützen Dialogübergabe, Berechnung und Katalogbearbeitung.
- **Schacht-Seite entkoppelt (A1-05, Teilpaket):** Das Schacht-ViewModel erhält Einstellungen, Dialoge sowie die drei Schacht-Dienste gezielt. ViewModel und Seite speichern den zentralen Container nicht mehr. 29 fokussierte Tests schützen Pflichtfeldwarnung, Protokollbefehle, Maßnahmen und Seitenaufbau.
- **Sanierungs-Matrix entkoppelt (A1-05, Teilpaket):** Das ViewModel erhält Einstellungen, Dialoge, Kostenabgleich und Cockpit-Aktualisierung gezielt. Es speichert den zentralen Container nicht mehr. Zwei neue ViewModel-Tests schützen Projektwurzel, Haltungsaufbau und die Abhängigkeitsgrenze.
- **Projekt-Portabilität zentralisiert (A1-05/A1-07, Vorbereitung):** Der dateibearbeitende Portabilitätsdienst besitzt jetzt einen Application-Vertrag und wird einmal zentral bereitgestellt. Das Import-ViewModel erzeugt ihn nicht mehr selbst. Sechs echte Datei-Tests schützen Pfadumstellung, Fotokopien und Namenskollisionen.
- **Import-Portabilitätsablauf ausgelagert (A1-05, Teilpaket):** Projektprüfung, Fortschritt, Speichern und Ergebnisaufbereitung liegen im testbaren `ImportProjectPortabilityController`. Das Import-ViewModel stößt den Ablauf nur noch an; zwei Verhaltenstests schützen Warnung und Erfolgsfall.
- **Projekt-Fotozuordnung zentralisiert (A1-05/A1-07, Vorbereitung):** Auch die dateibearbeitende Fotozuordnung besitzt jetzt einen Application-Vertrag und wird einmal zentral bereitgestellt. Das Import-ViewModel erzeugt sie nicht mehr selbst. Sechs echte Datei-Tests schützen Zuordnung, Kopieren und relative Projektpfade.
- **Import-Fotozuordnungsablauf ausgelagert (A1-05, Teilpaket):** Projektprüfung, Ordnerauswahl, Fortschritt, Speichern und Ergebnisaufbereitung liegen im testbaren `ImportProjectPhotoAssignmentController`. Das Import-ViewModel stößt den Ablauf nur noch an; zwei Verhaltenstests schützen Warnung und Erfolgsfall.
- **Import-Protokollwerkzeuge ausgelagert (A1-05, Teilpaket):** Verteilung und Neuerzeugung eigener Protokolle liegen in zwei kleinen, getrennt testbaren Controllern. Das Import-ViewModel stößt die Abläufe nur noch an; vier Verhaltenstests schützen Warnungen, Speichern, Status und sichere Fehleranzeige.
- **Protokoll-Neuerzeugung abstrahiert (A1-10, Vorbereitung):** Der dateibearbeitende Dienst ist über `IProtocolRegenerationService` erreichbar und zentral verdrahtet. Die bisherige öffentliche statische Fassade bleibt für bestehende Aufrufer unverändert.
- **Ein-Knopf-Import ausgelagert (A1-05/A1-10, Teilpaket):** Formatprüfung, Hintergrundlauf, Speichern und Ergebnisanzeige liegen im `ImportOneClickProjectController`; der Textbericht wird von einem dateibearbeitenden Dienst geschrieben und Fehler landen im Tageslog. Das Import-ViewModel sank dadurch auf 686 Zeilen. Drei UI-Tests und ein echter Dateitest schützen Start, Erfolg, unbekannte Formate und Berichtinhalt.
- **Import-Berichtsnavigation ausgelagert (A1-05, Teilpaket):** Projektpfad, letzter Bericht, Berichtsordner und sicheres Öffnen liegen im `ImportReportNavigationController`. Das Import-ViewModel kennt keine letzte Berichtsdatei mehr und sank auf 654 Zeilen; zwei Tests schützen fehlende Ordner und den erfolgreichen Öffnungspfad.
- **CSV-Importbericht ausgelagert (A1-05/A1-10, Teilpaket):** Steuerung, Fehlerschutz und atomare Dateiausgabe liegen im zentralen `ImportSummaryExporter`; technische Schreibfehler gehen ins Tageslog. `ProjectFieldCsvExporter` bleibt nur als kompatible statische Fassade, seine reine CSV-Maskierung darf statisch bleiben. Das Import-ViewModel sank auf 603 Zeilen; neun Dateitests und ein Architekturtest schützen Inhalt, Pfad und Schichtgrenze.
- **VSA-Katalogstatus ausgelagert (A1-05, Teilpaket):** SEC-/NOD-Erkennung, Statusaufbau und Neuladen liegen im `ImportCatalogController`. Technische Ladefehler gehen ins Tageslog statt als Rohtext in die Oberfläche. Das Import-ViewModel sank auf 563 Zeilen; drei Tests schützen Startstatus, SEC+NOD und fehlenden NOD-Katalog.
- **VSA-Importnachlauf ausgelagert (A1-05, Teilpaket):** Hintergrundbewertung, Fortschritt und Ergebnistext liegen im `ImportVsaEvaluationController`. Technische Fehler gehen ins Tageslog und werden in der Oberfläche nicht mehr offengelegt. Zwei Tests schützen Erfolg und sicheren Fehlerfall; das Import-ViewModel liegt bei 559 Zeilen.
- **Importseite vom God-Container getrennt (A1-05, Teilpaket):** `ImportPageViewModel` speichert den `ServiceProvider` nicht mehr. Dialoge, Einstellungen, Projektablage und die fünf manuellen Importdienste werden gezielt gehalten; alle ausgelagerten Abläufe erhalten ebenfalls nur ihre benötigten Dienste. Ein Architekturtest verhindert die Rückkehr des Containers.
- **Datenseite schrittweise entkoppelt (A1-05, Teilpaket):** Dialoge und Einstellungen werden als eigene Abhängigkeiten gehalten; sämtliche entsprechenden Laufzeit-Zugriffe über den `ServiceProvider` sind entfernt. Ein Architekturtest schützt diese Grenze, während der restliche Container in weiteren kleinen Paketen abgebaut wird.
- **Service-Locator der Datenseite entfernt (A1-05, Teilpaket):** Ansicht und Teildateien greifen nur noch gezielt auf Dialoge, Einstellungen, VSA-Auswertung und Codekatalog zu. Das Beobachtungsfenster hält ebenfalls nur noch die benötigten Einstellungen; die bisherige öffentliche Fenster-Schnittstelle bleibt unverändert nutzbar. Der Architekturtest prüft alle Teildateien gemeinsam.
- **Datenseiten-Fachdienste entkoppelt (A1-05, Teilpaket):** PDF-Ausgabe, Kostenabgleich, Cockpit-Aktualisierung, Mediensuche, Protokollzugriff und Maßnahmenempfehlung werden als klar benannte Abhängigkeiten gehalten. Nur die zwei Erzeugungsfunktionen für Videoanalyse und Sanierungs-KI verbleiben bis zum nächsten Teilpaket am Container.
- **Datenseite vom God-Container getrennt (A1-05 erledigt):** Auch Videoanalyse und Sanierungs-KI werden über zwei kleine, getestete Fabriken erzeugt. Die Datenseite und sämtliche ihrer Teildateien speichern den `ServiceProvider` nicht mehr. Ein schmaler Fensterstarter kapselt nur noch die Verträglichkeit zu den bestehenden Player- und Protokollfenstern. Der Architekturtest prüft die gesamte Teilklasse statt nur die Hauptdatei.
- **Protokoll-PDF sauber geschichtet (A1-06 erledigt):** `IProtocolPdfExporter`, Optionen und Hydraulik-Ergebnis bleiben als stabile Application-Verträge erhalten. Die sechs konkreten QuestPDF-Renderer liegen jetzt in Infrastructure; Application und UI besitzen keine eigene QuestPDF-Paketabhängigkeit mehr. Öffentliche Typnamen, Namespaces und Aufrufe bleiben gleich. Ein Schichttest und 116 gezielte PDF-/Dossier-/Hydrauliktests schützen Vertrag, Ausgabe und die neue Abhängigkeitsrichtung.
- **FFmpeg-Abbruch gehärtet (A2-01 erledigt):** Die Einzelbild-Extraktion nutzt den gemeinsamen Prozessleser. Bei Abbruch oder Fehler wird der gesamte FFmpeg-Prozessbaum beendet; ein echter Prozess-Test prüft, dass kein Hintergrundprozess weiterläuft.
- **Training-Center-Abbruch gehärtet (A2-02 erledigt):** Das Fenster besitzt einen eigenen Lebensdauer-Abbruchschutz. Beim Schließen werden SAM-Segmentierung sowie laufende Generierungs- und Batch-Aufgaben abgebrochen und die Token-Quellen freigegeben. Zwei Tests schützen Weitergabe und wiederholbares Aufräumen.
- **Dichtheitsimport gegen KI-Blockade gehärtet (A2-03 erledigt):** Der asynchrone KI-Aufruf ist vom aufrufenden Synchronisationskontext getrennt, verwendet `ConfigureAwait(false)` und endet auch bei einem nicht kooperierenden KI-Aufruf nach 25 Sekunden. Ein Regressionstest bildet einen blockierten Oberflächen-Kontext nach.
- **Videoanalyse-Verbindungen wiederverwendet (A2-04 erledigt):** Die Datenseite hält je Zeitlimit einen langlebigen `HttpClient`, statt bei jeder Analyse eine neue Verbindung aufzubauen. Beim Schließen werden alle Clients wiederholbar freigegeben. Zwei Tests schützen Wiederverwendung und Aufräumen.
- **Videoanalyse-Abbruchquelle freigegeben (A2-05 erledigt):** Beim Schließen des Pipeline-Fensters wird die laufende Analyse zuerst abgebrochen und die Token-Quelle danach freigegeben. Ein Lebensdauer-Guard schützt die Reihenfolge.
- **Datenseiten-Suchtimer gestoppt (A2-06 erledigt):** Beim Verlassen der Datenseite werden Such- und Layout-Verzögerungstimer gemeinsam gestoppt. Ein Lebensdauer-Guard verhindert verspätete Suchläufe auf der entladenen Ansicht.
- **Backup-Geheimnisse redigiert (A3-01 erledigt):** `umgebung.txt` und `RESTORE-ANLEITUNG.txt` schreiben Werte von Variablen mit `TOKEN`, `SECRET`, `KEY` oder `AUTH` nur noch als `***redigiert***`. Normale Wiederherstellungspfade bleiben lesbar; ein Integrationstest prüft beide Dateien.
- **Backup-Inhalte mit SHA-256 prüfbar (A3-02 erledigt):** `manifest.json` enthält für jede aktuelle Sicherungsdatei Pfad, Länge und SHA‑256. Der Proberestore prüft diese Nachweise vor Datenbank und Projekt; fehlende, zusätzliche oder veränderte Dateien werden gemeldet. Tests bestätigen eine gleich große beschädigte Datei und blockieren Manifest-Pfade außerhalb der Sicherung.
- **Sidecar-Token geschützt (A3-03 erledigt):** Nur `PipelineSidecarToken` wird in `settings.json` per Windows-DPAPI an das aktuelle Benutzerkonto gebunden. Alte Klartextwerte werden weiter gelesen und beim nächsten Speichern automatisch geschützt; ein beschädigter oder nach einem PC-Wechsel nicht mehr lesbarer Token verwirft nicht die übrigen Einstellungen. Drei Tests schützen Verschlüsselung, Migration und den sicheren Fehlerfall.
- **Wissensdatenbank-Pfad validiert (A3-04 erledigt):** Relative oder ungültige Pfade aus `SEWERSTUDIO_KNOWLEDGE_ROOT` werden nicht mehr als Datenbankordner verwendet; stattdessen bleibt der gespeicherte absolute Pfad oder der sichere Standard aktiv. Abweisungen und jeder gültige Umgebungs-Override werden im Release-tauglichen Tageslog festgehalten.
- **Lokales HTML nicht mehr direkt geöffnet (A3-05 erledigt):** `SafeShellOpen` erlaubt keine `.html`- oder `.htm`-Dateien mehr. Die tatsächlich benötigten PDF-, Bild-, Video-, Tabellen-, Text- und Ordnerpfade bleiben unverändert; zwei Tests schützen beide gesperrten Endungen.
- **IBAK-Originaldatenbank geschützt (A4-01 erledigt):** Firebird öffnet die Kunden-`.fdb` nicht mehr direkt, sondern ausschließlich eine eindeutige, schreibbare Temp-Kopie. Die Kopie wird nach dem Lesen wieder entfernt; zwei Tests sichern Inhaltsisolation, Schreibschutz des Originals und Aufräumen.
- **NPK-Export blockiert die Oberfläche nicht mehr (A4-02 erledigt):** CSV- und Excel-Leistungsverzeichnis erzeugen und schreiben ihre Dateien im Hintergrund. Ein gemeinsamer Laufstatus verhindert Doppelstarts; CSV wird zusätzlich atomar geschrieben. Zwei Tests schützen den Hintergrundlauf und die unveränderten Schaltflächen-Befehle.
- **Excel-Speicherverbrauch begrenzt (A4-03 erledigt):** Der speichergebundene ClosedXML-Vorlagenexport lehnt mehr als 20.000 Haltungen oder Schächte ab, bevor die Arbeitsmappe geladen oder eine Zieldatei angelegt wird. Die Oberfläche nennt die Grenze und empfiehlt das Aufteilen; drei Tests schützen Grenzwert, Meldung und den frühen Abbruch.
- **PDF-Textextraktion begrenzt (A4-04 erledigt):** Vor `pdftotext` wird die Seitenzahl gegen das vorhandene Budget geprüft. Externe und interne Extraktion laden insgesamt höchstens 16 Millionen Zeichen in den Speicher; die externe Textdatei wird dabei stückweise statt vollständig gelesen. Zwei Verhaltenstests und der vorhandene Nutzungs-Guard schützen beide Grenzen.
- **Browser-PDF-Export zeitlich begrenzt (A4-05 erledigt):** Angebot, Druckcenter und NPK-PDF besitzen ein hartes Gesamtlimit von zwei Minuten. Browserstart, HTML-Laden, PDF-Erzeugung und Chromium-Installation beachten den Abbruch; Installationsprozesse werden bei Ablauf samt Unterprozessen beendet, Seite und Browser begrenzt aufgeräumt. Zwei Tests unterscheiden Zeitablauf und Benutzerabbruch.
- **Firebird-Serverzugang fail-closed (A4-06 erledigt):** Die bekannten IBAK-Standarddaten bleiben ausschließlich für lokale Embedded-Dateien erlaubt. Server-, Netzwerk- und IPv6-Pfade erfordern ausdrücklich `IBAK_FDB_USER` und `IBAK_FDB_PASSWORD`; ohne beide Werte wird vor dem Verbindungsaufbau abgebrochen. Sieben neue Testfälle schützen Zugangspflicht und Pfaderkennung.
- **Selbst gestartete KI-Prozesse aufgeräumt (A5-01 erledigt):** Nur Ollama- und Sidecar-Prozesse, die Sewer Studio selbst startet, werden registriert. Beim Beenden der App schließt ein Windows-Job-Objekt diese Prozesse samt Unterprozessen; eine Prüfung von Prozess-ID und Startzeit schützt den Rückfallpfad vor Verwechslungen. Bereits vorher laufende KI-Dienste bleiben unangetastet. Zwei Tests schützen Prozessende und App-Verdrahtung.
- **Sidecar-Sicherheitsgrenze klargestellt (A5-02 erledigt):** Code und Betriebsdokumentation benennen die Host-Prüfung ausdrücklich nur als Schutz gegen DNS-Rebinding. Ausschließlich das geheime `X-Sidecar-Token` kontrolliert den Zugriff; selbst eine Host-Freigabe mit `*` umgeht das Token nicht. Drei gezielte und alle 86 GPU-freien Sidecar-Tests sind grün.
- **Sidecar bleibt lokal gebunden (A5-03 erledigt):** `SEWER_SIDECAR_HOST` akzeptiert nur `localhost`, IPv4-Loopback (`127.x.x.x`) oder IPv6-Loopback (`::1`). Python-Konfiguration und PowerShell-Startskript weisen `0.0.0.0`, LAN-Adressen und Hostnamen vor dem Dienststart ab. Acht neue und insgesamt 94 GPU-freie Sidecar-Tests sind grün.
- **CUDA-Ausfälle verständlich klassifiziert (A5-04 erledigt):** CUDA-, Treiber-, cuDNN-, cuBLAS- und Kernel-Ausfälle werden ohne interne Treiberdetails als vorübergehender 503-Fehler mit stabilem Code `cuda_unavailable` gemeldet. Der vorhandene C#-Client behandelt 503 nach genau einem Wiederholungsversuch als nicht verfügbaren KI-Dienst. Alle 97 GPU-freien Sidecar-Tests sind grün.
- **Leerer Sidecar-Token sperrt sicher (A5-05 erledigt):** Ein nicht initialisierter oder leerer Server-Token deaktiviert die Anmeldung nicht mehr. Jede Anfrage wird mit einem bereinigten 503 und `auth_unavailable` abgewiesen; normale Routentests verwenden nun ebenfalls einen ausdrücklichen Test-Token. Alle 97 GPU-freien Sidecar-Tests sind grün.
- **QGIS-Lesebrücke bewusst begrenzt (A5-06 erledigt):** Für den Einzelplatz bleibt die QGIS-Brücke kompatibel ohne Token, ist aber ausschließlich an IPv4-Loopback gebunden und akzeptiert nur `GET`/`HEAD`. Die Betriebsdokumentation nennt das lokale Leserisko und verlangt auf Mehrbenutzer-/Terminalservern die Deaktivierung. Ein Architekturtest schützt Code und Sicherheitsgrenze.
- **Ollama-Autostart lokal erzwungen (A5-07 erledigt):** Ein von Sewer Studio gestartetes Ollama erhält prozessbezogen `OLLAMA_HOST=127.0.0.1:<Port>` und kann keinen geerbten Netzwerk-Listener übernehmen. Bei einer nichtlokalen Ollama-URL erscheint eine klare Warnung; Sewer Studio startet dafür keinen eigenen Prozess. Zwei Tests schützen Startentscheidung und Prozessumgebung.
- **UI-Verhalten direkt geprüft (A6-01, Teilpaket):** Die Sanierungsoptimierung und die Parameterprüfung werden als echte ViewModels erzeugt und über ihre Befehle beziehungsweise Eingaben ausgeführt. Sechs neue Tests schützen Request-Zuordnung, Doppelstart-Sperre, KI-Fehler, Abbruch, Pflicht-/Auswahleingaben und Uhrpositionen. Noch offen sind weitere ViewModels.
- **Kleine UI-Datenmodelle direkt geprüft (A6-01, Teilpaket):** Maßnahmenauswahl, Codeparameter, Katalogspalten und Medienkonflikt-Kandidaten werden ohne Fenster oder Dateien instanziiert. Fünf Tests schützen Vorlagenabbildung, unabhängige Auswahl, Zahlen-/Auswahlvalidierung mit Fehlerheilung, sortierte Kataloganzeige und sichere Pfadaufteilung.
- **Sanierungsmaßnahmen direkt geprüft (A6-01, Teilpaket):** Das zusammenführende ViewModel wird mit echter Kostenberechnung, aber ohne Dateiablage oder KI-Aufruf instanziiert. Zwei Tests schützen die Haltungs-Stammdaten, den Schließen-Befehl und die Übernahme einer Regelempfehlung über die robuste Katalogauflösung.
- **Codekatalog-Speichern direkt geprüft (A6-01, Teilpaket):** Das Editor-ViewModel läuft auf einem echten STA-Dialogthread. Zwei Tests belegen, dass ungültige Kataloge mit verständlicher Meldung offen bleiben und nicht gespeichert werden, während gültige Änderungen gespeichert und bestätigt werden. Ein gemeinsamer STA-Testläufer begrenzt modale UI-Tests auf 15 Sekunden.
- **Kostenkatalog gegen Überschreiben geschützt (A6-01, Teilpaket):** Der Editor kann seinen konkreten Speicher für Tests gezielt erhalten; bestehende Aufrufer bleiben unverändert. Zwei echte Temp-Dateitests belegen erfolgreiches, atomares Speichern und dass ein beschädigter Benutzerkatalog weder überschrieben noch als Erfolg bestätigt wird.
- **Maßnahmenvorlagen direkt geprüft (A6-01, Teilpaket):** Der Vorlagen-Editor wird vollständig gegen isolierte Temp-Speicher ausgeführt. Zwei Tests schützen ID-Normalisierung, Dezimalmengen, Positionszuordnung und den Verlustschutz: Eine beschädigte Vorlagendatei bleibt unverändert und erzeugt eine klare Fehlermeldung.
- **Positionsvorlagen direkt geprüft (A6-01, Teilpaket):** Der Editor erhält seine beiden Dateispeicher optional und damit testbar; bestehende Aufrufer bleiben unverändert. Zwei Temp-Dateitests schützen Reihenfolge, Wartebox, Rückholen per Tiefkopie, Kataloganzeige, Speichern sowie den Überschreibschutz bei einer beschädigten Datei.
- **WinCan-Katalogauswahl direkt geprüft (A6-01, Teilpaket):** Suchdienst und Startordner sind optional übergebbar; der normale automatische Suchlauf bleibt unverändert. Ein echter Temp-XML-Test schützt Erkennung von SEC/NOD, Länder-/Standardfilter, Auswahl, Befehlsfreigabe und die bestätigte Pfadübergabe, ohne installierte WinCan-Ordner zu lesen.
- **Alter Preiskatalog-Editor isoliert geprüft (A6-01, Teilpaket):** Der bestehende Kompatibilitätsdienst akzeptiert optional einen Benutzerordner; ohne Übergabe bleibt sein bisheriger Pfad unverändert. Ein STA-Dateitest schützt Laden, Hinzufügen, Filtern und Speichern, ohne das echte Windows-Profil zu berühren. Damit kommt jedes öffentliche ViewModel wenigstens in einem UI-Test vor.
- **Testarten getrennt (A6-03, Teilpaket):** Maschinengebundene Tests tragen die Kategorie `Integration`; zukünftige Nachtlauftests verwenden `Endurance`. Der pre-push-Hook führt weiterhin alle drei großen Testprojekte aus, schließt diese zwei langsamen Kategorien aber ausdrücklich aus. Ein Regressionstest schützt den Filter in allen drei Befehlen.
- **Sidecar-Prozess messbar (A6-03, Vorbereitung):** Der token-geschützte lokale Health-Endpunkt liefert zusätzlich die tatsächliche Python-Prozess-ID. C#-Vertrag und E2E-Report übernehmen sie optional; ältere Sidecars bleiben kompatibel. Damit kann der Nachtlauf RAM und Handles des richtigen Prozesses statt des PowerShell-Starters messen.
- **KI-Nachtlauf fahrbar (A6-03, Teilpaket):** `tools/NightlySoakRunner` wiederholt den echten Video-/Sidecar-Golden-Test über ein oder mehrere Videos. Jede Runde schreibt Laufzeit, Python-Prozess-RAM, Handles, Health-VRAM und – wenn `nvidia-smi` verfügbar ist – den prozessbezogenen GPU-Speicher sofort in eine CSV. Feste Ober- und Wachstumsgrenzen, P95-Prüfung, `GC.Collect`, Strg+C und eine gehaltene Sidecar-Lebensdauer verhindern stille Fehler und unbeabsichtigte Prozessreste. Sechs Verhaltenstests schützen Optionen, Loopback-Grenze, Video-Wechsel, CSV, Leak- und Latenzabbruch. Der echte achtstündige Lauf auf der Zielmaschine ist weiterhin eine offene Freigabeprüfung.
- **QGIS-Brückenvertrag geprüft (A6-04 erledigt):** Der Pfad-Router ist von UI-Dispatcher und HTTP-Host getrennt. Vierzehn C#-Fälle prüfen alle Status-/GeoJSON-Endpunkte, Query-Strings, 404-Vertrag und den serialisierten Antwortcache. Fünf Python-Smokes rufen einen echten lokalen Testserver ab und prüfen Accept-Header, JSON/GeoJSON, stilles 404, sichtbares 503 sowie die Ablehnung nichtlokaler oder fehlerhafter Bridge-URLs. Dafür ist weder QGIS noch ein externes Python-Paket nötig.
- **KB-Bestandsmigration geprüft (A6-05 erledigt):** Ein echter SQLite-Test legt das alte Wissensdatenbank-Schema ohne die sechs späteren Sample-Spalten an und befüllt Samples, Embeddings und Versionsdaten. Das Öffnen über den produktiven Kontext ergänzt alle Spalten samt sicheren Standardwerten, setzt die Schema-Version und erhält jeden alten Fachwert sowie den Embedding-BLOB unverändert.
- **PDF-Importfehler begrenzt (A6-06, Teilpaket):** Der öffentliche PDF-Importweg wird mit einer leeren und einer nach dem PDF-Header abgeschnittenen Datei ausgeführt. Beide Dateien erzeugen genau einen protokollierten Einzelfehler, legen keine Haltung und keinen Importverlauf an und werfen keine Ausnahme bis zur aufrufenden Stapelverarbeitung. XTF, WinCan und FDB folgen getrennt.
- **XTF-Dateifehler isoliert (A6-06, Teilpaket):** Der öffentliche Mehrdatei-Adapter kann für Tests einen isolierten Parser ohne echtes Benutzerarchiv erhalten; normale Aufrufer bleiben unverändert. Ein abgeschnittenes XTF erzeugt genau einen Dateifehler, danach wird das folgende gültige SIA405-XTF weiterhin vollständig importiert. WinCan und FDB bleiben offen.
- **WinCan-DB-Fehler begrenzt (A6-06, Teilpaket):** Eine als `.db3` benannte Nicht-Datenbank wird über den echten WinCan-Importweg geöffnet. Sie liefert genau einen verständlichen Importfehler, der MDB-Fallback bleibt kontrolliert, Projekt und Schächte bleiben leer und die fremde Originaldatei ist bytegenau unverändert. Nur FDB bleibt offen.
- **IBAK-FDB-Fehler isoliert (A6-06 erledigt):** Der vollständige IBAK-Import läuft mit einer ungültigen `Arizona.fdb` und einer gültigen `Daten.txt`. Der optionale FDB-Fotozugriff fällt mit klarer Meldung auf die Dateinamenslogik zurück, die Haltung aus `Daten.txt` wird trotzdem importiert und das Kundenoriginal bleibt bytegenau unverändert. Damit sind die vier geforderten Importer-Negativpfade abgedeckt.
- **SAM/DINO ohne GPU startgeprüft (A6-07 erledigt):** Zwei schnelle CPU-Smokes ersetzen nur die schweren Fremdmodelle, durchlaufen aber die echten SewerStudio-Lader. Sie prüfen Modell- und Konfigurationsauswahl, CPU-Weitergabe sowie den Rückgabevertrag. Die bereits vorhandenen GPU-freien Endpunkttests schützen zusätzlich das Antwortschema und die sichtbare Fehlerkennzeichnung.
- **KB-Parallelzugriff geprüft (A6-08 erledigt):** Ein echter Zwei-Verbindungs-Test hält die Neuaufbau-Transaktion absichtlich offen. Die Suche bleibt dank WAL lesbar, sieht währenddessen den letzten vollständigen Bestand und übernimmt nach dem Commit automatisch die neuen Datensätze. Der Test ist auf zwei Sekunden begrenzt und würde eine Datenbanksperre sichtbar machen.
- **Schneller Alltags-Build ergänzt (A8-03 erledigt):** `AuswertungPro.Dev.slnf` enthält ausdrücklich nur die vier Produkt- und vier Testprojekte. Benötigte Projektverweise werden weiterhin automatisch gebaut, die übrigen eigenständigen Hilfsprogramme aber nicht mehr. README und ein Architekturtest schützen die Nutzung und den schlanken Inhalt.
- **Agenten-Einstieg aktualisiert (A8-04 erledigt):** `AGENTS.md` verweist auf die ausführliche aktuelle Architektur in `CLAUDE.md` und nennt nur noch die stabilen Arbeits-, Sicherheits- und Prüfregeln. Veraltete Angaben zu einem reinen PDF-Tool, .NET 8 und optionalen Tests sind entfernt; ein Test verhindert die Rückkehr dieser Widersprüche.
- **Diagnosepaket eingebaut (A8-05 erledigt):** Die Diagnose-Seite kann den Log-Ordner öffnen und ein ZIP speichern. Ein eigener Dienst nimmt höchstens sieben Tageslogs mit je maximal 5 MiB plus technische Systemangaben auf. Tokens, Benutzername und absolute Pfade werden nur in den ZIP-Kopien entfernt; Original-Logs und Projektdateien bleiben unberührt. Eine einzelne gesperrte Logdatei wird gemeldet und übersprungen, statt das ganze Paket zu verhindern.
- **.NET-Codeprüfer aktiviert (A8-06 erledigt):** Alle Projekte bauen ausdrücklich mit den SDK-Analyzern auf `latest-recommended`. Die 40 bereits im Altbestand vorkommenden Regelarten sind transparent in `.editorconfig` als IDE-Hinweise baseliniert; neue Regelarten bleiben Build-Warnungen. Ein Konfigurationstest verhindert vollständiges Abschalten oder ein unbemerktes Warnungen-als-Fehler-Gate.
- **Nutzerfehler sicher vereinheitlicht (A8-07 erledigt):** `UserError` übersetzt technische Ausnahmen zentral in kurze Hinweise und schreibt die vollständige Ursache ins Tageslog. Bewusst formulierte Fachhinweise bleiben über `UserFacingException` erhalten. Alle gefundenen UI-Dialoge sind umgestellt; ein Architekturtest verhindert neue rohe `ex.Message`-Ausgaben.
- **God-Class-Schlupfloch geschlossen (A1-09 erledigt):** Der Wartbarkeitstest zählt nun alle Dateien desselben partiellen Typs zusammen. `PlayerWindow` (5.451 Zeilen/97 Teile) und `HoldingFolderDistributor` (3.064 Zeilen/6 Teile) sind auf ihren heutigen Stand eingefroren und dürfen nur noch schrumpfen; neue partielle Klassen über 2.000 Zeilen schlagen sofort fehl.
- **Leeren Architekturtest aktiviert (A1-08, Teilpaket):** `UiArchitectureGuardTests` schützt nun tatsächlich die zentrale Aufbaugrenze: Nur `MainWindow` darf `App.Services` verwenden. Die Regel wurde unverändert aus dem großen Sammeltest verschoben; weitere fachlich passende Regeln und der Ersatz alter Textsuchen bleiben offen.
- **Player-Core-Regeln einsortiert (A1-08, Teilpaket):** Drei Grenzen gegen direkte UI-Service-, DialogHost- und Fensterzugriffe liegen nun in `PlayerWindowCoreArchitectureTests` statt im allgemeinen 1.705-Zeilen-Sammeltest. Alle Regeln bleiben einzeln erhalten; `ArchitectureFitnessTests` sank auf 1.669 Zeilen.
- **Overlay-Host-Regeln zusammengeführt (A1-08, Teilpaket):** Drei doppelte globale Texttests für Kalibrierung, Werkzeugzustand und Overlay-Eingabe sind in die bereits vorhandenen fachlichen `PlayerWindowOverlayHostArchitectureTests` integriert. Positive Host-Struktur und verbotene Direktzugriffe werden nun gemeinsam geprüft; der Sammeltest sank weiter auf 1.621 Zeilen. Die Gesamtzahl sank bewusst um drei Dubletten, ohne Schutzwirkung zu verlieren.
- **Dispatcher-Regeln zusammengeführt (A1-08, Teilpaket):** Die globalen Direktzugriffs- und Coding-Statistik-Prüfungen sind in `PlayerWindowWiringArchitectureTests` und `PlayerWindowCodingStatisticsArchitectureTests` integriert. Scheduler-Struktur und verbotene Dispatcher-Aufrufe stehen nun zusammen; der Sammeltest sank auf 1.592 Zeilen. Zwei doppelte Tests entfielen ohne Verlust einer Regel.
- **Overlay-Cleanup-Regeln zusammengeführt (A1-08, Teilpaket):** Die positiven Strukturprüfungen und die verbotenen Direktzugriffe für kurzlebige sowie Erkennungs-Einblendungen liegen nun gemeinsam in `PlayerWindowOverlayCleanupArchitectureTests`. Der Sammeltest sank auf 1.526 Zeilen. Zwei doppelte Tests entfielen ohne Verlust einer Regel.
- **Schema-Overlay-Regeln zusammengeführt (A1-08, Teilpaket):** Aufbau und verbotene Rückfälle für Schema-Zustand, Maussteuerung und Arbeitsabläufe werden nun gemeinsam in `PlayerWindowSchemaOverlayArchitectureTests` geprüft. Der Sammeltest sank auf 1.473 Zeilen. Drei doppelte Tests entfielen ohne Verlust einer Regel.
- **Live-KI-Grenzregeln fachlich getrennt (A1-08, Teilpaket):** 14 Rückfallregeln für Laufzeitaufbau, Lebenszyklus, Status, Dialoge, Snapshot, Overlay, Start/Stop und Quickscan liegen jetzt in `PlayerWindowLiveDetectionBoundaryArchitectureTests`. Die gemeinsame Quelltextsuche liegt einmal in `ArchitectureSourceGuard`; alle 59 bisherigen Regeln und Namen blieben erhalten. `ArchitectureFitnessTests` sank von 1.442 auf 1.134 Zeilen.
- **Player-Timer-Regeln zusammengeführt (A1-08, Teilpaket):** Positive Struktur und die vier Rückfallprüfungen für Erzeugung, Zustand, Abschalten und Coding-OSD-Gates liegen nun gemeinsam in `PlayerWindowTimerArchitectureTests`. Vier doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 1.134 auf 1.027 Zeilen.
- **Player-Tastaturregeln zusammengeführt (A1-08, Teilpaket):** Aufbau sowie die drei Rückfallprüfungen für Ereignisbesitz, Aktionsausführung und Shortcut-Bedienung liegen nun gemeinsam in `PlayerWindowKeyboardArchitectureTests`. Drei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 1.027 auf 963 Zeilen.
- **Player-Multi-Modell-Regeln zusammengeführt (A1-08, Teilpaket):** Analyseablauf, Ereignisprojektion und Maskenanzeige werden nun jeweils zusammen mit ihren Rückfallprüfungen in `PlayerWindowCodingMultiModelArchitectureTests` geschützt. Drei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 963 auf 892 Zeilen.
- **Player-Medienbesitz-Regeln zusammengeführt (A1-08, Teilpaket):** LibVLC-/MediaPlayer-Erzeugung und Media-Host-Besitz werden nun zusammen mit ihren Rückfallprüfungen in `PlayerWindowMediaInfrastructureArchitectureTests` geschützt. Zwei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 892 auf 860 Zeilen.
- **Player-Aufnahmeregeln zusammengeführt (A1-08, Teilpaket):** Nicht blockierende Coding-Fotoextraktion und gekapselte Player-Snapshots werden nun zusammen mit ihrem jeweiligen Aufbau in `PlayerWindowCodingPhotoArchitectureTests` und `PlayerWindowSnapshotArchitectureTests` geschützt. Zwei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 860 auf 832 Zeilen.
- **Player-Protokollregeln zusammengeführt (A1-08, Teilpaket):** Projektzugriff, Trainings-/Import-Mapping, PDF-/Vorschauablauf, Projektroot und Protokollkontext-Grenzen werden nun in `PlayerWindowShellProjectArchitectureTests`, `PlayerWindowProtocolEventMappingArchitectureTests`, `PlayerWindowProtocolPdfArchitectureTests` und `PlayerWindowRuntimeArchitectureTests` gemeinsam mit ihrem positiven Aufbau geschützt. Acht doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 832 auf 684 Zeilen.
- **Player-Laufzeitbesitz-Regeln zusammengeführt (A1-08, Teilpaket):** Overlay-, KI- und Session-Dienste, Analyse-Abbruchquelle sowie Session-VM-/Navigationszustand werden nun zusammen mit ihrem positiven Aufbau in `PlayerWindowRuntimeArchitectureTests` geschützt. Drei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 684 auf 629 Zeilen.
- **Player-Navigations-/Timeline-Regeln zusammengeführt (A1-08, Teilpaket):** Badge-/Sync-Details sowie verbotene Rohzugriffe auf Player-Zeit und MediaPlayer werden nun zusammen mit ihrem positiven Aufbau in `PlayerWindowCodingNavigationArchitectureTests` und `PlayerWindowTimelineHostArchitectureTests` geschützt. Drei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 629 auf 522 Zeilen.
- **Player-Visualregeln zusammengeführt (A1-08, Teilpaket):** Screenshot-Aufnahme, Bogenmarker, Werkzeug-Badge und Statusfarben werden nun zusammen mit ihrem positiven Aufbau in `PlayerWindowVisualInfrastructureArchitectureTests` geschützt. Vier doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; eine veraltete Kindsuche-Regel wurde durch die stärkere programmweite `VisualTreeConsolidationTests`-Prüfung ersetzt. Im allgemeinen Sammeltest bleibt nur eine Player-Themenregel; `ArchitectureFitnessTests` sank von 522 auf 434 Zeilen.
- **UI-Themenregeln fachlich getrennt (A1-08, Teilpaket):** Player-Ressourcen, alte Fensterfarben und dynamische Theme-Brushes liegen nun in `ThemeResourceArchitectureTests`. Die Akzentfarben-Rückfallprüfung für Seitentitel ist in den vorhandenen positiven Theme-Test integriert. `ArchitectureFitnessTests` sank von 434 auf 209 Zeilen; die neue Fachklasse bleibt mit 176 Zeilen klein.
- **Systemmonitor-Regeln zusammengeführt (A1-08, Teilpaket):** Externe Prozessausführung und kompakte Monitoranzeige werden nun in `SystemMonitorProcessSafetyTests` jeweils zusammen mit ihren positiven Sicherheits- und Aufbauprüfungen geschützt. Zwei doppelte Testhüllen entfielen ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 209 auf 176 Zeilen.
- **Dialogdienst-Regeln zusammengeführt (A1-08, Teilpaket):** Datenseite, Training Center und Beobachtungsfenster werden nun in `DesignAuditDialogMigrationTests` gemeinsam auf Dialogdienst-Nutzung und verbotene direkte `MessageBox`-Aufrufe geprüft. Eine doppelte Testhülle entfiel ohne Verlust einer Schutzprüfung; `ArchitectureFitnessTests` sank von 176 auf 158 Zeilen.
- **Fenstergrößen-Regel zusammengeführt (A1-08, Teilpaket):** Das Beobachtungsfenster wird nun in `DesignAuditChromeAndGlyphTests` gemeinsam auf Titel, maximierten Start, Mindestgröße und das Verbot der alten festen Größe geprüft. Eine doppelte Testhülle samt privatem XAML-Helfer entfiel; `ArchitectureFitnessTests` sank von 158 auf 125 Zeilen.
- **Sanierungs-Matrix-Regeln zusammengeführt (A1-08, Teilpaket):** Matrixdarstellung und Datenseiten-Navigation werden nun in `DesignAuditThemeResourceTests` jeweils zusammen mit dem neuen Aufbau und den Verboten des alten Gruppenlayouts beziehungsweise Fensterpfads geschützt. Zwei doppelte Testhüllen samt privatem Blockleser entfielen; `ArchitectureFitnessTests` sank von 125 auf 80 Zeilen.
- **Symbolregel fachlich verschoben (A1-08, Teilpaket):** Die XAML-Prüfung gegen sichtbare Symbolzeichen und Emoji-Schriften liegt nun bei den echten `FluentIconTests`. Die Regel und ihre sechs geprüften Dateien blieben unverändert; `ArchitectureFitnessTests` sank von 80 auf 41 Zeilen.
- **Architektur-Sammeltest aufgelöst (A1-08 erledigt):** Die letzte Prüfung gegen toten Shell-Guide-Code liegt nun in `ShellNavigationPolicyTests`. Alle früheren Regeln des 1.705-Zeilen-Sammeltests sind damit fachlich eingeordnet, mit stärkeren bestehenden Regeln zusammengeführt oder durch semantische Typprüfungen ersetzt; die leere Datei `ArchitectureFitnessTests.cs` wurde gelöscht.
- **Player-Bedieneinstellungen ausgelagert (A1-01, Teilpaket):** Lautstärke, automatische Stummschaltung, Mute und Overlay-Transparenz werden im `PlayerControlSettingsController` normalisiert und gespeichert. `AppSettings` erfüllt dafür einen kleinen Vertrag; das Fenster wendet nur noch den fertigen Zustand auf seine Bedienelemente an. Acht Verhaltenstests schützen Grenzwerte und genau einen Speichervorgang. Der eingefrorene `PlayerWindow`-Umfang sank von 5.451 auf 5.423 Zeilen.
- **Protokoll-Neuerzeugung instanzbasiert (A1-10, Teilpaket):** Der echte PDF- und Dateischreibpfad liegt nun im injizierbaren `ProtocolRegenerationAdapter`. PDF-Ausgabe und Einzel-Neuerzeugung besitzen kleine Application-Verträge; Import und Datenseite verwenden dieselbe zentral aufgebaute Instanz. Die alte statische Klasse bleibt nur als kompatible Fassade. Ein neuer Dateitest belegt die injizierte PDF-Ausgabe.
- **Plan-PDF-Import instanzbasiert (A1-10, Teilpaket):** Dateikopie, Kollisionen und Dokumenterkennung liegen im `PlanPdfImportService`; der Ein-Knopf-Import erhält dieselbe zentral aufgebaute Instanz. Die bisherige statische API bleibt als dünne Fassade erhalten. Ein neuer Dateitest ersetzt die PDF-Erkennung gezielt und prüft Kopie sowie Überspringen bytegenau.
- **Haltungsumbenennung instanzbasiert (A1-10, Teilpaket):** Die Datenseite erhält den dateibearbeitenden `IHoldingRenameService` zentral und ruft keine statische IO-Klasse mehr direkt auf. `HoldingRenameFileService` bleibt durch die bisherigen neun Datei-/Rollback-Tests und einen neuen Instanztest geschützt; die öffentliche statische Fassade bleibt für bestehende Aufrufer kompatibel.
- **Dropdown-Speicher instanzbasiert (A1-10, Teilpaket):** Projekt-, Haltungs- und Schachtseite verwenden denselben zentralen `IDropdownOptionsStore` statt direkter statischer Dateizugriffe. Frei wählbare Pfade machen Speichern, beschädigte JSON-Dateien und Isolation testbar; ein Instanz-Lock schützt gleichzeitige Schreibvorgänge. Die bisherige statische API bleibt als Kompatibilitätsfassade erhalten, und ein Architekturtest verhindert neue Direktzugriffe aus den drei ViewModels.
- **Schachtumbenennung instanzbasiert (A1-10, Teilpaket):** Der Schachtseiten-Controller erhält `IShaftRenameService` gezielt und ruft keine statische Dateioperation mehr auf. Ordner-, PDF- und Pfadumbenennung bleiben durch die zwei bestehenden Dateitests und einen neuen Instanztest geschützt; die öffentliche statische Fassade bleibt kompatibel.
- **Import-Quelldateispeicher instanzbasiert (A1-10, Teilpaket):** Kopieren, Kollisionsprüfung und Metadatenpflege liegen im zentral bereitgestellten `StoredImportFileService` hinter einem Application-Vertrag. Das Import-ViewModel nutzt die Instanz statt der statischen UI-Klasse; die öffentliche Fassade bleibt kompatibel und enthält keine Dateioperation mehr. Ein echter Temp-Dateitest schützt Kopie und relativen Projektpfad.
- **Projekt-Rettungspunkte instanzbasiert (A1-10, Teilpaket):** Speichern und Ein-Knopf-Import verwenden denselben zentral bereitgestellten `ProjectRestorePointStore`. Die bisherige statische API bleibt als dünne Kompatibilitätsfassade erhalten; die eigentliche JSON-Prüfung, Kopie und Begrenzung liegen im Instanzdienst. Ein Paralleltest belegt acht gleichzeitige, eindeutige Rettungspunkte und der Instanz-Lock verhindert Namenskollisionen.
- **Projekt-Rettung instanzbasiert (A1-10, Teilpaket):** Die Shell erhält `IProjectRecoveryService` zentral und ruft keine statische Dateioperation mehr direkt auf. Auswahl der neuesten `.bak`-/Restore-Point-Datei und Quarantäne des beschädigten Originals liegen im serialisierten Instanzdienst; die bisherige statische API bleibt als dünne Fassade erhalten. Acht echte Rettungstests und ein Architekturtest schützen Datenwahl, Verlustschutz und Verdrahtung.
- **Import-Quellarchiv instanzbasiert (A1-10, Teilpaket):** Der Ein-Knopf-Import erhält `IImportSourceArchiver` zentral. Rekursive Auswahl, atomare Kopie, Wiederverwendung und Kollisionsnamen liegen im serialisierten Instanzdienst; damit konkurrieren zwei Aufrufe nicht mehr um denselben Zielnamen. Die statische API bleibt als dünne Fassade erhalten, sechs Dateitests und ein Architekturtest schützen das Verhalten.
- **Dichtheitsprotokoll-Verteilung instanzbasiert (A1-10, Teilpaket):** Der Ein-Knopf-Import erhält `IDichtheitImportDistributor` zentral. Kandidatensuche, KI-Zweitmeinung, Idempotenz und PDF-Kopie liegen im Instanzdienst; die bisherige statische API bleibt als dünne Fassade erhalten. Acht gezielte Datei-/KI-Tests und ein Architekturtest schützen Verteilung, Wiederholung und den blockierten Oberflächenkontext.
- **Kanalimport-Verteilung instanzbasiert (A1-10, Teilpaket):** Der Ein-Knopf-Import erhält `IKanalImportDistributor` zentral. Originalprotokoll-Auswahl, Video-Fallback, Pfadrelativierung und Dateikopie liegen im Instanzdienst; Datums- und Pfadhelfer bleiben über die kompatible statische Fassade erreichbar. Vier gezielte Infrastrukturtests und ein Architekturtest schützen Auswahl, Leerlauf und Verdrahtung.
- **KI-Wissenssicherung isoliert testbar (A1-10, Vorbereitung):** Knowledge-, AppData-, Training-Center- und Temp-Pfade werden als ein klarer Laufzeitkontext übergeben statt mitten im Export und Import global aufgelöst. Drei echte Temp-Ordner-Tests schützen ZIP-Inhalt, zielgenauen Import und die Wiederherstellung der alten Datei bei Abbruch. Die öffentliche statische API und alle produktiven Pfade bleiben unverändert; als nächstes folgt der Instanzdienst.
- **KI-Wissenssicherung instanzbasiert und zerlegt (A1-10, Teilpaket):** Einstellungen und Training Center verwenden denselben zentralen `IKnowledgeBackupService`; die öffentliche statische API bleibt nur als kleine Kompatibilitätsfassade. ZIP-/Rollback-Ablauf, Dateikatalog und Import-Nachbearbeitung liegen in getrennten Dateien mit höchstens 428 Zeilen statt einer 606-Zeilen-Mischklasse. Sechs echte Temp-Ordner-Tests schützen Export, Import, Abbruch-Rollback, atomaren Archiversatz, Training-Center-Rollback und den Ausschluss gleichzeitiger Wissensoperationen.
- **Protokoll-Trainingsspeicher instanzbasiert (A1-10, Teilpaket):** JSON-Lesen, Duplikatpruefung und atomare Ausgabe liegen im `ProtocolTrainingFileStore` hinter `IProtocolTrainingStore`. Fenster und KI-Dienst erhalten dieselbe zentral aufgebaute Instanz; die bisherige statische API bleibt als kompatible Fassade. Sechs gezielte Tests schuetzen Dateiformat, Duplikatregel, Sortierung, beschädigte Dateien, Sicherungskopie und parallele Schreibvorgaenge; Lesefehler werden im Tageslog gemeldet.
- **Training-Center-Einstellungen instanzbasiert (A1-10, Teilpaket):** Laden, Korruptionssicherung und atomare Ausgabe liegen im `TrainingCenterSettingsFileStore` hinter `ITrainingCenterSettingsStore`. Der zentrale `ServiceProvider` verbindet die bestehende statische API mit genau einer Instanz. Fuenf gezielte Prüfungen schuetzen Verdrahtung, Rundlauf, beschädigte Dateien, Sicherungskopie und parallele Schreibvorgaenge; Lesefehler werden im Tageslog gemeldet.
- **Selbsttraining-Verlauf instanzbasiert (A1-10, Teilpaket):** Laden, 20-Lauf-Begrenzung, Korruptionskopie und atomare Ausgabe liegen im `SelfTrainingHistoryFileStore` hinter `ISelfTrainingHistoryStore`. Der zentrale `ServiceProvider` verbindet die kompatible statische API mit genau einer Instanz. Fuenf gezielte Prüfungen schuetzen Verdrahtung, Begrenzung, beschädigte Dateien, Sicherungskopie und paralleles Anfuegen.
- **Trainingssample-Speicher instanzbasiert (A1-10, Teilpaket):** Laden, Eval-Sperre, Duplikat-/Update-Merge, Backup-Rotation und Wiederherstellung liegen im `TrainingSampleFileStore` hinter `ITrainingSampleStore`. Der zentrale `ServiceProvider` baut genau eine Instanz und das Training Center erhält sie gezielt; globale Wissenspfade beeinflussen seine Tests nicht mehr. Fuenf gezielte Prüfungen schuetzen Verdrahtung, Eval-Trennung, beschädigte Dateien, atomare Sicherung und paralleles Zusammenfuehren.
- **Trainingsframe-Speicher instanzbasiert (A1-10, Teilpaket):** Ordneranlage, Video-Frame-Extraktion und PNG-Ausgabe liegen im `TrainingFrameFileStore` hinter `ITrainingFrameStore`. Der zentrale `ServiceProvider` baut genau eine Instanz; Generator und Selbsttraining erhalten den Vertrag gezielt, während die bisherige statische API kompatibel bleibt. Zehn gezielte Prüfungen schützen Dateinamenregel, vorhandene Frames, injizierte Extraktion und zentrale Verdrahtung.
- **VSA-Schattenprotokoll instanzbasiert (A1-10, Teilpaket):** JSONL-Ausgabe, Ordneranlage und Schreibsperre liegen im `VsaShadowTelemetryFileWriter` hinter `IVsaShadowTelemetryWriter`. Der VSA-Dienst erhält die zentrale Instanz ausdrücklich; die bisherige statische API und das Best-Effort-Verhalten bleiben kompatibel. Fünf gezielte Prüfungen schützen JSON-Inhalt, Leerfall, Diagnosegrund, echte Injektion und zentrale Verdrahtung.
- **Sidecar-Telemetrie instanzbasiert (A1-10, Teilpaket):** JSONL-Ausgabe, 10-MB-Rotation und Schreibsperre liegen im `SidecarTelemetryFileWriter` hinter `ISidecarTelemetryWriter`. `VisionPipelineClient` erhält den Schreiber testbar, während die bisherige statische API und alle bestehenden Konstruktoraufrufe kompatibel bleiben. Drei gezielte Prüfungen schützen echte JSON-Ausgabe, Injektion und zentrale Verdrahtung.
- **Telemetrie-Pfadauflösung instanzbasiert (A1-10, Teilpaket):** Umgebungsvariable und lokaler Windows-Anwendungsordner werden einmal im zentralen `TelemetryFilePathResolver` hinter `ITelemetryPathResolver` aufgelöst. Sidecar-, Pipeline- und VSA-Schreiber erhalten dieselbe Instanz; ihre parameterlosen Konstruktoren und die öffentliche statische Pfad-API bleiben kompatibel. Vier Pfad-/Verbrauchertests und eine Verdrahtungsprüfung schützen Speicherorte, Leerfall und die vollständige Nutzung in allen drei Schreibern.
- **Lehrer-Annotationen instanzbasiert (A1-10, Teilpaket):** JSON-Speicher, Bildordner, Duplikatregel und Loeschen liegen im `TeacherAnnotationFileStore` hinter `ITeacherAnnotationStore`. Galerie und zentraler `ServiceProvider` verwenden den Instanzvertrag; die statische API bleibt kompatibel. Zehn gezielte Prüfungen schuetzen Verdrahtung, Galerie, beschädigte Dateien, Sicherungskopie sowie paralleles Anfuegen und Loeschen; IO-Fehler werden im Tageslog gemeldet.
- **KI-Sanierungssitzungen instanzbasiert (A1-10, Teilpaket):** Laden, Haltungsfilter, ID-Ersetzung und atomare Ausgabe liegen im `AiOptimizationSessionFileStore` hinter `IAiOptimizationSessionStore`. Datenseite und Optimierungs-ViewModel erhalten die zentrale Instanz direkt; die statische API bleibt kompatibel. Drei gezielte Tests schuetzen Rundlauf, beschädigte Dateien, paralleles Speichern und die echte ViewModel-Injektion; Lesefehler werden im Tageslog gemeldet.
- **KI-Sicherungsfehler bereinigt (A8-07 Nachhärtung):** Export und Import des KI-Wissens zeigen bei unerwarteten Ausnahmen keine internen Pfade mehr. Der sichtbare Status und Dialog verwenden `UserError`, die vollständige Ursache landet im Tageslog; ein bewusster Abbruch wird nicht als Fehler gemeldet. Zwei Tests schützen beide Richtungen.
- **ViewModel-Status bereinigt (A8-07 Nachhärtung):** Sieben Fehlerpfade in Karte, Schacht-PDF-Nachlauf, VSA, Protokoll-KI, Projektanlage und Sanierungsoptimierung zeigen keine rohen technischen Meldungen oder Dateipfade mehr. Die vollständige Ursache bleibt im Tageslog; ein Architekturtest verhindert neue direkte `ex.Message`-Ausgaben in sichtbaren ViewModel-Statusfeldern.
- **Player-/Foto-/Startstatus bereinigt (A8-07 Nachhärtung):** Fotoladen, Messfoto-Overlay-Export, KI-Schnellscan und KI-Autostart zeigen keine rohen Ausnahme-Texte oder Dateipfade mehr. Vollständige Ursachen landen im Tageslog; der Architekturtest schützt nun auch direkt beschriebene sichtbare Textfelder und die Hauptstatuszeile.
- **Erster echter WPF-Fenster-Smoke (A6-01, Teilpaket):** Karten- und Maßnahmendialog werden auf einem echten STA-Oberflächen-Thread unsichtbar geöffnet, bis zum Leerlauf verarbeitet und wieder geschlossen. Ein 15-Sekunden-Wächter verhindert, dass ein hängendes Fenster den Testlauf blockiert. Hauptfenster und komplexe Fachfenster bleiben offen.
- **Vier weitere WPF-Fenster startgeprüft (A6-01, Teilpaket):** Hydraulik, abgedockte Haltungstabelle, Live-Frame und Textvorschau werden real geöffnet und geschlossen. Der Test fand dabei eine starre Theme-Ressource in der Textvorschau; dynamische Farben verhindern nun den Startabbruch bei noch nicht geladenem App-Theme. Hauptfenster und komplexe KI-/Protokollfenster bleiben offen.
- **Player-Bedienanzeige ausgelagert (A1-01, Teilpaket):** `PlayerControlSettingsView` verteilt den fertigen Lautstärke-, Mute- und Transparenzzustand auf Player und Bedienelemente. Das Fenster kennt weder Symbol-/Hilfetexte noch die sieben einzelnen Anzeigeupdates. Zwei STA-Verhaltenstests schützen Initialzustand und Lautstärkewechsel; der eingefrorene `PlayerWindow`-Umfang sank weiter auf 5.410 Zeilen.
- **Player-Tastaturhilfe ausgelagert (A1-01, Teilpaket):** `PlayerShortcutOverlayController` besitzt Sichtbarkeit und Tastenentscheidung der Hilfe. Das Fenster leitet nur noch Tastendruck sowie Öffnen/Schließen weiter. Sieben Verhaltensfälle schützen F1, Fragezeichen, Escape, blockierte und durchgelassene Tasten; der eingefrorene `PlayerWindow`-Umfang sank auf 5.396 Zeilen.
- **Player-Bedieneingaben gebündelt (A1-01, Teilpaket):** `PlayerControlInputController` übernimmt Startschutz, Geschwindigkeit, Lautstärke, Mute und Overlay-Transparenz. Die Erstellung der Einstellungs- und Anzeigehelfer liegt nun in der zentralen Player-Controller-Fabrik statt im Fenster. Vier neue Verhaltenstests schützen Initialisierung, ignorierte Frühereignisse, Speichern und Geschwindigkeitsänderung; `PlayerWindow` sank auf 5.335 Zeilen.
- **Player-Positionssteuerung ausgelagert (A1-01, Teilpaket):** `PlayerPositionInputController` liest die Positionsleiste, berechnet Zielzeit oder relative Position und steuert Vorschau sowie Scrubben über die vorhandenen Player-Verträge. Drei neue Verhaltenstests schützen normales Springen, Ziehvorschau und Scrubben; `PlayerWindow` sank auf 5.306 Zeilen.
- **Doppelten Player-Ladeablauf entfernt (A1-01, Teilpaket):** Der unbenutzte zweite Aufbau von `PlayerWindowLoadedWorkflow` wurde entfernt; nur der tatsächlich verdrahtete `PlayerWindow_Loaded`-Pfad bleibt. Ein Rückfalltest erzwingt genau einen Ladeablauf. `PlayerWindow` sank auf 5.284 Zeilen.
- **Player-Wiedergabesteuerung ausgelagert (A1-01, Teilpaket):** `PlayerPlaybackController` bündelt Start, Pause, Stopp, Zeitabfrage, Zeitsprung und laufende Anzeige. Das Fenster leitet diese Befehle nur noch weiter. Vier Verhaltenstests schützen Aufrufreihenfolge, Zeitgrenzen, die Ziehvorschau und die Bedienknöpfe; `PlayerWindow` sank auf 5.248 Zeilen.
- **Player-Codierungsnavigation ausgelagert (A1-01, Teilpaket):** `CodingNavigationController` bündelt Meteranzeige, Videoabgleich, aktuellen Codehinweis sowie Vor/Zurück. Auch die Fehlerprotokollierung liegt dort. Drei Verhaltenstests schützen Reihenfolge, Navigationszustand und Fehlerpfad; `PlayerWindow` sank auf 5.190 Zeilen.
- **Live-Erkennungsbestätigung ausgelagert (A1-01, Teilpaket):** `LiveDetectionConfirmationTrainingController` bündelt Annahme, Korrektur, Zeitbezug und Ergebnisbehandlung. Der Trainings-Schreiber wird einmal je Player-Fenster übergeben statt bei jedem Klick neu aufgebaut. Zwei Verhaltenstests schützen Mehrfachspeicherung, Korrekturauswahl, Frame-Zeit und Rückkehr zur Erkennung; `PlayerWindow` sank auf 5.140 Zeilen.
- **Manuelles Erkennungstraining ausgelagert (A1-01, Teilpaket):** `LiveDetectionManualMarkTrainingController` bündelt Katalogauswahl, Sitzungseintrag, Frame-Speicherung und Ergebnisbehandlung. Bestätigung und manuelle Markierung teilen sich nun denselben einmal erstellten Trainings-Schreiber. Zwei Verhaltenstests schützen wiederholtes Speichern und den nebenwirkungsfreien Abbruch; `PlayerWindow` sank auf 5.126 Zeilen.
- **Player-Trainingsaufbau ausgelagert (A1-01, Teilpaket):** `LiveDetectionTrainingControllerSetFactory` baut Bestätigungs- und Markierungscontroller samt gemeinsamem Schreiber außerhalb des Player-Konstruktors auf. Ein gemeinsamer Verhaltenstest schützt beide Wege und dieselbe Schreiberinstanz; `PlayerWindow` sank auf 5.105 Zeilen.
- **Codierungs-Bestätigung ausgelagert (A1-01, Teilpaket):** `CodingConfirmationDecisionController` bündelt Annehmen, Bearbeiten, Ablehnen, Panel-Abschluss und Wiedergabefortsetzung. Das Fenster enthält nur noch die drei Klick-Weiterleitungen. Drei Verhaltenstests schützen Entscheidungswert, Training, Löschen, Auswahl und Fortsetzen; `PlayerWindow` sank auf 5.072 Zeilen.
- **Schema-Overlay-Steuerung ausgelagert (A1-01, Teilpaket):** `CodingSchemaOverlayController` bündelt Erzeugen, Aktivieren, Platzieren, Ziehen, Aktualisieren, Leeren und Mausradsteuerung der Schema-Overlays. `PlayerWindow` enthält dafür nur noch kurze Ereignis-Weiterleitungen. Drei neue Verhaltenstests schützen Aufbau, Darstellungsreihenfolge, Winkeländerung und Aufräumen; der eingefrorene Umfang sank von 5.072 auf 4.982 Zeilen.
- **Kalibrierungs-Maussteuerung ausgelagert (A1-01, Teilpaket):** `CodingCalibrationPointerController` besitzt Startpunkt, Vorschau und Abschluss der manuellen Kalibrierung. Das Fenster leitet die drei Mausphasen nur noch weiter. Zwei neue Verhaltenstests schützen den nebenwirkungsfreien Ruhezustand sowie die vollständige Reihenfolge mit denselben Start-/Endpunkten; `PlayerWindow` sank von 4.982 auf 4.954 Zeilen.
- **Manuellen Kalibrierungsbefehl ausgelagert (A1-01, Teilpaket):** `CodingManualCalibrationController` bündelt Ein-/Ausschalten, Referenzberechnung und die sichere Anwendung auf Overlay und aktives Schema. Das Fenster enthält nur noch den Klick-Weiterleiter. Drei neue Verhaltenstests schützen fehlende Laufzeitvoraussetzungen, das Leeren alter Einblendungen und die vollständige DN-/Schema-Anwendung; `PlayerWindow` sank von 4.954 auf 4.924 Zeilen.
- **Ereignisliste und Statistik aktualisieren ausgelagert (A1-01, Teilpaket):** `CodingEventsRefreshController` bündelt Sortierung, Listenbindung, Statistik und die verzögerte Einfärbung. Das Fenster behält nur zwei kurze Aufrufe. Drei neue Verhaltenstests schützen fehlende Ereignisdaten, Sortier-/Anzeige-Reihenfolge und die getrennte Statistikaktualisierung; `PlayerWindow` sank von 4.924 auf 4.914 Zeilen.
- **Import-/KI-Spaltenwechsel ausgelagert (A1-01, Teilpaket):** `CodingImportReferenceDropController` entscheidet über Kopieren, Verschieben, fehlende Session und Session-Entfernung. Das Fenster aktualisiert nach erfolgreichem Wechsel nur noch Liste, Zähler und Abgleich. Drei neue Verhaltenstests schützen neue IDs, Originalerhalt, Abbruch ohne Session und sortiertes Verschieben; `PlayerWindow` sank von 4.914 auf 4.907 Zeilen.
- **Importbefund-Bestätigung ausgelagert (A1-01, Teilpaket):** `CodingImportReferenceConfirmationController` prüft Auswahl und VSA-Code, setzt die Annahme und steuert Speichern, Erfolgsmeldung und Neuabgleich in fester Reihenfolge. Das Fenster liefert nur noch Auswahl und Anzeigeaktionen. Drei neue Verhaltenstests schützen Leerlauf, fehlenden Code, Entscheidung und Speicherkette; `PlayerWindow` sank von 4.907 auf 4.905 Zeilen.
- **Player-Kindsuche zentralisiert (A8-02-Nachlauf/A1-01):** Die sechs benannten Suchen in den Coding-Listen verwenden jetzt `VisualTreeSafe.FindNamedDescendant`; die rekursive private Player-Kopie und ihre eigene Partial-Datei sind entfernt. Zwei neue Verhaltenstests schützen verschachtelte Treffer und sichere Leerfälle, Architekturtests verhindern die Rückkehr der Kopie. `PlayerWindow` sank von 97 auf 96 partielle Dateien und von 4.905 auf 4.885 Zeilen.
- **KI-Befundkontext ausgelagert (A1-01, Teilpaket):** Filterung, VSA-Codeauflösung, Katalogtext und Dublettenprüfung liegen im kleinen `CodingFindingContext`. Das Fenster hält nur eine Instanz und reicht deren vier Operationen an die bestehenden Arbeitsabläufe weiter; drei Verhaltenstests schützen aktuelle Event-/Importquellen, Codenormalisierung und Katalogauflösung. Das eigene Filter-Partial entfiel, `PlayerWindow` sank von 96 auf 95 Teile und von 4.885 auf 4.844 Zeilen.
- **KI-Analysekontext ausgelagert (A1-01, Teilpaket):** Rohrende-Prüfung, Befundnähe, segmentierte Befunde und Snapshot-Aufnahme liegen im kleinen `CodingAnalysisContext`. Aktuelle Ereignisse, Kalibrierung und Bildseitenverhältnis werden erst beim jeweiligen Aufruf gelesen. Drei Verhaltenstests schützen diesen Datenbezug, die Segmentierungsweitergabe und den Abbruch-Token. Das bisherige Helper-Partial entfiel, `PlayerWindow` sank von 95 auf 94 Teile und von 4.844 auf 4.796 Zeilen.
- **Trainingsspeicherung ausgelagert (A1-01, Teilpaket):** Einzelbefunde und Befundlisten werden über den kleinen `CodingTrainingPersistenceContext` an die vorhandene geschützte Trainingsspeicherung übergeben. Aktiver Codierkontext, Haltungsbezug, Inspektionsdatum, Benutzer, Zeitpunkt und bevorzugtes Analysebild werden beim jeweiligen Speichern aktuell gelesen. Drei Verhaltenstests schützen Einzel-, Leer- und Batchpfad. Das bisherige Persistence-Partial entfiel, `PlayerWindow` sank von 94 auf 93 Teile und von 4.796 auf 4.759 Zeilen.
- **Rohrgrenzen-Kontext ausgelagert (A1-01, Teilpaket):** Die Player-Anbindung für automatische Rohranfang- und Rohrende-Ereignisse liegt im kleinen `CodingBoundaryContext`. Ereignislisten, Session, Meterstände, Timeline-Fallback und analysiertes Bild werden bei jedem Aufruf aktuell übernommen; die vorhandenen Import-, Dubletten- und Foto-Workflows bleiben unverändert. Zwei Verhaltenstests schützen Start- und Endrequest. Das bisherige Boundary-Partial entfiel, `PlayerWindow` sank von 93 auf 92 Teile und von 4.759 auf 4.709 Zeilen.
- **KI-Zustandsüberwachung ausgelagert (A1-01, Teilpaket):** Initialisierung, Monitor-Ereignisse, Statusanwendung und Stoppen liegen im `CodingPipelineHealthController` hinter einem kleinen Vertrag. Das Fenster verdrahtet nur Laufzeitquellen, UI-Aktionen und Lebenszyklusaufrufe; die vorhandenen Health-Workflows sowie der zentrale KI-Laufzeitbesitz bleiben unverändert. Zwei Verhaltenstests schützen deaktivierte Initialisierung, UI-Fadenwechsel, erneute Schließen-Prüfung und Monitor-Stopp. Zwei Health-Partials entfielen, `PlayerWindow` sank von 92 auf 90 Teile und von 4.709 auf 4.654 Zeilen.
- **Live-KI-Statusanzeige ausgelagert (A1-01, Teilpaket):** Live-Erkennungsbadge, YOLO-Status, Coding-KI-Status und Befundzusammenfassung laufen über den `LiveDetectionStatusController` hinter einem kleinen Vertrag. UI-Fadenwechsel und Pulsentscheidung verwenden unverändert die vorhandenen Workflows; das Fenster verdrahtet nur seine Anzeigeelemente. Drei Verhaltenstests schützen verzögerte UI-Anwendung, Status-/Pulsreihenfolge und die unveränderte Ergebnisweitergabe. Das bisherige Status-Partial entfiel, `PlayerWindow` sank von 90 auf 89 Teile und von 4.654 auf 4.630 Zeilen.
- **Coding-KI-Pulssteuerung ausgelagert (A1-01, Teilpaket):** Start, Mehrfachstart-Schutz, Stop und Laufzustand der Pulsanimation liegen im `LiveDetectionPulseController` hinter einem kleinen Vertrag. Er verwendet unverändert den vorhandenen Zustandscontroller und die vorhandenen Start-/Stop-Workflows; das Fenster verdrahtet nur den Animationsring. Ein Verhaltenstest schützt Start, doppelten Start und Stop samt Zustandswechsel. Das bisherige Puls-Partial entfiel, `PlayerWindow` sank von 89 auf 88 Teile und von 4.630 auf 4.615 Zeilen.
- **Live-KI-Startlebenszyklus ausgelagert (A1-01, Teilpaket):** Klickentscheidung, Dialogstart und Verdrahtung der Erkennungslaufzeit liegen im `LiveDetectionLifecycleController` hinter einem kleinen Vertrag. Das Fenster enthält nur noch den synchronen Klick-Weiterleiter und verbindet seine Anzeigeaktionen zentral. Die vorhandenen Start-, Laufzeit-, VRAM- und QualityGate-Abläufe bleiben unverändert. Zwei Verhaltenstests schützen Stoppen sowie die vollständige Startreihenfolge. Das bisherige Start-Partial entfiel, `PlayerWindow` sank von 88 auf 87 Teile und von 4.615 auf 4.596 Zeilen.
- **Live-KI-Stoppsteuerung ausgelagert (A1-01, Teilpaket):** Laufzeitstopp, Statuswechsel, Overlay-Bereinigung, Wiedergabepause und verzögertes Ausblenden liegen im `LiveDetectionStopController` hinter einem kleinen Vertrag. Fenster, Coding-Modus und Schließen-Abläufe verwenden dieselbe Instanz; die vorhandenen Stop- und Timer-Workflows bleiben unverändert. Zwei Verhaltenstests schützen den Schließen-Fall ohne UI sowie die vollständige sichtbare Stopp-Reihenfolge. Das bisherige Stopp-Partial entfiel, `PlayerWindow` sank von 87 auf 86 Teile und von 4.596 auf 4.583 Zeilen.
- **Markierwerkzeug-Steuerung ausgelagert (A1-01, Teilpaket):** Öffnen, Aktivieren, Overlay-Bereitstellung und Deaktivieren der manuellen Punkt-, Ellipsen-, Freihand- und Rechteckwerkzeuge laufen über den `LiveDetectionMarkToolController` hinter einem kleinen Vertrag. Das Fenster behält nur die Klick-Weiterleitungen und die zentrale Verdrahtung; die vorhandenen Aktivierungs-, Overlay- und Aufräumabläufe bleiben unverändert. Zwei Verhaltenstests schützen die vollständige Aktivierungs- und Aufräumreihenfolge. Das bisherige Markierwerkzeug-Partial entfiel, `PlayerWindow` sank von 86 auf 85 Teile und von 4.583 auf 4.558 Zeilen.
- **Manuelle SAM-Segmentierung ausgelagert (A1-01, Teilpaket):** Box-Zuordnung, aktuelle Rohrkalibrierung, SAM-Aufruf, Quantifizierungsübernahme und die Entscheidung zwischen Maske und Bogenmarker laufen über den `LiveDetectionMarkSegmentationController` hinter einem kleinen Vertrag. Das Fenster reicht Markierung und Bild nur noch weiter und verdrahtet die Anzeigeaktionen; die vorhandenen Segmentierungs- und Renderabläufe bleiben unverändert. Zwei Verhaltenstests schützen Box, DN, Quantifizierung und Bogenmarker. Das bisherige Segmentierungs-Partial entfiel, `PlayerWindow` sank von 85 auf 84 Teile und von 4.558 auf 4.515 Zeilen.
- **Codiermodus-Ausstieg ausgelagert (A1-01, Teilpaket):** Abschlussprüfung, Rückkehr bei offenen Streckenschäden und die vollständige Aufräumreihenfolge laufen über den `CodingModeExitController` hinter einem kleinen Vertrag. Das Fenster verdrahtet nur aktuelle Sitzung, KI-Zustand und Anzeigeaktionen; Klick und fehlgeschlagener Sitzungsstart rufen dieselbe Instanz auf. Zwei Verhaltenstests schützen die erfolgreiche Reihenfolge und die sichere Rückkehr in den Codiermodus bei blockiertem Abschluss. Das bisherige Exit-Partial entfiel, `PlayerWindow` sank von 84 auf 83 Teile und von 4.515 auf 4.498 Zeilen.
- **Codierungsübernahme und Schließschutz ausgelagert (A1-01, Teilpaket):** Übernehmen, Projekt-Markierung, sicheres Speichern und die Rückfrage bei ungespeicherten Codierungen laufen über den `CodingApplyController` hinter einem kleinen Vertrag. Das Fenster verdrahtet nur Sitzung, Haltung und Anzeigeaktionen; Schließen und Protokollvorschau verwenden dieselbe Instanz. Drei Verhaltenstests schützen die bisherige Reihenfolge, die Übernahme ohne Einblendung beim Schließen und die aktuelle Haltung. Das bisherige Apply-Partial entfiel, `PlayerWindow` sank von 83 auf 82 Teile und von 4.498 auf 4.462 Zeilen.
- **Druckcenter entkoppelt (A1-05, Teilpaket):** Das ViewModel erhält Einstellungen, Dialoge, PDF-Ausgabe und Kostenabgleich gezielt. Es speichert den zentralen Container nicht mehr. Zwölf fokussierte Tests schützen Hintergrund-Aktualisierung, Filter, Auswahl und Leistungsverzeichnis-Aufbau.
- **Push-Schutz repariert:** Der pre-push-Hook prüft jetzt Infrastruktur-, Pipeline- und UI-Tests. Ein roter UI- oder Wartbarkeitstest blockiert damit den Push.
- **Gesamtprüfung grün:** 9.295 Tests bestanden (2.735 Infrastruktur, 1.856 Pipeline, 4.642 UI und 62 ProjectModernizer). Zwei maschinengebundene Tests wurden planmäßig übersprungen. Der vollständige Release-Build endet mit 0 Fehlern und 0 Warnungen. Zusätzlich sind 5 QGIS-Python-Smokes und 97 GPU-freie Sidecar-Tests grün.

- **Pipeline-Ablaufdateien instanzbasiert (A1-10, Teilpaket):** JSONL-Ausgabe, atomare Laufzusammenfassung und Schreibsperre liegen im `PipelineTraceFileWriter` hinter `IPipelineTraceWriter`. Der zentrale `ServiceProvider` reicht genau eine Instanz ueber Fabrik und beide Analysewege weiter; die bisherige statische API und der oeffentliche Frame-Typ bleiben kompatibel. Sieben Datei-/Fassadentests, ein echter Verbrauchertest und ein Verdrahtungstest schuetzen Dateiformat, paralleles Schreiben, Pfade, Nullverhalten, Fehlerisolation sowie die vollstaendige Feldweitergabe.
- **Video-Startfehlerbericht instanzbasiert (A1-10, Teilpaket):** Die Datenseite erhaelt den zentralen `IVideoStartErrorLogWriter` und schreibt keine Fehlerdatei mehr ueber eine statische UI-Klasse. Dateiname, Inhalt und Best-Effort-Verhalten bleiben durch drei Fassadentests, einen direkten Dateitest und einen Verdrahtungstest geschuetzt; die bisherige oeffentliche API bleibt als kleine Kompatibilitaetsfassade erhalten.
- **Wissensdatenbank-WAL-Pruefschritt instanzbasiert (A1-10, Teilpaket):** Die Vollsicherung erhaelt den zentralen `IKnowledgeWalCheckpoint` statt der statischen Datenbankfunktion. Ein echter SQLite-Test und drei isolierte Diensttests schuetzen Lesbarkeit, festen Datenbankpfad, fehlende Dateien und Fehlerisolation; der bestehende Sicherungstest belegt weiterhin genau einen Aufruf. Die oeffentliche statische API bleibt als Kompatibilitaetsfassade erhalten.
- **Wissensdatenbank-Gesundheitspruefung instanzbasiert (A1-10, Teilpaket):** Existenzpruefung und schreibgeschuetztes SQLite `quick_check` liegen im `KnowledgeBaseHealthInspectionService` hinter `IKnowledgeBaseHealthInspector`. Der Programmstart erhaelt die zentral aufgebaute Instanz und verwendet ihr kombiniertes Ergebnis vor jeder normalen Datenbankoeffnung; die bisherige statische API samt Ergebnis-Typ bleibt kompatibel. Drei Fassadenfaelle, ein direkter SQLite-Test und ein echter Injektionstest schuetzen gueltige, beschaedigte und fehlende Datenbanken sowie die klare Startwarnung.
- **Programm-Einstellungsspeicher instanzbasiert (A1-10, Teilpaket):** Atomare Ausgabe, Sicherungskopie, Schreibschutzbehandlung und Wiederholungen liegen im `SettingsFileStore` hinter `ISettingsFileStore`. `AppSettings` verwendet dieselbe zentral aufgebaute Instanz fuer sofortiges und verzoegertes Speichern; die statische API bleibt nur als Kompatibilitaetsfassade. Neun bestehende Fassadentests, zwei direkte Dateitests, zwei echte Verbrauchertests und ein Verdrahtungstest schuetzen den sicherheitskritischen Pfad.
- **Settings-Rettungspunkte instanzbasiert (A1-10, Teilpaket):** Kopie, sichere Ordnernamen und 20-Dateien-Begrenzung liegen im `SettingsRestorePointStore` hinter `ISettingsRestorePointStore`. Der zentrale `ServiceProvider` reicht dieselbe Instanz an den Einstellungsspeicher weiter; die bisherige statische API bleibt als duenne Kompatibilitaetsfassade erhalten. Ein Verhaltenstest, zwei direkte Infrastrukturtests und ein Architekturtest schuetzen Dateikopie, zentrale Verdrahtung und acht parallele, eindeutige Rettungspunkte.
- **Windows-Explorer-Start instanzbasiert (A1-10, Teilpaket):** Pfadpruefung und Prozessstart liegen im `ExplorerRevealLauncher` hinter `IExplorerRevealService`. Haltungs- und Schachtansicht verwenden dieselbe zentral aufgebaute Instanz; die bisherige statische API bleibt als duenne Kompatibilitaetsfassade erhalten. Drei direkte Diensttests, drei Fassadentests und ein Architekturtest schuetzen Datei-/Ordnerauswahl, fehlende Ziele, Startfehler und die Verdrahtung ohne einen echten Explorer im Test zu oeffnen.
- **Sicheres Öffnen instanzbasiert (A1-10, Teilpaket):** Pfadprüfung, erlaubte Dateitypen und Windows-Prozessstart liegen im `SafeShellOpenService` hinter `ISafeShellOpenService`. Alle bestehenden Aufrufer verwenden über die kompatible statische API dieselbe zentral aufgebaute Instanz; die UI-Fassade enthält keine Datei- oder Prozessoperation mehr. Zwei bestehende Fassadentests, ein direkter Diensttest und eine Verdrahtungsprüfung schützen fehlende Dateien und das Blockieren ausführbarer Dateitypen.
- **Einstellungs- und Diagnoseordner instanzbasiert geöffnet (A1-10, Teilpaket):** Ordneranlage und anschliessendes sicheres Öffnen liegen im `FolderOpenService` hinter `IFolderOpenService`. Einstellungs- und Diagnoseseite erhalten dieselbe zentral aufgebaute Instanz; `SettingsPathWorkflow` enthält keine produktive Ordner- oder Prozessoperation mehr. Zwei direkte Diensttests, die bestehenden sechs Ablaufprüfungen, zwei echte ViewModel-Injektionstests und eine Verdrahtungsprüfung schützen Reihenfolge, Fehleranzeige und zentrale Nutzung.
- **Settings-Quarantaene instanzbasiert (A1-10, Teilpaket):** Verschieben, Copy-Fallback und Ordneranlage fuer eine nicht lesbare `settings.json` liegen im `SettingsQuarantineStore` hinter `ISettingsQuarantineStore`. Der Programmstart reicht dieselbe Instanz vom Laden bis zum zentralen `ServiceProvider` weiter; `AppSettings.Load()` und die bisherige statische API bleiben kompatibel. Sieben bestehende Fassadentests, drei direkte Dateitests und ein Architekturtest schuetzen Dateiname, Inhalt, fehlende Dateien, Fehlerisolation und Verdrahtung.
- **Legacy-Settings-Migration instanzbasiert (A1-10, Teilpaket):** Die einmalige Kopie aus dem alten Programmordner liegt im `SettingsMigrationService` hinter `ISettingsMigrationService`. Der Programmstart verwendet bis zum zentralen `ServiceProvider` dieselbe Instanz; die bisherige statische API bleibt kompatibel. Kopierfehler blockieren den Start weiterhin nicht, werden aber nicht mehr verschluckt, sondern im Tageslog erfasst. Vier bestehende Fassadentests, drei direkte Dateitests und ein Architekturtest schuetzen Kopie, Ueberschreibschutz, Leerfall, Fehlerergebnis und Verdrahtung.
- **Projektordner-Anlage instanzbasiert (A1-10, Teilpaket):** Die idempotente Anlage der verbindlichen Projektstruktur liegt im `ProjectStructureInitializer` hinter `IProjectStructureInitializer`. Shell und Ein-Knopf-Import verwenden dieselbe zentral aufgebaute Instanz; Konstanten, reine Pfadhelfer und die bisherige statische API bleiben kompatibel. Neun bestehende Strukturtests, ein Paralleltest, ein echter Verbrauchertest und zwei erweiterte Architekturregeln schuetzen Ordnerumfang, Mehrfachaufrufe und Verdrahtung.
- **Kanalexport-Erkennung instanzbasiert (A1-10, Teilpaket):** Die rekursive Erkennung von WinCan-, IKAS-, IBAK- und KINS-Quellen liegt im `KanalExportDetectionService` hinter `IKanalExportDetectionService`. Der Ein-Knopf-Import erhaelt die zentral aufgebaute Instanz; die bisherige statische API und alle Ergebnis-Typen bleiben kompatibel. Vierzehn bestehende Verhaltenstests, zwei neue Dienst-/Verbrauchertests und eine Architekturregel schuetzen Formaterkennung, Fehlerfaelle und Verdrahtung.
- **KIAS-/IBAK-Ordnererkennung instanzbasiert (A1-10, Teilpaket):** Die Pruefung von `Arizona.fdb`, `Film/Daten.txt`, Report-PDFs sowie Gegen- und Wiederholungsaufnahmen liegt im `KiasExportPatternDetectionService` hinter `IKiasExportPatternDetector`. Die allgemeine Kanalexport-Erkennung erhaelt dieselbe zentral aufgebaute Instanz; die bisherige statische API samt verschachteltem Ergebnis-Typ bleibt kompatibel. Die reinen KIAS-Dateinamensregeln bleiben statisch. Zwei echte Ordnertests, ein Injektionstest und eine Verdrahtungspruefung schuetzen Merkmale, Zaehler, Meldungstext und zentrale Nutzung.
- **IBAK-Firebird-Zugangsdaten instanzbasiert (A1-10, Teilpaket):** Lesen von `IBAK_FDB_USER` und `IBAK_FDB_PASSWORD` sowie der Aufbau der Embedded- und Foto-Verbindungen liegen im zentralen `IbakFdbConnectionOptionsService` hinter `IIbakFdbConnectionOptions`. Der IBAK-Import erhält die Instanz direkt; statische Topologie-Aufrufer verwenden dieselbe Instanz über die kompatible Fassade. Die reine Serverpfadregel bleibt statisch. Elf bestehende und ein isolierter Zugangstest sowie eine Verdrahtungsprüfung schützen lokale Standardwerte, Server-Fail-Closed, Passworttreue und zentrale Nutzung.
- **KINS-Textanreicherung instanzbasiert (A1-10, Teilpaket):** Lesen und Auswerten von `kiDVDaten.txt` sowie das empty-only Setzen von Video-Timecodes, inspizierter Laenge und Aufnahmedatum liegen im `KinsDvdTextEnrichmentService` hinter `IKinsDvdTextEnricher`. Der Ein-Knopf-Import erhaelt die zentral aufgebaute Instanz; die bisherige statische API und ihr Ergebnis-Typ bleiben kompatibel. Neun direkte Verhaltensfaelle, ein echter Import-Injektionstest und eine Verdrahtungspruefung schuetzen Meter-Zuordnung, Prioritaeten, fehlende Dateien, Zaehler und zentrale Nutzung.
- **KINS-DBF-Anreicherung instanzbasiert (A1-10, Teilpaket):** Rekursive Suche und Auswertung von `haltung.DBF` und `schacht.DBF` sowie die empty-only Anreicherung liegen im `KinsDbfWhitelistEnrichmentService` hinter `IKinsDbfWhitelistEnricher`. Der Ein-Knopf-Import erhaelt die zentral aufgebaute Instanz; die bisherige statische API und ihr Ergebnis-Typ bleiben kompatibel. Neun direkte Verhaltensfaelle, ein echter Import-Injektionstest und eine Verdrahtungspruefung schuetzen Schachtanlage, Prioritaeten, fehlende Ordner, Zaehler und zentrale Nutzung.
- **KINS-Gesamtprotokollsuche instanzbasiert (A1-10, Teilpaket):** Die rekursive PDF-Suche, der Deckblatt-Ausschluss und die Auswahl nach Dateigroesse liegen im `KinsGesamtprotokollFileLocator` hinter `IKinsGesamtprotokollLocator`. Der Ein-Knopf-Import erhaelt die zentral aufgebaute Instanz; die bisherige statische API bleibt kompatibel. Vier echte Ordnerfaelle, ein Import-Injektionstest und eine Verdrahtungspruefung schuetzen Auswahl, Gleichstand, Leerfall und zentrale Nutzung.
- **VSA/XTF-Medienpfadsuche instanzbasiert (A1-10, Teilpaket):** Die Suche nach Foto- und Videodateien relativ zur XTF und bis zu drei Exportebenen liegt im `VsaMediaPathFileResolver` hinter `IVsaMediaPathResolver`. Der XTF-Import erhaelt die zentral aufgebaute Instanz; die bisherige interne statische API bleibt als duenne Kompatibilitaetsfassade erhalten. Sechs direkte Pfadtests, ein echter VSA-Importfall und eine Verdrahtungspruefung schuetzen Suchreihenfolge, Leerfall und zentrale Nutzung.
- **PDF-Textextraktion instanzbasiert (A1-10, Teilpaket):** Suche und Start von `pdftotext`, Temp-Datei-Ausgabe, PdfPig-Rueckfall sowie Seiten- und Textmengenbegrenzung liegen im `PdfTextExtractionService` hinter `IPdfTextExtractor`. Der PDF-Import erhaelt die zentral aufgebaute Instanz; die bisherige oeffentliche statische API und der Ergebnis-Typ bleiben kompatibel. Vier bestehende Sicherheitspruefungen, ein echter PDF-Rueckfalltest und eine Verdrahtungspruefung schuetzen Budgets, Prozessaufruf und zentrale Nutzung.
- **PDF-Dokumenttyp-Textpraefix instanzbasiert (A1-10, Teilpaket):** PdfPig-Zugriff, begrenztes Lesen der ersten PDF-Seiten und der bestehende Textdatei-Rueckfall liegen im `PdfTextPrefixReaderService` hinter `IPdfTextPrefixReader`. Die zentrale Dokumenttyp-Fassade behaelt ihre oeffentlichen Signaturen und enthaelt keine Datei- oder PdfPig-Arbeit mehr; die reinen Regeln fuer TV-Protokoll, Dichtheitspruefung, Plan und Deckblatt bleiben bewusst statisch. Sechs bestehende Textregeln, ein echter mehrseitiger PDF-Fall, ein direkter Dienstrundlauf und eine Verdrahtungspruefung schuetzen Seitenbegrenzung, Rueckfall und zentrale Nutzung.
- **PDF-OCR-Rueckfall instanzbasiert (A1-10, Teilpaket):** Suche und Start von `pdftoppm` und Tesseract, Seitenbild-Erzeugung, Temp-Bereinigung sowie der begrenzte Ganzdokument-Lauf liegen im `PdfOcrExtractionService` hinter `IPdfOcrExtractor`. Der PDF-Import erhaelt dieselbe zentral aufgebaute Instanz wie alle kompatiblen OCR-Aufrufer; die statische Fassade enthaelt keine Datei- oder Prozessarbeit mehr. Vier OCR-Verhaltensfaelle, die bestehende Prozess-Sicherheitspruefung und eine Verdrahtungspruefung schuetzen Meldungen, Import-Injektion und zentrale Nutzung.
- **Schacht-Ganzdokument-OCR instanzbasiert (A1-10, Teilpaket):** Die eigene 40-Seiten-Schranke, PdfPig-Seitenzaehlung und das seitenweise Zusammenfuehren der OCR-Ausgabe liegen im `SchachtProtocolOcrReaderService` hinter `ISchachtProtocolOcrReader`. Der Schachtprotokoll-Import erhaelt Textauslese und OCR zentral; der doppelte statische Ganzdokument-Helfer ist entfernt. Ein echter Grenzfall, ein Teilseitenfall, der bestehende OCR-Rueckfall, ein Injektionstest und eine Verdrahtungspruefung schuetzen Meldungen, Seitenzahlen und zentrale Nutzung.
- **PDF-Formularfelder instanzbasiert (A1-10, Teilpaket):** Dateipruefung, PdfPig-Formularzugriff und die seitenweise Feldwertauslese liegen im `PdfFormFieldReaderService` hinter `IPdfFormFieldReader`. Die Schachtverteilung verwendet ueber ihre kompatible Fassade die zentral aufgebaute Instanz; die reinen Regeln fuer Datum und Schachtnummer bleiben statisch. Ein selbst erzeugtes interaktives PDF, zwei Fehlerfaelle, fuenf Parserregeln und eine Verdrahtungspruefung schuetzen Feldnamen, Werte, Leerfall und zentrale Nutzung.
- **PDF-Dateigroessenpruefung instanzbasiert (A1-10, Teilpaket):** Existenz, Dateilaenge und die optionale Umgebungsgrenze liegen im `PdfFileSafetyService` hinter `IPdfFileSafetyChecker`. Textauslese und OCR erhalten dieselbe zentral aufgebaute Instanz; die statische Kompatibilitaetsfassade enthaelt keinen `FileInfo`-Zugriff mehr. Reine Seiten- und Grenzwertberechnungen bleiben bewusst statisch. Sechs bestehende Regeltests, ein direkter Diensttest und eine Verdrahtungspruefung schuetzen Meldungen, Standardgrenze und zentrale Nutzung.
- **Atomarer PDF-Austausch instanzbasiert (A1-10, Teilpaket):** Staging im Zielordner, PdfPig-Pruefung, Sicherungskopie, `File.Replace`-/Move-Rueckfall und Wiederherstellung liegen im `AtomicPdfFileReplacementService` hinter `IAtomicPdfFileReplacer`. Die zentrale Instanz nutzt dieselbe PDF-Dateigroessenpruefung; die bisherige interne statische API bleibt als kleine Fassade ohne Datei- oder PdfPig-Zugriff erhalten. Zwei echte Austauschtests, der bestehende Fehler-Rollback und eine Verdrahtungspruefung schuetzen Originalerhalt, `.bak`-Inhalt und zentrale Nutzung.
- **Schacht-PDF-Auswahlerweiterung instanzbasiert (A1-10, Teilpaket):** Die Ordnersuche nach zusammengehoerigen Schachtprotokoll- und Schachtfoto-PDFs liegt im `ShaftPdfSelectionExpansionService` hinter `IShaftPdfSelectionExpander`. Die bestehende statische API bleibt als kleine Kompatibilitaetsfassade ohne Ordnerzugriff erhalten und verwendet die zentral aufgebaute Instanz. Drei bestehende Auswahlfaelle, ein direkter Diensttest und eine Verdrahtungspruefung schuetzen Paarbildung, Gross-/Kleinschreibung, normale Einzeldateien und zentrale Nutzung.
- **Verteil-Dateiuebertragung instanzbasiert (A1-10, Teilpaket):** Die Suche nach einem freien Kollisionsnamen sowie Kopieren und Verschieben von Dateien liegen im `DistributionFileTransferService` hinter `IDistributionFileTransfer`. Alle bestehenden Verteiler-Aufrufer behalten ihre Signaturen und verwenden die statische Kompatibilitaetsfassade ohne eigenen Dateizugriff; der zentrale `ServiceProvider` stellt ihre Instanz bereit. Drei Fassadenfaelle, ein direkter Dienstrundlauf und eine Verdrahtungspruefung schuetzen Nummerierung, Quellerhalt beim Kopieren, Quellentfernung beim Verschieben und zentrale Nutzung.
- **Video-Konfliktkandidaten instanzbasiert (A1-10, Teilpaket):** Stabile Kandidatennamen und das kollisionsgeschuetzte Kopieren mehrdeutiger Videos liegen im `VideoConflictCandidateCopyService` hinter `IVideoConflictCandidateCopier`. Der Dienst verwendet die zentrale Verteil-Dateiuebertragung; die bestehende statische API bleibt als Fassade ohne Dateizugriff erhalten. Die reinen Texte fuer fehlende und mehrdeutige Videos bleiben bewusst statisch. Zwei echte Kopierfaelle, ein Injektionstest und eine Verdrahtungspruefung schuetzen Dateiendungen, Reihenfolge, vorhandene Kandidaten und zentrale Nutzung.
- **Schacht-Excelvorlagenleser instanzbasiert (A1-10, Teilpaket):** Das Auffinden und Auslesen der Schachtspalten aus `Schaechte.xlsx` liegt im `SchaechteTemplateColumnFileReader` hinter `ISchaechteTemplateColumnReader`. Die Schachtseite erhaelt die zentral aufgebaute Instanz; die bisherige statische API bleibt kompatibel. Fuenf bestehende Verhaltenstests, ein neuer direkter Diensttest und zwei Architekturregeln schuetzen Vorlagenwahl, Spaltenreihenfolge, Leerfaelle und Verdrahtung.
- **Git-Commit-Erkennung der Vollsicherung instanzbasiert (A1-10, Teilpaket):** Das Lesen von `.git/HEAD`, losen Referenzen und `packed-refs` liegt im `GitCommitFileResolver` hinter `IGitCommitResolver`. Die Vollsicherung erhaelt die zentral aufgebaute Instanz und schreibt deren Ergebnis ins Manifest; die bisherige statische API bleibt kompatibel. Vier bestehende Verhaltenstests, ein direkter Diensttest, ein echter Manifest-Verbrauchertest und eine Verdrahtungspruefung schuetzen Detached-HEAD, Referenzen, Fehlerfaelle und den Sicherungsnachweis.
- **Zielmarker-Schutz der Vollsicherung instanzbasiert (A1-10, Teilpaket):** Pruefung auf Fremddaten, Anlage des Sicherungsordners und Schreiben des versteckten Schutzmarkers liegen im `BackupTargetMarkerGuardService` hinter `IBackupTargetMarkerGuard`. Die Vollsicherung erhaelt die zentrale Instanz direkt; die bisherige statische API bleibt als Fassade ohne Dateioperation erhalten. Die reinen Regeln gegen Ziel-in-Quelle und Loeschen ausserhalb des Sicherungsroots bleiben bewusst statisch. Acht bestehende Sicherheitsfaelle, ein direkter Diensttest, ein echter Injektionstest und eine Verdrahtungspruefung schuetzen Fremddaten, Marker und zentrale Nutzung.
- **SQLite-Schnappschuesse der Vollsicherung instanzbasiert (A1-10, Teilpaket):** SQLite-Erkennung, WAL-/SHM-Zuordnung, konservative Groessenberechnung, Online-Sicherung und `integrity_check` liegen im `SqliteSnapshotCopyService` hinter `ISqliteSnapshotCopier`. Vollsicherung und Ordnerspiegel erhalten dieselbe zentrale Instanz; die bisherige interne statische API bleibt als kleine Fassade ohne Datei- oder Datenbankzugriff erhalten. 16 bestehende Spiegeltests, ein direkter Datenbanktest, ein echter Injektionstest und eine Verdrahtungspruefung schuetzen laufende WAL-Datenbanken, Wiederholung, Integritaet und zentrale Nutzung.
- **Projektdateisuche instanzbasiert (A1-10, Teilpaket):** Die Suche nach alten und neuen Projektablagen liegt im `ProjectFileDiscoveryService` hinter `IProjectFileDiscovery`. Die Übersicht erhält die zentral aufgebaute Instanz und ruft keine statische Dateioperation mehr direkt auf; nicht lesbare Basisordner werden im Tageslog gemeldet. Die bisherige öffentliche statische API bleibt als kleine Kompatibilitätsfassade erhalten. Sieben bestehende Fassadentests, ein direkter Dateitest und zwei Verdrahtungs-/Verhaltenstests schützen Suchumfang, Duplikatregel und echte Injektion.
- **Projekt-Drop-Pfadsuche instanzbasiert (A1-10, Teilpaket):** Die Erkennung einer per Drag-and-drop übergebenen Projektdatei oder eines Projektordners liegt im `ProjectDropFilePathResolver` hinter `IProjectDropPathResolver`. Die Übersicht erhält die zentral aufgebaute Instanz; die bisherige statische API bleibt als kleine Kompatibilitätsfassade ohne Dateioperation erhalten. Vier bestehende Verhaltenstests, ein direkter Dateitest, zwei Verdrahtungsprüfungen und ein echter Injektionstest schützen neue sowie alte Projektablagen.
- **Projektfoto-Referenzen instanzbasiert (A1-10, Teilpaket):** Die Reparatur veralteter Fotoverknuepfungen und die eindeutige Suffix-Suche im zentralen Haltungsfoto-Ordner liegen im `ProjectPhotoReferenceNormalizationService` hinter `IProjectPhotoReferenceNormalizer`. Die Projektablage erhaelt die zentral aufgebaute Instanz und verwendet sie beim Laden sowie vor dem Speichern; ihr parameterloser Konstruktor bleibt kompatibel. Fuenf bestehende Projektdatei-Faelle, ein direkter Diensttest, ein Injektionstest und eine Verdrahtungspruefung schuetzen Original-/Aktuell-Revisionen, VSA-Funde, Umbenennungen, Duplikate und das bewusste Nicht-Raten bei mehreren Treffern.
- **KI-Frame-Fotos instanzbasiert (A1-10, Teilpaket):** Das Prüfen, Benennen und Schreiben analysierter Videoframes liegt im `CodingFramePhotoFileStore` hinter `ICodingFramePhotoStore`. Der Player erhält die zentral aufgebaute Instanz und ruft keine statische Dateioperation mehr direkt auf; die bisherige öffentliche statische API bleibt als Kompatibilitätsfassade erhalten. Bestehende Foto-Tests, ein Kollisionsfall, ein direkter Diensttest und eine Verdrahtungsprüfung schützen Inhalt, Fotoverknüpfung und zentrale Nutzung.
- **Markierte KI-Bildvorschau instanzbasiert (A1-10, Teilpaket):** Auswahl des Befundfotos, Temp-Ordneranlage und Schreiben des markierten Vorschaubilds liegen im `CodingDefectPreviewRenderer` hinter `ICodingDefectPreviewRenderer`. Der zentrale `ServiceProvider` verbindet die bestehende statische API mit genau einer Instanz. Drei bestehende Bildtests und eine Verdrahtungsprüfung schützen Markierung, Rückfall auf das Originalbild und zentrale Nutzung.
- **Dossier-Fotoverfügbarkeit instanzbasiert (A1-10, Teilpaket):** Das Prüfen vorhandener Protokollfotos liegt im `DossierPhotoFileAvailabilityService` hinter `IDossierPhotoAvailabilityService`; die reine Pfadauflösung bleibt statisch. Datenseite und Druckcenter erhalten dieselbe zentrale Instanz, während die bisherige öffentliche statische API kompatibel bleibt. 18 bestehende Fassadentests, ein direkter Dateitest, ein injizierter Druckablauf und zwei Verdrahtungsprüfungen schützen Pfadauflösung, gelöschte Einträge und die zentrale Nutzung.
- **Schacht-Dateizielsuche instanzbasiert (A1-10, Teilpaket):** Die Prüfung und Auflösung verknüpfter Schacht-PDFs sowie Ordner liegt im `SchachtFileTargetPathResolver` hinter `ISchachtFileTargetResolver`. Schacht-ViewModel und -Ansicht verwenden die zentral aufgebaute Instanz; die bisherige statische API bleibt als kleine Kompatibilitätsfassade ohne Dateioperation erhalten. Fünf bestehende Verhaltenstests, ein direkter Dateitest, zwei Verdrahtungsprüfungen und ein Injektionstest schützen relative Projektpfade, PDF-Vorrang und Ordnerziele.
- **Inspektionsprotokoll-Suche instanzbasiert (A1-10, Teilpaket):** Die vollständige Datei- und PDF-Suche liegt im `InspectionProtocolFileLocator` hinter `IInspectionProtocolFileLocator`. Datenseite, Dossierdruck und Karte erhalten dieselbe zentrale Instanz; die 515-zeilige UI-Klasse ist auf eine 60-zeilige Kompatibilitätsfassade ohne Datei- oder PdfPig-Zugriff geschrumpft. Die reinen Token- und Kandidatenregeln bleiben statisch in Application. 22 Fassadentests, ein direkter Diensttest, ein injizierter Dossierablauf und fünf Verdrahtungsprüfungen schützen alte und neue Projektpfade, Schacht-PDFs sowie die Unterscheidung zwischen Inspektionsprotokoll und Lageplan.
- **Dichtheitsprotokoll-Suche instanzbasiert (A1-10, Teilpaket):** Die rekursive Suche nach verteilten DP-PDFs liegt im `DichtheitProtocolFileLocator` hinter `IDichtheitProtocolFileLocator`. Der neue `DataPageDichtheitPdfController` kapselt Öffnen und Nutzermeldungen; die Datenseite delegiert nur noch und sank auf 987 Zeilen. Die bisherige statische API bleibt als kleine Kompatibilitätsfassade ohne Ordnersuche erhalten. Fünf bestehende Fassadentests, ein direkter Dateitest, drei Controllerfälle und drei Verdrahtungsprüfungen schützen externe Zielwurzeln, optionale Überordner, Sortierung, Meldungen und echte Injektion.
- **Kataster-XTF-Suche instanzbasiert (A1-10, Teilpaket):** Bevorzugte Dateinamen, Ordnerprüfung und die Auswahl der grössten XTF-Datei liegen im `KatasterXtfFilePathResolver` hinter `IKatasterXtfPathResolver`. Karte, Kartenvorladen, QGIS-Brücke, Export und Einstellungen verwenden dieselbe zentral aufgebaute Instanz; die bisherige statische API bleibt als kleine Kompatibilitätsfassade ohne Dateioperation erhalten. Die bestehenden Verhaltenstests, ein direkter Dateitest und fünf Verdrahtungsprüfungen schützen Auswahlregeln und echte Injektion.
- **Offline-Kartenordnersuche instanzbasiert (A1-10, Teilpaket):** Die Prüfung der `satellit`-/`av`-Ordner und die Korrektur des alten `uri`-Pfads liegen im `OfflineBasemapDirectoryResolver` hinter `IOfflineBasemapPathResolver`. Die Kartenansicht verwendet die zentral aufgebaute Instanz; die bisherige statische API bleibt als kleine Kompatibilitätsfassade ohne Ordnerzugriff erhalten. Vier bestehende Verhaltenstests, ein direkter Ordnertest und zwei Verdrahtungsprüfungen schützen die Rückwärtskompatibilität.
- **Offline-Kartenlayer instanzbasiert (A1-10, Teilpaket):** Ordnerprüfung und Erzeugung der Satellit-, AV- und OpenStreetMap-Layer liegen im `KarteBasemapLayerService` hinter `IKarteBasemapLayerFactory`. Die Kartenansicht erhält die zentral aufgebaute Instanz und ruft keine statische IO-Fabrik mehr auf; die bisherige öffentliche API bleibt als kleine Kompatibilitätsfassade erhalten. Ein direkter Ordnertest, ein echter ViewModel-Injektionstest und zwei Verdrahtungsprüfungen schützen Layernamen, fehlende Ordner und zentrale Nutzung.
- **VSA-Katalogpfadsuche instanzbasiert (A1-10, Teilpaket):** Die Prüfung konfigurierter Dateien, Umgebungsordner, WinCan-Ablagen und des KEK-Manifests liegt im `VsaCatalogFilePathResolver` hinter `IVsaCatalogPathResolver`. Der zentrale `ServiceProvider` verwendet die Instanz beim Aufbau des Codekatalogs; die bisherige statische UI-API bleibt als kleine Kompatibilitätsfassade ohne Dateioperation erhalten. Vier bestehende Fassadentests, ein direkter Dateitest und eine Verdrahtungsprüfung schützen Suchreihenfolge, Quellenliste und zentrale Nutzung.
- **Programmordner-Suche instanzbasiert (A1-10, Teilpaket):** Die Suche nach `AuswertungPro.sln` über App- und Arbeitsordner liegt im `ProgramRootFileLocator` hinter `IProgramRootLocator`. Beide Bereinigungen erhalten dieselbe zentral aufgebaute Instanz; die bisherigen statischen Fabriksignaturen bleiben kompatibel und enthalten keine eigene Ordnersuche mehr. Zwei direkte Dateitests, ein Injektionstest und zwei Verdrahtungsprüfungen schützen Suchreihenfolge, Rückfall und zentrale Nutzung.
- **Repo-Wurzelsuche der Vollsicherung instanzbasiert (A1-10, Teilpaket):** Die optionale Suche nach `AuswertungPro.sln` liegt im `RepositoryRootFileLocator` hinter `IRepositoryRootLocator`. Die Vollsicherung erhält die zentral aufgebaute Instanz; fehlt der Marker, bleibt die Quellcode-Komponente weiterhin leer statt einen falschen Ordner zu sichern. Die öffentliche statische API bleibt als Kompatibilitätsfassade erhalten. Drei bestehende Fassadentests, ein direkter Dateitest, ein echter Verbrauchertest und zwei Verdrahtungsprüfungen schützen Datei-/Ordnerstart, Leerfall und zentrale Nutzung.
- **Quellliste der Vollsicherung instanzbasiert (A1-10, Teilpaket):** Repo-, Wissens-, Programm-, Benutzer- und Projektpfade sowie der gefilterte `SEWER*`-Umgebungsschnappschuss werden im zentralen `FullBackupSourcesProvider` hinter `IFullBackupSourcesProvider` gesammelt. Die Vollsicherung erhält diese Instanz direkt; die öffentliche statische API bleibt als Kompatibilitätsfassade erhalten. Zwei isolierte Quelllisten-Tests und eine Verdrahtungsprüfung schützen Repo-Startpfad, Systemordner, Umgebungsfilter und Video-Option.
- **ffmpeg-Pfadprüfung des Selbsttrainings instanzbasiert (A1-10, Teilpaket):** Die Prüfung eines konfigurierten ffmpeg-Pfads liegt im `TrainingFfmpegFilePathResolver` hinter `ITrainingFfmpegPathResolver`. Der zentrale `ServiceProvider` verbindet die bisherige statische Trainings-API mit genau einer Instanz; die UI-Fassade enthält keinen Dateizugriff mehr. Sieben bestehende Fassadenfälle, ein direkter Dateitest und zwei Verdrahtungsprüfungen schützen Alias, Leerfall, vorhandene und fehlende Dateien.
- **Allgemeine FFmpeg-/FFprobe-Suche instanzbasiert (A1-10, Teilpaket):** Umgebungsvariable, WinGet-, Chocolatey-, Scoop-, manuelle und Program-Files-Suche sowie die Prüfung eines danebenliegenden `ffprobe.exe` liegen im `FfmpegFileLocator` hinter `IFfmpegExecutableLocator`. Der zentrale `ServiceProvider` verbindet die bisherige statische API mit genau einer Instanz; nur die reine Ableitung eines FFprobe-Namens bleibt statisch. Zwei echte Dateitests, die bestehenden Video-/Analyseprüfungen und zwei Verdrahtungsprüfungen schützen Suchreihenfolge, PATH-Rückfall und zentrale Nutzung.
- **Gemeinsamer Prozessausgabeleser instanzbasiert (A1-10, Teilpaket):** Prozessstart, paralleles Leeren von Standard- und Fehlerausgabe sowie das Beenden des gesamten gestarteten Prozessbaums bei Abbruch liegen im `ProcessOutputReaderService` hinter `IProcessOutputReader`. ffmpeg-, ffprobe-, Schnellscan- und Vollanalyse-Aufrufer behalten die bisherige statische Signatur und verwenden die zentral aufgebaute Instanz. Zwei echte Prozessfaelle, ein direkter Diensttest und eine Verdrahtungspruefung schuetzen Ausgaben, Rueckgabecode, Abbruch und zentrale Nutzung.
- **Video-Frame-Extraktion instanzbasiert (A1-10, Teilpaket):** ffmpeg-Aufruf, Temp-PNG, Rueckgabecodepruefung, Bildauslese und sichere Temp-Bereinigung liegen im `VideoFrameExtractionService` hinter `IVideoFrameExtractor`. Alle bestehenden Foto-, Trainings- und Analyseaufrufer behalten die statische Signatur und verwenden ueber die Fassade die zentral aufgebaute Instanz samt gemeinsamem Prozessausgabeleser. Drei gezielte Diensttests und eine Verdrahtungspruefung schuetzen Erfolg, Fehlercode, fehlende Videos, Argumente und Temp-Aufraeumen.
- **Trainingsvorschau-Erzeugung instanzbasiert (A1-10, Teilpaket):** Der Batch-Import erhält den zentralen `ITrainingPreviewFrameExtractor` gezielt statt eines statischen Dateizugriffs. Video-Prüfung, feste Zwei-Sekunden-Position, sichere Fall-ID und Fehlerisolation liegen im kleinen `TrainingPreviewFrameExtractionService`; die öffentliche statische API bleibt als Kompatibilitätsfassade erhalten. Drei Verhaltenstests, ein echter Injektionstest und zwei Verdrahtungsprüfungen schützen den Ablauf.
- **Sidecar-Startpfadsuche instanzbasiert (A1-10, Teilpaket):** Die Aufwärtssuche nach `sidecar/start_sidecar.ps1` und die Prüfung der Windows-PowerShell liegen im `SidecarScriptFileLocator` hinter `ISidecarScriptLocator`. Der zentrale `ServiceProvider` verbindet die bisherige statische Start-API mit genau einer Instanz; die Fassade enthält keinen Datei- oder Ordnerzugriff mehr. Zwei direkte Dateitests und zwei Verdrahtungsprüfungen schützen Suchreihenfolge, Systempfad, Rückfall und zentrale Nutzung.
- **Sidecar-Token-Auflösung instanzbasiert (A1-10, Teilpaket):** Die priorisierte Auflösung aus Programmeinstellung, kanonischer und alter Umgebungsvariable sowie lokaler `.sidecar_token`-Datei liegt im `SidecarTokenFileResolver` hinter `ISidecarTokenResolver`. Der zentrale `ServiceProvider` verbindet die bisherige statische API mit genau einer Instanz; nur die reine Token-Normalisierung bleibt statisch. Ein echter Datei- und Umgebungsrundlauf, die bestehenden HTTP-Header-Tests und zwei Verdrahtungsprüfungen schützen Priorität, Rückwärtskompatibilität und das Verbot, den Schlüssel an externe Ziele zu senden.
- **VSA-zu-YOLO-Klassenkarte instanzbasiert (A1-10, Teilpaket):** Laden, stabile Standard-IDs, automatische neue IDs und atomare Ausgabe von `yolo_class_map.json` sowie `classes.txt` liegen im `VsaYoloClassMapFileStore` hinter `IVsaYoloClassMapStore`. Der zentrale `ServiceProvider` verbindet die öffentliche statische API mit genau einer Instanz. Ein echter Datei-Rundlauf, die bestehende Standard-ID-Prüfung, die Export-/Live-Erkennungstests und zwei Verdrahtungsprüfungen schützen ID-Stabilität, Wiederladen und zentrale Nutzung.
- **KI-Prozess-Lebenszyklus instanzbasiert (A1-10, Teilpaket):** Vormerken und Beenden ausschliesslich selbst gestarteter Ollama-/Sidecar-Prozesse liegen im `AiStartedProcessLifetimeService` hinter `IAiStartedProcessLifetime`. Start, zentraler `ServiceProvider` und Programmende verwenden dieselbe Instanz; die bisherige statische API bleibt als kleine Kompatibilitätsfassade ohne Prozessverwaltung erhalten. Zwei echte Prozess-Isolationstests, der bestehende Fassadentest und eine Verdrahtungsprüfung schützen Prozessbesitz, Unterprozess-Beendigung und zentrale Nutzung.
- **GPU-Modellauswahl instanzbasiert (A1-10, Teilpaket):** Suche und Aufruf von `nvidia-smi` liegen im zentralen `GpuModelSelectionService` hinter `IGpuModelSelector`. Die statische API behält Konstanten, Profiltyp und reine Auto-Modus-Regel als Kompatibilitätsfassade; Modellschwellen und Rückfall bleiben unverändert. Drei isolierte Hardware-/Prozesssimulationen, die bestehenden Einstellungsprüfungen und eine Verdrahtungsprüfung schützen Systemordner, PATH, Zeitlimits und Modellwahl.
- **KI-Einstellungsauflösung instanzbasiert (A1-10, Teilpaket):** Zusammenführung von gespeicherten Werten, aktuellen und alten Umgebungsvariablen sowie GPU-Automatik liegt im zentralen `AiPlatformSettingsResolver` hinter `IAiPlatformSettingsResolver`. Die öffentliche statische API bleibt als Kompatibilitätsfassade; nur ihre reinen Parser bleiben statisch. Zwei isolierte Auflösungstests, die bestehenden Konfigurationsprüfungen und eine Verdrahtungsprüfung schützen Priorität, Alt-Namen, Token, Auto-Modell und Kontextgröße.
- **PDF-Dossier-Zusammenführung instanzbasiert (A1-10, Teilpaket):** Dateiprüfung, PdfPig-Zugriff und das Zusammenfügen erzeugter Berichte mit Originalprotokollen liegen im `PdfMergeService` hinter `IPdfMergeService`. Datenseite und Druckcenter erhalten dieselbe zentral aufgebaute Instanz; bestehende Konstruktoren und die öffentliche statische API bleiben kompatibel. Zwei echte PDF-Tests, 21 bestehende Druckablauftests und eine Verdrahtungsprüfung schützen Seitenreihenfolge, fehlende Dateien, Fehler-Rückfall und zentrale Nutzung.
- **Pipeline-Umgebungsoptionen instanzbasiert (A1-10, Teilpaket):** Klassifikator-Schalter, erwartetes YOLO-Modell und kompatible Erkennungsschwellen werden im zentralen `PipelineEnvironmentOptionsService` hinter `IPipelineEnvironmentOptions` gelesen. Batch- und Einzelframe-Pipeline erhalten den Vertrag gezielt; die öffentliche statische API bleibt als kompatible Fassade erhalten. Ein isolierter Quellen-Test, zwei direkte Verbraucherprüfungen, die bestehenden Pipeline-Verhaltenstests und eine Verdrahtungsprüfung schützen Standardwerte, Alt-Namen, Modellname, Schwellen und zentrale Nutzung.
- **Sicherungsmanifest-Integrität instanzbasiert (A1-10, Teilpaket):** Rekursive Dateisuche, SHA-256-Erzeugung und vollständige Manifestprüfung liegen im zentralen `BackupManifestIntegrityService` hinter `IBackupManifestIntegrityService`. Die Vollsicherung erhält diese Instanz direkt; die öffentliche statische API bleibt als kompatible Fassade für Prüfwerkzeug und bestehende Aufrufer erhalten. Ein echter Hash-Rundlauf, ein injizierter Manifestlauf, die bestehenden Beschädigungs- und Pfadsicherheitstests sowie eine Verdrahtungsprüfung schützen Inhalt, Fremdpfade und zentrale Nutzung.
- **Haltungs-Katasterindex instanzbasiert (A1-10, Teilpaket):** Prüfung, optionaler TSV-Neuaufbau und Laden des schnellen Haltungsindex liegen im `HaltungCadastreIndexProvider` hinter `IHaltungCadastreIndexProvider`. Die Exportseite erhält die zentrale Instanz direkt; die bisherigen statischen Signaturen bleiben als kompatible Fassade erhalten. Ein echter XTF-zu-TSV-Rundlauf und eine Verdrahtungsprüfung schützen Felder, Frischeprüfung und zentrale Nutzung.
- **Haltungs-Katastertabelle instanzbasiert (A1-10, Teilpaket):** Streaming aus grossen SIA405-XTF-Dateien, atomare TSV-Ausgabe, Wiedereinlesen und Frischeprüfung liegen im `HaltungCadastreTableFileStore` hinter `IHaltungCadastreTableStore`. Der Katasterindex erhält genau diese zentrale Instanz; die öffentliche statische API und die reine Schachtpaar-Zerlegung bleiben kompatibel. Der bestehende echte XTF-zu-TSV-Rundlauf und die erweiterte Verdrahtungsprüfung schützen Inhalt, Metadaten und zentrale Nutzung.
- **M150-/WinCan-MDB-Zeilenauslese instanzbasiert (A1-10, Teilpaket):** Temp-Skript, begrenzter PowerShell-/OLEDB-Aufruf, JSON-Einlesen und sichere Temp-Bereinigung liegen im `PowerShellM150MdbRowReader` hinter `IM150MdbRowReader`. Der bestehende M150-Parser verwendet die zentrale Instanz; seine internen und öffentlichen Aufrufe bleiben unverändert. Vier gezielte Prozess-/JSON-Prüfungen und eine Verdrahtungsprüfung schützen Timeout, Argumente, Fehlermeldung, Zeileninhalt und Aufräumen.
- **PDF-Textkorrektur der Verteilung instanzbasiert (A1-10, Teilpaket):** PdfPig-Zugriff, Temp-PDF-Ausgabe und sichere Bereinigung liegen im `PdfTextLayerRewriteService` hinter `IPdfTextLayerRewriter`. Haltungs- und Schachtverteilung verwenden ueber ihre kompatible Fassade dieselbe zentral aufgebaute Instanz; die Quelldatei bleibt unveraendert. Ein echter PDF-Rundlauf und eine Verdrahtungspruefung schuetzen Trefferzahl, neue Nummer, Ausgabedatei und zentrale Nutzung.
- **Player-Medienbesitz semantisch geprueft (A1-08, Teilpaket):** Zwei Typpruefungen untersuchen die kompilierten Klassen statt Dateitext: `PlayerWindow` darf nur `PlayerMediaRuntime` und `PlayerMediaHosts`, aber keine rohen LibVLC-/MediaPlayer-Felder oder -Eigenschaften besitzen; `PlayerMediaRuntime` darf diese Typen nicht als oeffentliche Felder oder Eigenschaften freigeben. Die dadurch ersetzte Textregel ist entfernt, der Sammeltest sank von 1.473 auf 1.458 Zeilen.
- **Live-Bestaetigungszustand semantisch geprueft (A1-08, Teilpaket):** Eine Typpruefung beweist am kompilierten Modell, dass `LiveDetectionController` den `DetectionConfirmationBuffer` besitzt und `PlayerWindow` weder diesen Speicher noch rohe Frame- oder Fundlisten-Felder haelt. Die gleichwertige Suche nach alten Feldnamen ist entfernt; der Sammeltest sank von 1.458 auf 1.442 Zeilen.

- **M150-XML-/Textquelle instanzbasiert (A1-10, Teilpaket):** Sicheres XML-Laden mit Leerraumtreue und der nur bei Bedarf verwendete UTF-8-Text-Rueckfall liegen im `M150XmlTextFileReader` hinter `IM150SourceFileReader`. Der statische M150-Parser behaelt seine Signaturen, verarbeitet aber nur noch gelieferte Inhalte und enthaelt keinen Dateizugriff mehr. Vier neue Quellen-/Rueckfalltests, die vier bestehenden M150-Importfaelle und eine Verdrahtungspruefung schuetzen XML-Zaehler, Warnung, Umlaute, verzoegertes Textlesen und zentrale Nutzung.

- **XTF-Haltungsleser instanzbasiert (A1-10, Teilpaket):** Dateipruefung, geschuetztes XML-Laden und die Zuordnung von Haltung sowie oberem und unterem Schacht liegen im `XtfHoldingFileReader` hinter `IXtfHoldingFileReader`. `XtfHelper` behaelt seine oeffentlichen Signaturen als kleine Fassade; die reine Dateinamenszuordnung bleibt statisch. Drei bestehende Verhaltensfaelle und eine Verdrahtungspruefung schuetzen Namespaces, kaputte und fehlende Dateien, Suchreihenfolge sowie zentrale Nutzung bei der PDF-Verteilung.

- **XTF-Stammdatenquelle instanzbasiert (A1-10, Teilpaket):** Rekursive XTF-Suche, Dateipruefung und geschuetztes XML-Laden liegen im `XtfStammdatenSourceReader` hinter `IXtfStammdatenSourceReader`. `XtfStammdatenExtractor` behaelt seine oeffentlichen Signaturen als kompatible Fassade und verarbeitet nur noch gelieferte XML-Dokumente; Material-, Laengen-, DN- und Nutzungsartregeln bleiben bewusst statisch. Vier Verhaltenstests und eine Verdrahtungspruefung schuetzen Namespaces, Dezimalwerte, Rundung, kaputte und fehlende Dateien, Mehrdatei-Merge, Injektion und zentrale Nutzung.

- **Verteil-PDF-Seitenleser instanzbasiert (A1-10, Teilpaket):** Begrenzte PDF-Textextraktion, PdfPig-Rueckfall und das Erhalten leerer Bildseiten liegen im `DistributionPdfPageReadingService` hinter `IDistributionPdfPageReader`. Haltungs-, Schacht- und Dichtheitsverteilung verwenden ueber ihre kompatible Fassade dieselbe zentral aufgebaute Instanz. Vier bestehende PDF-Verhaltenstests, eine direkte Injektionspruefung und eine Verdrahtungspruefung schuetzen Seitenzahl, Reihenfolge, Textnormalisierung, Bild-PDF-Rueckfall und zentrale Nutzung.

- **IBAK-/KIAS-PDF-Stammdatenquelle instanzbasiert (A1-10, Teilpaket):** Report-Ordnerwahl, rekursive PDF-Suche, Dateipruefung und begrenzte Textauslese liegen im `IbakPdfStammdatenSourceReader` hinter `IIbakPdfStammdatenSourceReader`. `IbakPdfStammdatenExtractor` behaelt seine oeffentlichen Signaturen als kompatible Fassade; seine Feldregeln und Quellenzusammenfuehrung bleiben bewusst statisch. Sieben Text-, Datei-, PDF- und Injektionstests sowie eine Verdrahtungspruefung schuetzen Zwei-Seiten-Grenze, Report-Vorrang, Fehlerfall, Feldwerte, Mehrdatei-Merge und zentrale Nutzung.
- **Wissenspfade instanzbasiert (A1-10, Teilpaket):** Umgebungsvariable, gespeicherter Pfad, Start-Cache, Ordneranlage und die einmalige Alt-Datenkopie liegen im `KnowledgeBasePathService` hinter `IKnowledgeBasePathService`. Der zentrale `ServiceProvider` verwendet die Instanz direkt; die oeffentliche statische API bleibt als kompatible Fassade erhalten. Sieben bestehende Pfadfaelle, ein Isolationstest, ein echter Migrationsrundlauf und eine Verdrahtungspruefung schuetzen Prioritaet, Cache, Originalerhalt und zentrale Nutzung.
- **Protokoll-PDF-Assets instanzbasiert (A1-10, Teilpaket):** Logo- und Fotoauflösung, begrenzte Projektsuche sowie die fehlertolerante Bildauslese liegen im `ProtocolPdfAssetFileResolver` hinter `IProtocolPdfAssetResolver`. Die Application-Schicht enthält nur noch den Vertrag; der konkrete PDF-Exporter erhält den Dienst im Konstruktor. Sein parameterloser Konstruktor und die interne Dossier-Kompatibilität bleiben erhalten. Acht Logo-/Fotopfadfälle, sechs Fotoseitenfälle und ein Schichttest schützen Suchreihenfolge, Umbenennungsrückfall, Deduplizierung, defekte Bilder und echte Injektion ohne Dateisystem.
- **KI-Beweisbild-Renderer instanzbasiert (A1-10, Teilpaket):** Bildladen, WPF-Markierung, Ordneranlage und PNG-Ausgabe liegen im `EvidenceFrameImageRenderer` hinter `IEvidenceFrameRenderer`. Defektvorschau und Trainingsframe-Speicher verwenden Instanzen; die bisherige statische API bleibt als dünne Kompatibilitätsfassade erhalten. Zwei echte Bildtests und ein neuer Injektionstest schützen Rohbild-Erhalt, SAM-Markierung und die austauschbare Vorschauausgabe.
- **Sicheres XML-Laden instanzbasiert (A1-10, Teilpaket):** Dateiöffnung, DTD-Sperre und blockierte externe Entitäten liegen im `SafeXmlDocumentLoader` hinter `ISafeXmlDocumentLoader`. Katalog-, WinCan-, XTF-, M150- und IBAK-Leser erhalten den Dienst als Abhängigkeit; die bisherige statische API bleibt als dünne Kompatibilitätsfassade erhalten. Der neue `LegacyXtfSourceReader` hält Formaterkennung und XML-Dateizugriff aus dem 997-zeiligen Altimporter heraus. Neun Sicherheits-, Leerraum-, Injektions- und Verdrahtungstests sowie 33 bestehende Importtests schützen XML-Härtung und Importverhalten.

Die nachfolgenden Fundstellen beschreiben weiterhin den Zustand **vor** dieser Umsetzung und bleiben als nachvollziehbares Audit erhalten. Noch offene mittel- und langfristige Punkte stehen in der Roadmap dieses Berichts.

> Stand: 2026-07-13 · Methode: 7 spezialisierte KI-Agenten (parallel, rein lesende Code-Analyse) auf Basis des committeten Standes. Kritische Funde wurden zur Gegenprüfung vorgesehen — **es trat kein Fund der Stufe „Kritisch" auf**, daher waren keine Gegenprüfungen nötig. Kein Framework-/Plattformwechsel vorgeschlagen.
> Belegte Zusatzprüfungen des Orchestrators sind mit ✔ markiert.
>
> **Nachtrag 2026-07-13:** `A6-02` ist umgesetzt. `tools/SidecarE2eSmoke`
> dekodierte drei Bilder aus einem echten Projektvideo und prüfte YOLO, DINO, SAM,
> Quantifizierung sowie die produktive Mehrmodell-Kette gegen einen Golden-Vertrag.
> Ergebnis auf der Zielmaschine: **PASS** (3/3 Bilder, 0 Modellfehler).

---

## 1. Gesamtbewertung Wartbarkeit & Stabilität

**Schulnote: 2,5 (gut–befriedigend).**

Das Programm ist im Normalbetrieb **stabil**: Es gibt keinen Fund mit Datenverlust-, Absturz- oder offenem Sicherheitsrisiko. Speichern ist durchgehend atomar, das Backup ist überdurchschnittlich sorgfältig (SHA256-Verify, SQLite-Online-Snapshot mit `integrity_check`, Versionierung, Ziel-Guards), SQL ist parametrisiert (keine Injection gefunden), die Token-Absicherung des KI-Dienstes ist sauber, und die Schichtarchitektur ist zyklenfrei mit aktiven Fitness-Tests.

Zwei Dinge ziehen die Note nach unten:

- **Blindflug im Fehlerfall (Release-Diagnostik):** Ein großer Teil der Warn-/Fehlerpfade schreibt nur nach `Debug.WriteLine` — das wird im Release-Build vom Compiler **entfernt**. Wenn produktiv etwas still schiefgeht, steht im Tageslog nichts. Für einen Solo-Entwickler, der zugleich Support ist, ist das das praktisch gefährlichste Einzelthema.
- **Belastbarkeit ist unbewiesen:** Die ~4.300 Tests decken die Rechenlogik hervorragend ab, aber es gibt **keinen** echten Dauer-/Belastungstest, **keinen** End-to-End-Test gegen einen laufenden KI-Dienst mit echtem Video und **keine** WPF-UI-Automation. Der auftragsrelevante Schwerpunkt „Belastung" ist damit isoliert nur mit Note 4 bewertbar.

**Bereichsnoten der 7 Agenten:**

| Bereich | Note | Kurzurteil |
|---|---|---|
| Architektur / große Klassen / MVVM | 3 | solide, aber versteckte God-Classes + MVVM-Lecks |
| Nebenläufigkeit / Ressourcen | 2 | überdurchschnittlich, wenige klare Lücken |
| Datei- & Projektsicherheit | 2 | defensiv gebaut, nur Härtungspunkte offen |
| Schnittstellen (PDF/DB/QGIS/Excel) | 2 | robust, Restrisiken bei Fremd-DB & Extremgrößen |
| KI-Dienst / GPU / Netzwerk | 2,5 | Kern-Sicherheit stark, Resilienz-Lücken |
| Testabdeckung / Belastung | 4 | Logik top, Dauer/E2E/UI praktisch abwesend |
| Wartbarkeit (Solo) | 2,5 | gutes Fundament, Diagnostik-Blindfleck |

---

## 2. Top-3-kritische Funde

Kein Fund erreichte „Kritisch"; die folgenden drei sind die schwerwiegendsten „Hoch"-Funde mit dem größten realen Effekt.

**T1 — Diagnostik verschwindet im Produktions-Build** (Agent 7, `A8-01`)
`BestEffort.Report` und 81 `Debug.WriteLine`-Stellen (u.a. „Trainings-JSON korrupt" in `TrainingSamplesStore.cs:225`) schreiben über `System.Diagnostics.Debug.WriteLine` — durch `[Conditional("DEBUG")]` im Release entfernt. Der gute `FileLogger` existiert, wird von diesen Pfaden aber nicht genutzt. **Folge:** Stille Fehler hinterlassen im Tageslog keine Spur.
→ *Zentrale Sink von `Debug` auf `Trace`/`ILogger` umstellen (Quick Win < 1h), dann in den Hot-Paths den Logger durchreichen.*

**T2 — Belastbarkeit ist nicht getestet** (Agent 6, `A6-01`/`A6-02`/`A6-03`)
Die 1.072 „UI-Tests" sind zu großen Teilen Quelltext-Guards (137 Dateien lesen `.cs` als String); nur ~25 instanziieren wirklich ein ViewModel. Zum Scan-Zeitpunkt gab es keinen E2E-Test gegen echten Sidecar/Video und keine Dauer-/Leak-Test-Infrastruktur. **Nachtrag:** Der echte Sidecar-/Video-Vertragstest (`A6-02`) ist inzwischen vorhanden und grün. Zusätzlich werden 15 zuvor völlig unberücksichtigte ViewModels mit 23 echten Verhaltenstests ausgeführt; zwei einfache WPF-Fenster besitzen einen Öffnen-/Schließen-Smoke. Der neue `NightlySoakRunner` kann die KI-Kette wiederholt fahren und RAM, Handles, Latenz sowie VRAM überwachen. Smokes der Haupt-/Fachfenster, die restliche Anwendungskette und der tatsächlich ausgeführte 8-Stunden-Lauf bleiben offen. → *Nachtlauf-Konzept in Punkt 6.*

**T3 — Grossdatei-Guard ist rot, aber das Push-Gate prüft ihn nicht** (Agent 1, `A1-01`/`A1-02` + ✔ Orchestrator-Prüfung)
✔ Verifiziert: `SchaechtePage.xaml.cs` hat **1001 Zeilen** — genau über der Guard-Grenze (>1000, leere Whitelist). Der `MaintainabilityFitnessTests` ist damit **aktuell rot**. ✔ Der pre-push-Hook führt nur `Infrastructure.Tests` + `Pipeline.Tests` aus — die **UI-Tests laufen nicht im Gate**, deshalb blockiert der rote Guard keinen Push und die „alles grün"-Meldung deckte ihn nie ab. Zusätzlich versteckt der reine Datei-Zähler echte God-Classes: ✔ `PlayerWindow` = **119 Dateien / 9.479 Zeilen** über partielle Klassen verteilt.
→ *SchaechtePage-Fachlogik auslagern (bringt Datei unter die Grenze), UI-Tests ins Gate aufnehmen, Guard auf „Zeilen je Typ" erweitern.*

---

## 3. Detaillierte Einzelergebnisse pro Schwerpunkt

### Schwerpunkt 1 — Zu große Klassen & Dateien

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A1-01 | Hoch → **teilweise erledigt** | `PlayerWindow` bleibt mit 82 partiellen Dateien und 4.462 aggregierten Zeilen groß; zahlreiche Coding-, LiveDetection- und Playback-Controller sowie Tastaturhilfe, Bedieneingaben, Positions-, Wiedergabe-, Codierungsnavigation, Erkennungsbestätigung, manuelles Training, dessen Aufbau, Startlebenszyklus, Stoppsteuerung, Markierwerkzeug-Steuerung, manuelle SAM-Segmentierung, Codiermodus-Ausstieg, Codierungsübernahme und Schließschutz, Codierungs-Bestätigung, Ereignislisten-/Statistikaktualisierung, Import-/KI-Spaltenwechsel, Importbefund-Bestätigung, KI-Befund-, KI-Analyse-, Trainingsspeicher- und Rohrgrenzen-Kontext, KI-Zustandsüberwachung, Live-KI-Statusanzeige, Coding-KI-Pulssteuerung sowie die vollständige manuelle Schema-/Kalibrierungssteuerung sind bereits ausgelagert; der tote zweite Ladeablauf und die private Coding-Kindsuche sind entfernt | Weiter in kleinen Verhaltenseinheiten extrahieren; die bisherigen Auslagerungen sind über fünfzig Verhaltensfälle geschützt |
| A1-02 | Hoch → **erledigt** | `SchaechtePage.xaml.cs` = ✔ 1001 Zeilen (Guard >1000, Whitelist leer → Test rot) | Fachlogik ist ausgelagert; die Datei liegt mit 930 Zeilen wieder unter der Grenze |
| A1-09 | Mittel → **erledigt** | `MaintainabilityFitnessTests.Partial_types_cannot_hide_growth_across_many_small_files` | Alle Dateien eines partiellen Typs werden gemeinsam gezählt; bekannte Altlasten sind exakt eingefroren, neue Typen über 2.000 Zeilen und jedes Wachstum schlagen fehl |
| A1-08 | Mittel → **erledigt** | `UiArchitectureGuardTests` schützt die Composition-Root-Grenze; alle übrigen Regeln liegen in ihren fachlichen Testklassen. Medienbesitz und Live-Bestaetigungszustand pruefen kompilierte Typen statt Namen; positive Struktur und Rückfallverbote stehen zusammen. `ArchitectureFitnessTests.cs` ist vollständig aufgelöst und gelöscht | Neue Architekturregeln direkt beim betroffenen Fachtest ergänzen; Quelltextprüfungen bei passenden künftigen Umbauten weiter durch Typ-/Verhaltenstests ersetzen |

### Schwerpunkt 2 — Versteckte UI-/Fachlogik-Abhängigkeiten

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A1-03 | Hoch → **erledigt** | `SchaechtePage.xaml.cs:929` `new ProjectCostStoreRepository(...)` + Persistenzlogik im Code-behind | In `SchachtMassnahmenDialogController` ausgelagert und testgeschützt |
| A1-04 | Hoch → **erledigt** | `ProtocolEntryEditorDialog.xaml.cs:540/609` KI-Vorschlag + VSA-Validierung im Dialog | KI-Ablauf und VSA-Gesamtprüfung sind in zwei kleine ViewModels ausgelagert und mit 10 fokussierten Tests geschützt; der Dialog sank auf 789 Zeilen |
| A1-05 | Hoch → **erledigt** | `ServiceProvider.cs:50` God-Container mit IO im Ktor; als Ganzes in ~15 ViewModels injiziert | Alle Fach-ViewModels speichern nur noch ihre gezielten Abhängigkeiten; auch die Datenseite samt Teildateien ist containerfrei. Die Shell nutzt den Container weiterhin bewusst als zentralen Aufbaupunkt für Seiten und Navigation |
| A1-07 | Mittel → **erledigt** | `TrainingCenterWindow.xaml.cs:83-84`, `MediaSearchWindow.xaml.cs:114` — Dienste per `new` | Medien-Suche, Training-Store, Import, KB-Diagnose, Review-Warteschlange, SAM und Few-Shot werden zentral verdrahtet; bedarfsgesteuertes Erzeugen ist testgeschützt |
| A1-06 | Mittel → **erledigt** | `Infrastructure/Reports/ProtocolPdfExporter.cs` und die übrigen QuestPDF-Renderer | Application enthält nur Verträge, Optionen und reine Berechnungen; ausschließlich Infrastructure referenziert QuestPDF. Öffentliche Typnamen und Namespaces bleiben kompatibel |
| A1-10 | Niedrig → **teilweise erledigt** | Protokoll-Neuerzeugung und -Trainingsspeicher, PDF-Dossier-Zusammenführung, PDF-Dokumenttyp-Textpraefix, Schacht-Ganzdokument-OCR und PDF-Formularfelder, Programm-Einstellungen samt Rettungspunkten, Korruptionsquarantaene und Legacy-Migration, Projektordner-Anlage, Projektdatei-, Drop-, Projektfoto-Referenz-, Programmwurzel- und Sicherungs-Repo-Pfadsuche, allgemeine FFmpeg-/FFprobe-Suche, gemeinsamer Prozessausgabeleser, Video-Frame-Extraktion und ffmpeg-Pfadprüfung des Selbsttrainings, Sidecar-Startpfad-, Token- und KI-Prozess-Lebenszyklus-Auflösung, persistierte VSA-zu-YOLO-Klassenkarte, KI-Frame-Fotos samt markierter Bildvorschau, Dossier-Fotoverfügbarkeit, Schacht-Dateizielsuche, Schacht-PDF-Auswahlerweiterung, Verteil-Dateiuebertragung und Video-Konfliktkandidaten, Inspektions- und Dichtheitsprotokoll-Suche, Kataster-XTF-, Offline-Kartenordner-, Offline-Kartenlayer-, VSA-Katalog- und VSA/XTF-Medienpfadsuche, Kanalexport-, KIAS-/IBAK-Ordnererkennung sowie KINS-Text-/DBF-Anreicherung und -Gesamtprotokollsuche, Schacht-Excelvorlagenleser, Git-Commit-Erkennung, Zielmarker-Schutz und SQLite-Schnappschuesse der Vollsicherung, Windows-Explorer-Start und sicheres Datei-/Ordneröffnen samt Ordneranlage, Training-Center-Einstellungen, Trainingssample- und Trainingsframe-Speicher, VSA-Schattenprotokoll, Sidecar-Telemetrie, Pipeline-Ablaufdateien, Video-Startfehlerberichte, Wissensdatenbank-WAL-Pruefschritt und -Gesundheitspruefung, Selbsttraining-Verlauf, Lehrer-Annotationen, KI-Sanierungssitzungen, Plan-PDF-Import, Haltungs-/Schachtumbenennung, Dropdown-Speicher, Import-Quelldateispeicher, Projekt-Rettungspunkte, Projekt-Rettung, Import-Quellarchiv, CSV-Importbericht, Dichtheits- und Kanalimport-Verteilung sowie die zerlegte KI-Wissenssicherung besitzen injizierbare Instanzdienste; ihre öffentlichen statischen APIs bleiben nur als Kompatibilitätsfassaden | Weitere IO-Klassen einzeln umstellen; reine Rechner statisch lassen |

### Schwerpunkt 3 — Nebenläufigkeit, Abbruch, Speicher

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A2-01 | Mittel → **erledigt** | `VideoFrameExtractor.cs:41-63` — ffmpeg-Prozess wird bei Abbruch nicht getötet | Einzelbild-Extraktion läuft über `ProcessOutputReader`; dieser beendet bei Abbruch/Fehler den gesamten Prozessbaum. Ein echter Abbruchtest schützt das Verhalten |
| A2-02 | Mittel → **erledigt** | `TrainingCenterWindow.xaml.cs` — keine CTS, SAM-Segmentierung ohne Token | Fenster-Lebensdauer-Token wird an SAM durchgereicht; Closing bricht Fenster- und ViewModel-Arbeit ab und gibt die Token-Quellen wiederholbar frei |
| A2-03 | Mittel → **erledigt** | `DichtheitImportDistributor.cs:112-134` — asynchroner KI-Aufruf konnte im Synchronisationskontext blockieren | KI-Aufruf läuft isoliert über `Task.Run`, wartet intern mit `ConfigureAwait(false)` und besitzt einen unabhängig wirksamen 25-Sekunden-Abbruch; Regressionstest mit blockiertem Oberflächen-Kontext ist grün |
| A2-04 | Mittel → **erledigt** | `DataPageVideoAnalysisController.cs:82` u.a. — `new HttpClient` pro Aufruf | Controller verwendet je konfiguriertem Zeitlimit einen langlebigen Client; `DataPageViewModel.Dispose()` gibt den Cache sicher und wiederholbar frei |
| A2-05 | Niedrig → **erledigt** | `VideoAnalysisPipelineWindow.xaml.cs:61-66` — CTS nie disposed | Fenster-Closing ruft erst `Cancel()` und anschließend `Dispose()` auf; Architekturtest schützt Reihenfolge |
| A2-06 | Niedrig → **erledigt** | `DataPage.xaml.cs` — Such-Debounce-Timer bei Unloaded nicht gestoppt | Unloaded stoppt Such- und Layout-Timer; Architekturtest schützt beide Aufräumpfade |

*Positiv bestätigt: SafeFireAndForget/BoundedBackgroundTaskRunner breit genutzt; PlayerWindow-LibVLC-Cleanup sauber; ShellViewModel meldet statische Events in Dispose ab; kein SkiaSharp-Bitmap-Leak (kommt im Code nicht vor).*

### Schwerpunkt 4 — Datei- & Projektsicherheit

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A3-01 | Mittel → **erledigt** | `FullBackupSourcesFactory.cs:53-70` — alle `SEWER*`-Env-Vars inkl. Tokens im Klartext in `umgebung.txt`/`RESTORE-ANLEITUNG.txt` | Gemeinsamer Redaktor schwärzt Werte von `*TOKEN*/*SECRET*/*KEY*/*AUTH*` in beiden Ausgabedateien; normale Werte bleiben wiederherstellbar |
| A3-02 | Mittel → **erledigt** | `FullBackupService.cs:434-493` — Manifest ohne Pro-Datei-Hashes; `DirectoryMirror` erkennt Änderung nur über Größe+Zeit | Manifest enthält Pfad, Länge und SHA-256 jeder aktuellen Datei; `FullBackupSmoke --verify-restore` prüft fehlende, zusätzliche und inhaltlich abweichende Dateien vor dem fachlichen Restore-Test |
| A3-03 | Niedrig → **erledigt** | `JsonProjectRepository.cs:94-104` / `AppSettings.cs:192` — Klartext-JSON inkl. `PipelineSidecarToken` | Token ist per DPAPI (`ProtectedData`, CurrentUser) geschützt; `projekt.json` bleibt wie vorgesehen Klartext |
| A3-04 | Niedrig → **erledigt** | `KnowledgeBasePaths.cs:131-147` — `SEWERSTUDIO_KNOWLEDGE_ROOT` ungeprüft als Wurzel | Leere, relative und ungültige Werte werden verworfen; gültige Overrides und Abweisungen landen im Tageslog |
| A3-05 | Niedrig → **erledigt** | `SafeShellOpen.cs:8-13` — `.html/.htm` in Whitelist, kein Pfad-Containment | `.html/.htm` sind aus der Freigabeliste entfernt; vorhandene Aufrufer benötigen diese Typen nicht |

### Schwerpunkt 5 — PDF-, Datenbank- & QGIS-Schnittstellen

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A4-01 | Mittel → **erledigt** | `IbakFdbConnectionOptions.cs:25-42` — IBAK-`.fdb` wird read-**write** (Embedded) geöffnet | Alle aktiven Firebird-Lesepfade arbeiten auf einer eindeutigen Temp-Kopie; das Original bleibt unangetastet |
| A4-02 | Niedrig → **erledigt** | `BuilderPageViewModel.Output.cs:269/224` — NPK-Export blockiert UI-Thread | CSV- und Excel-LV laufen als `async Task` über einen kleinen Hintergrund-Runner; Befehlsnamen bleiben kompatibel |
| A4-03 | Niedrig → **erledigt** | `ExcelTemplateExportService.cs` — ClosedXML hält gesamtes Workbook im RAM | Harte Obergrenze von 20.000 Datensätzen vor dem Laden der Arbeitsmappe; verständliche Fehlermeldung mit Aufteilungshinweis |
| A4-04 | Niedrig → **erledigt** | `PdfTextExtractor.cs:70/99` — Seiten-Budget nur im PdfPig-Fallback, nicht bei pdftotext | Seitenzahl wird vor `pdftotext` geprüft; beide Pfade begrenzen extrahierten Text auf 16 Millionen Zeichen |
| A4-05 | Niedrig → **erledigt** | `OfferHtmlToPdfRenderer.cs:84-145` — Playwright ohne hartes Gesamt-Timeout | Verknüpftes Zwei-Minuten-Limit durch Browser-, PDF- und Installationspfad; begrenztes Ressourcen-Aufräumen |
| A4-06 | Niedrig → **erledigt** | `IbakFdbConnectionOptions.cs:9-10` — SYSDBA/masterkey im Code (Env-übersteuerbar) | Defaults bleiben nur für lokale Embedded-Dateien; Serverpfade verlangen Benutzer und Passwort aus der Umgebung |

*Positiv bestätigt: SQL parametrisiert (keine Injection); WinCan `.db3` ReadOnly; QGIS-Bridge Loopback-only, GET-only, Exception-gekapselt, in `App.OnExit` disposed; große Haupt-Exports laufen korrekt via `Task.Run`.*

### Schwerpunkt 6 — KI-Dienst, GPU-Ausfälle & lokale Netzwerksicherheit

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A5-01 | Mittel → **erledigt** | `DefaultAiStartupLauncher.cs:68` / `App.xaml.cs:231` — Sidecar/Ollama bei App-Ende nicht beendet (~21 GB VRAM bleiben) | Von Sewer Studio gestartete KI-Prozesse werden per Windows-Job-Objekt verfolgt und beim App-Ende samt Unterprozessen beendet; bereits laufende Fremdprozesse bleiben unangetastet |
| A5-02 | Mittel → **erledigt** | `sidecar/main.py:92-144` — Trusted-Host-Check per Host-Header trivial umgehbar | Code, README und Regressionstest stellen klar: Host-Prüfung schützt gegen DNS-Rebinding; allein das verpflichtende Token ist die Zugriffssperre |
| A5-03 | Mittel → **erledigt** | `sidecar/config.py:9` + `start_sidecar.ps1:66` — `SEWER_SIDECAR_HOST` ungeprüft an uvicorn | Python-Konfiguration und Startskript erlauben ausschließlich Loopback; Nicht-Loopback bricht vor dem Dienststart mit verständlicher Meldung ab |
| A5-04 | Mittel → **erledigt** | `sidecar/main.py:77-89` + `VisionPipelineClient.cs:186-197` — CUDA-Fehler (kein OOM) → generischer 500, Client wrappt nicht | Typische CUDA-/Treiberfehler liefern einen bereinigten 503 mit stabilem Fehlercode; der bestehende 503-Pfad des Clients wiederholt einmal und meldet danach `SidecarUnavailableException` |
| A5-05 | Niedrig → **erledigt** | `sidecar/main.py:146-154` — Token-Enforcement fail-open bei leerem Token | Ohne initialisierten Server-Token werden alle Anfragen mit 503 und stabilem Fehlercode abgewiesen; ein leerer Token schaltet die Anmeldung nie aus |
| A5-06 | Niedrig → **erledigt** | `QgisBridgeServer.cs:68` — Bridge (8765) ohne Token, read-only Loopback | Einzelplatz-Annahme, reines Lesen und lokales Restrisiko sind dokumentiert und testgeschützt; auf Mehrbenutzer-Systemen ist die Bridge zu deaktivieren |
| A5-07 | Niedrig → **erledigt** | `AiStartupOrchestrator.cs:76-82` — Ollama ohne Auth, keine Loopback-Erzwingung | Autostart überschreibt `OLLAMA_HOST` prozessbezogen mit IPv4-Loopback und passendem Port; nichtlokale Adressen werden gewarnt und nicht automatisch gestartet |

*Positiv bestätigt: `secrets.token_urlsafe(32)`, `hmac.compare_digest`, Token nur an Loopback gesendet, Decompression-Bomb-Schutz, OOM → LRU-Eviction + 503 + 1 Retry.*

### Schwerpunkt 7 — Fehlende Belastungs- & Bedienungstests

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A6-01 | Hoch → **teilweise erledigt** | Kein öffentliches ViewModel ist mehr völlig ohne Testbezug; sechs Fenster besitzen echte Öffnen-/Schließen-Smokes | 15 zuvor unberücksichtigte ViewModels werden mit 23 Verhaltenstests direkt ausgeführt; Smokes des Hauptfensters sowie komplexer KI-/Protokollfenster bleiben offen |
| A6-02 | Hoch → **erledigt** | `tools/SidecarE2eSmoke` + `SidecarRealVideoIntegrationTests` | Echter Sidecar, reales Video, YOLO/DINO/SAM, Quantifizierung und Golden-Vertrag sind umgesetzt und auf der Zielmaschine grün |
| A6-03 | Hoch → **teilweise erledigt** | Kategorien und Push-Filter sind geschützt; `tools/NightlySoakRunner` fährt mehrere echte Videos im Wechsel und misst Zeit, Python-RAM, Handles, P95 sowie optional prozessbezogenen VRAM in CSV | Echten 8-Stunden-Lauf auf der Zielmaschine durchführen; Import, PDF-Export, KB und QGIS sind noch nicht Teil der Schleife |
| A6-04 | Mittel → **erledigt** | `QgisBridgeEndpointRouterTests` + `integrations/qgis/tests/test_bridge_http.py` | Alle C#-Endpunkte und der echte lokale Python-HTTP-Abruf sind ohne QGIS testbar; nichtlokale Ziele werden abgewiesen |
| A6-05 | Mittel → **erledigt** | `KnowledgeBaseInfrastructureTests.KnowledgeBaseContext_MigriertAlteDatenbankOhneDatenverlust` | Reale Alt-DB wird auf sechs neue Sample-Spalten migriert; Sample-, Embedding- und Versionsdaten bleiben vollständig erhalten |
| A6-06 | Mittel → **erledigt** | PDF: leer/abgeschnitten; XTF: kaputt vor gültig; WinCan: ungültige DB3; IBAK: ungültige FDB mit gültiger `Daten.txt` | Alle vier Importwege begrenzen Dateifehler; Folgedaten werden weiter verarbeitet und DB-Originale bleiben unverändert |
| A6-07 | Mittel → **erledigt** | `sidecar/tests/test_model_cpu_smoke.py` + vorhandene Endpunkt-Vertragstests | DINO- und SAM-2.1-Lader laufen mit Ersatzmodellen auf CPU; Auswahl, Device, Rückgabe und Antwortschema sind im normalen Testlauf geschützt |
| A6-08 | Niedrig → **erledigt** | `KnowledgeBaseConcurrencyTests.Retrieval_BleibtWaehrenOffenerRebuildTransaktionLesbar` | Offene Rebuild-Transaktion blockiert Retrieval nicht; alter Snapshot bleibt vollständig, nach Commit wird der neue Bestand geladen |

### Schwerpunkt 8 — Wartbarkeit für Solo-Entwickler

| ID | Schweregrad | Fundstelle | Empfehlung |
|---|---|---|---|
| A8-01 | Hoch → **erledigt** | `BestEffort.Write`, `App.xaml.cs` und `ReleaseOperationalLoggingArchitectureTests` | BestEffort-Warnungen gehen in das Tageslog, der Rückfall nutzt Release-taugliches `Trace`; `Debug.WriteLine` ist aus dem Produktionscode entfernt und der echte Dateilog ist getestet |
| A8-02 | Mittel → **erledigt** | Alle UI-Elternsuchen sind auf `VisualTreeSafe.GetParentSafe` und `FindAncestor` konsolidiert; die benannte Kindsuche des Players liegt zusätzlich in `FindNamedDescendant`. Verhaltenstests decken logische und visuelle Vorfahren, verschachtelte Treffer und sichere Leerfälle ab; Architekturtests verbieten neue Direktzugriffe und Player-Kopien | Zentralen Helfer beibehalten; neue Suchfälle dort ergänzen |
| A8-03 | Mittel → **erledigt** | `AuswertungPro.Dev.slnf` + `DevelopmentSolutionFilterTests` | Alltagsfilter führt nur 4 Produkt- und 4 Testprojekte; notwendige Projektverweise bleiben automatisch enthalten, eigenständige Tools entfallen |
| A8-04 | Mittel → **erledigt** | `AGENTS.md` + `ProjectAgentInstructionsTests` | Kurzer aktueller Einstieg verweist auf `CLAUDE.md`, nennt .NET 10, Sidecar, QGIS sowie verbindliche Wartungs- und Testregeln; veraltete Angaben sind testgeschützt entfernt |
| A8-05 | Niedrig → **erledigt** | `DiagnosticsPackageService` + `DiagnosticsPageViewModel` | Log-Ordner ist direkt erreichbar; begrenztes ZIP enthält bereinigte Logs und Systemangaben, keine Projektdateien; Erfolg und Fehler sind getestet |
| A8-06 | Niedrig → **erledigt** | `Directory.Build.props`, `.editorconfig`, `AnalyzerConfigurationTests` | `latest-recommended` ist aktiv; 40 bekannte Altregelarten bleiben als sichtbare IDE-Hinweise baseliniert, neue Regelarten werden zu Build-Warnungen |
| A8-07 | Niedrig → **erledigt** | `UserError`, `UserFacingException` und `UserErrorArchitectureTests`; KI-Sicherungs-, ViewModel-, Player- und Fotostatus nachgehärtet | Technische Ausnahmen werden zentral und verständlich abgebildet, vollständig protokolliert und weder im UI-Dialog noch in geschützten sichtbaren Statusfeldern roh angezeigt; bewusst sichere Fachhinweise bleiben möglich |

---

## 4. Quick Wins (< 2 Stunden, sofort umsetzbar)

1. ✔ **`A8-01`:** `BestEffort` schreibt wichtige Warnungen in das Tageslog und fällt ohne App-Sink auf `Trace` zurück; ein echter Dateilog-Test schützt den Release-Weg.
2. **`A3-01`:** Token-Werte in `umgebung.txt`/`RESTORE-ANLEITUNG.txt` redigieren (Namensfilter).
3. **`A2-01`:** ffmpeg bei Abbruch killen (`p.Kill(entireProcessTree:true)`).
4. **`A2-05`/`A2-06`:** CTS disposen + Such-Timer im Unloaded stoppen.
5. ✔ **`A8-03`:** `AuswertungPro.Dev.slnf` bietet den schlanken Alltags-Build mit 4 Produkt- und 4 Testprojekten.
6. ✔ **`A8-06`:** SDK-Analyzer laufen auf `latest-recommended`; eine dokumentierte Einstiegsbaseline verhindert Warnflut ohne die Prüfer abzuschalten.
7. ✔ **`A8-04`:** `AGENTS.md` ist ein kurzer aktueller Einstieg mit Verweis auf `CLAUDE.md`; ein Test verhindert veraltete Angaben.
8. **`A4-02`:** NPK-Export in `Task.Run` auslagern (kein UI-Einfrieren).
9. **`A5-02`/`A5-03`:** Host-Check-Kommentar klarstellen + Warnung bei Nicht-Loopback-Host.
10. **`A6-03` (Teil 1):** `[Trait]`-Kategorien einführen, damit Endurance-Tests trennbar werden.

---

## 5. Strategische Empfehlungen

1. **Diagnose-Sichtbarkeit vervollständigt:** Release-Logging (`A8-01`), Diagnosepaket (`A8-05`) und zentral gemappte Nutzerfehler (`A8-07`) sind erledigt.
2. **Grossdatei-Guard reparieren und ins Gate nehmen (`A1-09`, `A1-02`, Push-Hook):** Guard auf „Zeilen je Typ" erweitern (schließt das Partial-Schlupfloch), `SchaechtePage` unter die Grenze bringen, und die UI-Tests in den pre-push-Hook aufnehmen — sonst laufen Architektur-Guards nie automatisch.
3. **Echte God-Classes zerlegen statt weiter fragmentieren (`A1-01`, `A1-03`, `A1-04`):** PlayerWindow-Partials und die Fachlogik in SchaechtePage/ProtocolEntryEditorDialog schrittweise in Services/Controller mit Interface überführen — testgeschützt, kein Big-Bang.
4. **DI-Hygiene (`A1-05`, `A1-07`, `A1-10`):** ViewModels nur die benötigten Interfaces geben statt des ganzen `ServiceProvider`; per-`new`-Streuung in Fenstern beenden. Das macht Kernabläufe unit-testbar.
5. **KI-Prozess-Lebenszyklus (`A5-01`, `A5-04`):** ✔ Selbst gestartete Sidecar-/Ollama-Prozesse werden beim App-Ende kontrolliert gestoppt; CUDA-Ausfälle erscheinen als klarer, vorübergehender KI-Fehler.
6. **Fremd-DB schützen (`A4-01`):** IBAK-`.fdb` nur über eine Temp-Kopie/read-only anfassen — schützt Kunden-Originaldaten.

---

## 6. Vorschlag für erweiterte Teststrategie

**Ziel:** Die risikoreichsten Ketten (echtes Video, echter Sidecar, QGIS, WPF-Runtime, DB-Migration im Bestand) automatisiert absichern — sie laufen heute in **keinem** Test.

**Stufe A — Fundament (Quick Wins, Tage):**
- `[Trait]`-Kategorien (Unit/Integration/Endurance); Standardlauf + Push-Gate = nur Unit; UI-Tests ins Gate aufnehmen.
- Die ~46 ViewModels per `new + Command` testen (kein UI-Thread nötig) — ersetzt echte Deckung, die die String-Guards nur vortäuschen.

**Stufe B — Integration (auf der Zielmaschine, nicht CI):**
- ✔ **Headless Pipeline-Treiber** (`tools/SidecarE2eSmoke`): startet bei Bedarf einen echten Sidecar, dekodiert drei Videobilder, fährt YOLO/DINO/SAM→Quantifizierung durch und prüft den Vertrag gegen `golden/pipeline-contract.v1.json`. Als `[Trait("Category","Integration")]` maschinengebunden ausführbar.
- ✔ **Negativtests je Importer:** PDF, XTF, WinCan-DB3 und IBAK-FDB begrenzen kaputte/abgeschnittene Dateien statt den Gesamtimport abzubrechen (`A6-06`).
- ✔ **KB-Migrationstest** mit Alt-DB → Upgrade auf sechs neue Spalten + vollständiger Datenerhalt (`A6-05`).
- ✔ **QGIS-Bridge-Smoke:** echter lokaler Python-HTTP-Server + C#-Vertragstest für alle Endpunkte (`A6-04`).
- ✔ **SAM/DINO CPU-Smoke:** echte SewerStudio-Lader mit kleinen Ersatzmodellen plus GPU-freie Endpunkt-Vertragstests (`A6-07`).

**Stufe C — Nachtlauf / Belastung (`tools/NightlySoakRunner`, 8 h, maschinengebunden):**
- ✔ Erste fahrbare Stufe: Schleife über N Test-Videos mit echter KI-Auswertung und Golden-Vertrag; Video-Import, QualityGate, PDF-Export, KB-Index und QGIS-Sync bleiben als Erweiterung offen.
- ✔ Nach jeder Runde: `GC.Collect`, Assertion auf **Prozess-RAM-/Handle-Obergrenze** und **Sidecar-Latenz-Perzentil**; **VRAM** via `nvidia-smi` mitschreiben; Abbruch bei Regression/Leak. Ausgabe als sofort gespeicherte CSV.
- **UI-Runtime:** schmaler StaFact-Smoke pro Hauptfenster (öffnen/schließen ohne Exception), damit ein Oberflächen-Absturz im Dauerbetrieb auffällt.

**Wichtig:** LibVLC, GPU, QGIS und Ollama laufen nur auf der Zielmaschine — Stufe B/C sind bewusst **maschinengebundene Jobs**, kein CI. Das Push-Gate bleibt schnell (Unit).

---

## 7. Roadmap für die nächsten zwei Releases (nur Wartbarkeit, keine Technologiewechsel)

### Release N+1 — „Sichtbarkeit & Sicherheitsnetz" (~1 Woche)
- **Diagnose-Sichtbarkeit:** ✔ Release-Logging (`A8-01`), Diagnosepaket (`A8-05`) und Fehler-Mapper (`A8-07`) sind erledigt.
- **Test-Gate reparieren:** UI-Tests in den pre-push-Hook; `A1-09` (Guard je Typ); `A1-02`/`A1-03` (SchaechtePage-Fachlogik auslagern → Guard wieder grün).
- **Quick Wins:** ✔ `A3-01`, `A2-01`, `A2-05`, `A2-06`, `A8-03`, `A8-04`, `A8-06`, `A4-02`, `A5-02`/`A5-03` sind erledigt.
- **Ergebnis:** Fehler werden im Feld sichtbar, Architektur-Guards laufen automatisch, Build schneller.

### Release N+2 — „Belastbarkeit & Entkopplung" (~2 Wochen)
- **Teststrategie Stufe B + C:** ✔ Headless Pipeline-Treiber (`A6-02`), messbarer KI-Nachtlauf (`A6-03`, Werkzeug), QGIS-Brücken-Smoke (`A6-04`), KB-Bestandsmigration (`A6-05`), Importer-Negativtests (`A6-06`), CPU-Modell-Smokes (`A6-07`) und KB-Parallelzugriff (`A6-08`) erledigt; offen bleiben der echte 8-Stunden-Lauf und die restliche Anwendungskette.
- **God-Class-Abbau (testgeschützt):** PlayerWindow-Partials in Services überführen (`A1-01`); der ProtocolEntryEditorDialog ist bei KI und VSA-Validierung erledigt (`A1-04`).
- **DI-Hygiene:** ViewModels auf Interface-Konstruktoren umstellen (`A1-05`, `A1-07`), beginnend bei den am häufigsten geänderten.
- **KI-Resilienz:** ✔ Prozess-Lebenszyklus (`A5-01`), CUDA-Fehler-Klassifizierung (`A5-04`) und Schutz der IBAK-Originaldatei (`A4-01`) sind erledigt.
- **Ergebnis:** Der 8-Stunden-Nachtlauf ist fahrbar, die größten Wartungsbremsen sind entschärft, das KI-Subsystem verhält sich bei Ausfällen berechenbar.

---

### Offene Prüfungen (Grenzen dieses Scans)
- Rein statische Analyse, kein Build/Testlauf/Profiler/echter Fuzz-/Pentest (auftragsgemäß). Der rote Grossdatei-Guard (`A1-02`) und die Push-Hook-Lücke wurden vom Orchestrator per `wc -l` / Hook-Inspektion ✔ verifiziert, der tatsächliche UI-Testlauf nicht.
- Nicht vertieft: die ~60 CLI-Tools, Sidecar-Interna der einzelnen Inferenz-Routen, QuestPDF-Speicherverhalten bei sehr vielen Fotos, echte Fremd-DB-Beispieldateien, Laufzeit-Leak-Messung.
