# Build output
bin/
obj/
*.user
*.suo
.vs/
*.rsuser

# NuGet
*.nupkg
packages/
.nuget/

# Rider / JetBrains
.idea/

# Claude session data (inkl. .claude/worktrees/ — temporaere Arbeitskopien, nie committen)
.claude/

# OS
Thumbs.db
desktop.ini
.DS_Store

# Python (global) — vorher nur unter sidecar/, jetzt auch tools/, scripts/, etc.
**/__pycache__/
**/*.pyc

# Pytest / Coverage Artefakte (Phase 5.1)
**/.coverage
**/.coverage.*
**/coverage.xml
**/htmlcov/
**/.pytest_cache/

# Build artifacts
artifacts/

# Logs / temp
*.log
buildlog.txt
*.tmp

# Python Sidecar
sidecar/.venv/
sidecar/__pycache__/
sidecar/**/__pycache__/
sidecar/**/*.pyc
sidecar/models/**/*.pt
sidecar/models/**/*.pth
sidecar/models/**/*.bin
sidecar/models/**/*.onnx
sidecar/models/**/*.safetensors
sidecar/models/**/*.engine
sidecar/test_export_tmp/
sidecar/training_export/

# Sidecar Florence-2 Shadow-Log (281 Frame-JPGs, 6.6 GB) — generiert zur Laufzeit
sidecar/florence2_shadow_log/

# Sidecar Root-Modelle (analog zu Knowledge/-Frames-Ignore)
sidecar/*.pt
sidecar/*.engine
sidecar/*.onnx
sidecar/*.safetensors
sidecar/*.pth
sidecar/*.bin

# Lokale SDK/Runtime-Kopien (gross)
.dotnet/
.dotnet_cli/
.dotnet_home/

# IDE
.vscode/

# Rohdaten / Legacy (nicht ins Repo)
Rohdaten/
_legacy/
runs/

# YOLO-Modelle im Projektroot
*.pt
yolo_cls_export/
yolo_cls_export_v2/
yolo_cls_runs/

# KnowledgeBase SQLite (potentiell mehrere GB, nicht ins Repo)
**/Knowledge/*.db
**/Knowledge/*.db-wal
**/Knowledge/*.db-shm
**/KnowledgeBase.db

# WPF Temp-Projektdateien (Build-Artefakte)
*_wpftmp.csproj

# VS / dotnet generierter Build-Cache pro csproj
*.csproj.lscache

# Tools
tools/PdfImageAnalyzer/

# ── Audit-Fix 2026-04: Reproduzierbarkeit ───────────────────────────────
# Knowledge-Subverzeichnisse die regenerierbar / generiert sind und
# nicht ins Repo gehoeren. Verhindert, dass git status 100k+ Files zeigt.

# Generierte Frames + Few-Shot-Bilder (regenerierbar aus Videos)
Knowledge/fewshot_images/
Knowledge/frames/
Knowledge/frames_extracted/
Knowledge/frames_temp/
Knowledge/teacher_images/

# Trainings- und Lerndaten (regenerierbar / mit Backup-Mirror)
Knowledge/training_samples.json
Knowledge/teacher_annotations.json
Knowledge/measures_learning.json
Knowledge/measures-model.zip
Knowledge/yolo_runs/
Knowledge/florence2_shadow_log/
Knowledge/batch_processed.txt

# Generierte Markt-/Sanierungs-JSONs (kommen aus Python-Reparser, Quelle in Sanddisk)
Knowledge/sanierung/marktpreise_*.json
Knowledge/sanierung/historische_sanierungen.json

# JSON-Backups mit Zeitstempel (alle .bak_<timestamp>)
**/*.bak_*.json
**/*.bak_*.csv

# Test-Ausgaben
tests/**/TestResults/

# YOLO-Datasets im Repo-Root (gehoeren auf D:/yolo_sewer_v*)
yolo_sewer_v*/

# Config-Backups
src/**/Config/*.bak_*.json

# ── Repo-Hygiene 2026-05-08 ─────────────────────────────────────────────
# Sidecar-Modell-Hilfsdateien (Tokenizer, Vocab, Inference-Notebooks)
# regenerierbar aus dem Modell-Repo
sidecar/models/**/*.ipynb
sidecar/models/**/tokenizer.json
sidecar/models/**/vocab.json
sidecar/models/**/preprocessor_config.json
sidecar/models/**/special_tokens_map.json
sidecar/models/**/*.txt

# Benchmark-Frames sind generiert aus Eval-Set
benchmarks/frames/

# UI-Default-Settings ZIP (Build-Artefakt der Settings-Page)
src/AuswertungPro.Next.UI/AppSettings.zip

